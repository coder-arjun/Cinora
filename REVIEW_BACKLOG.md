# Cinora — Review Backlog

Deferred review findings (Medium/Low not fixed at discovery time). Each has a target milestone or is an accepted trade-off. Critical/High are NEVER deferred here — they block their milestone.

## Deferred from Phase 1 / Milestone 1.1 domain review (2026-07-02)

| ID | Sev | Finding | Decision / Target |
|----|-----|---------|-------------------|
| M2 | Med | `DomainException` doc promises HTTP 400 mapping, but §5 `GlobalExceptionHandler` maps only `ValidationException`→400 / `NotFoundException`→404 / else 500 — a `DomainException` would surface as 500. | **RESOLVED (Milestone 1.4):** `GlobalExceptionHandler` now maps `Cinora.Domain.Exceptions.DomainException`→400 ProblemDetails; covered by the `DomainException_maps_to_400_problem_details` integration test. |
| L1 | Low | `Friend` trips CA1716 (VB keyword); currently handled with a scoped, justified `[SuppressMessage]`. | **Accepted for now** (ships, respects the mandated schema name). Optional refinement: rename CLR type to `Friendship` + `ToTable("Friend")` in 1.2 to drop the suppression (needs a 1-line ADR). Revisit only if a zero-suppressions policy is adopted. |
| L2 | Low | Domain factories read `DateTime.UtcNow` directly → time-based fields non-deterministic to unit-test. | **Accepted (Phase 1 YAGNI).** Introduce `System.TimeProvider` (BCL — no dependency-rule breach) at factories when deterministic time tests are needed. |
| L5 | Low | Factories accept `Guid.Empty` for FK-style ids. | **Accepted.** DB referential integrity (1.2) rejects non-existent ids. Add `Guard.NotEmpty` later if in-domain rejection is wanted. |

## Active handoffs to Milestone 1.2 (database-agent) — NOT deferred, do in 1.2
- Configure `User.Id` (and all client-generated `Guid` PKs) as `ValueGeneratedNever()` so EF doesn't store-generate the key that must equal `ApplicationUser.Id`.
- `Rating`↔`int` value converter that validates on read (`v => Rating.From(v)`); add a `CHECK (Rating BETWEEN 1 AND 10)` column constraint (defense-in-depth for M1).
- Explicit column lengths matching the domain `*MaxLength` consts; set explicit lengths for `Device.Endpoint`/`P256dhKey`/`AuthSecret` (currently unbounded) rather than `nvarchar(max)`.
- No navigation properties by design → configure relationships with `HasOne<T>().WithMany().HasForeignKey(x => x.Prop)`; reads project via `IAppDbContext`.

_(All 1.2 handoffs above were implemented in Milestone 1.2 and verified by both reviews.)_

## From Milestone 1.2 review (2026-07-02)

**Fixed immediately in the 1.2 polish round** (not deferred): Movie unique index → `(TmdbId, MediaType)`; connection-string null fail-fast in `AddInfrastructure`; `IAppDbContext.Users`→`Users` table guard test.

**Blocking requirement for Milestone 1.3 (NOT a backlog item — must be met in 1.3):**
- Registration must create `ApplicationUser` (via `UserManager.CreateAsync`, which self-commits) and the `Domain.User` row in a **single explicit `IDbContextTransaction`**, because the shared-PK FK (`Users → AspNetUsers`) requires the identity row first and a failed second step would orphan an `AspNetUsers` row. Acceptance: an integration test proving a mid-registration failure leaves **no** `AspNetUsers` row.

**Deferred:**
| ID | Sev | Finding | Target |
|----|-----|---------|--------|
| A3 | Low | No `EnableRetryOnFailure` on `UseSqlServer` (transient-fault resiliency). | Phase 6 (irrelevant for LocalDB). |
| A6 | Low | All-`Restrict` User FKs push account/GDPR deletion into orchestrated app code (ordered child-delete then `Users` then `AspNetUsers`). | Account-deletion feature (later phase); prefer explicit orchestrated delete over cascades for auditability. |
| A5 | Low | ADR-0002 example says `Database=Cinora`; implementation uses `CinoraDev`. | **RESOLVED (2026-07-02, verified in the ADR-0008 doc pass):** ADR-0002's example string now reads `Database=CinoraDev` (its 2026-07-02 amendment + "Last verified against code" footer), matching `appsettings.Development.json`; no stale `Database=Cinora` example remains in `docs/`. |
| — | Info | `Friend` uniqueness is directed `(RequesterId, AddresseeId)` → `(A,B)` and `(B,A)` can coexist. Intended; enforce the "one relationship per pair" domain rule in Phase 4. | Phase 4. |

## From Milestone 1.3 (auth vertical)

**Bundled security-hardening pass — target BEFORE Phase 1 exit (after 1.6 Tailwind/TS so CSP nonces wire against the real asset pipeline). Owner: security-agent.**
- **Global fallback authorization policy** — currently auth is per-action `[Authorize]`/`[AllowAnonymous]`, so a newly added action with no attribute is anonymous by default (fail-open). Add a fallback policy requiring auth, with explicit `[AllowAnonymous]` opt-outs. _Highest-priority hardening item._
- **Security headers + CSP** with script nonces (co-design with the 1.6 Tailwind/TS pipeline).
- **Rate limiting** on `login` / `register` / `external-login` endpoints (.NET `RateLimiter`).

**Other 1.3 follow-ups:**
- `DomainException` → HTTP 400: ~~add an integration test at **1.4** (when `GlobalExceptionHandler` lands) asserting the MVC boundary maps it to 400 (currently 500)~~. **DONE (1.4)** — `DiagnosticsPipelineTests.DomainException_maps_to_400_problem_details` asserts the boundary returns 400 `application/problem+json`. Ties to backlog **M2** (resolved).
- **Live Google OAuth**: manual verification only — needs free client id/secret in user-secrets; automated suite covers the unconfigured-provider branch. Commands are in PROGRESS/1.3 report.
- `EnableRetryOnFailure` (A3) MUST stay off unless the registration transaction is wrapped in `Database.CreateExecutionStrategy().ExecuteAsync(...)` — enabling SQL retry otherwise breaks user-initiated transactions.
- Integration tests use LocalDB `CinoraTest` + a process-wide `ASPNETCORE_ENVIRONMENT=Testing`/connection-string env var set in the factory's static ctor (keeps Google unconfigured + deterministic). A second integration test class must reuse the factory via a non-parallel `ICollectionFixture` and avoid count-based assertions. (CI/Testcontainers path is moot under the local-only constraint.)

### Disposition after code + security review (2026-07-02)
**Fixed immediately (1.3 hardening fix round):** F1 global fallback authZ policy; F6 global `AutoValidateAntiforgeryToken`; F4 register user-enumeration → generic error; F5 external-login `email_verified` check; F7 clear external cookie; CR3 rollback with `CancellationToken.None`; CR4 null-guard instead of `user!`; CR6 document `EnableRetryOnFailure` coupling at the DI site.
**Fixed in the testing follow-up round:** CR1 CreateAsync-failure (duplicate-email) rollback test; CR2 `RegisterExternalAsync` failure-path tests; CR5 open-redirect negative test; CR7 delete vacuous `SmokeTests.cs`.
**Bundled hardening pass — ✅ COMPLETE (Milestone 1.6b, 2026-07-02):** F1 (global fallback authZ, done in 1.3), **F2** security headers + strict CSP (`default-src 'self'`; no nonces needed — no inline scripts/styles) + HSTS (non-Dev) + `UseHttpsRedirection`, **F3** per-IP fixed-window rate limiting on `login`/`register`/`external-login` (429; 10/min Prod, relaxed 10k/min Dev/Testing, config-overridable). Verified: headers present, CSP doesn't block same-origin hashed assets, 84/84 tests green. **Future CSP extensions (do NOT widen early):** Phase 2 add `https://image.tmdb.org` to `img-src` (posters); when interactive Alpine components land use the `@alpinejs/csp` build (never `'unsafe-eval'`); add `connect-src`/rate-limit policies for review/comment/like writes + any TMDB/AI proxy endpoints in their phases.

## From Milestone 1.4 review (2026-07-02)
**Fixed immediately (1.4 fix round):** #1 `UseSerilogRequestLogging` outermost (4xx no longer logged as Error/500 w/ stack trace); #2 fail loud on duplicate handler registration; #3 diagnostics env-guard tightened to Development/Testing; + behavior-order and error-path log-level lock-in tests.
**Accepted / deferred:**
- #5 (Low) — mediator `Send`/`Handle` omit the `Async` suffix: **accepted**, deliberate mirror of the MediatR API shape to keep swap-cost low; no change.
- #4 (Low) — `Sender.WrapperCache` keyed by request type only; a covariant `Send<object>` of the same request under a different `TResponse` would `InvalidCastException`. **Theoretical** (TResponse is inferred; one-TResponse-per-request invariant holds). Revisit only if covariant dispatch is ever intended (key cache on `(requestType, responseType)`).
- Deploy assertion: prod must have `ASPNETCORE_ENVIRONMENT=Production` exactly (a typo like `Prod` would expose diagnostics). **DONE (1.7)** — startup `Log.Warning` guard on unrecognized environment names.

## From Phase 1 exit review (2026-07-02) — all documentation-only
- **Doc pass (documentation-agent, in flight):** write ADR 0004 (free/local-only infrastructure) + ADR 0005 (hand-rolled mediator vs MediatR — licensing); sweep `solution-structure.md` §4/§5/§7/§8 "MediatR" → "hand-rolled mediator (ISender)"; update §5/§6 options names to `AiOptions`/`CacheOptions`/`FileStorageOptions`; reconcile ADR 0001 §3 ("never on Infrastructure implementation types") with ADR 0003's auth carve-out (Web may use Identity types for the auth vertical); fix ADR 0002 example connection string (`Cinora`→`CinoraDev`, closes A5).
- **#4 (Low, optional) — security headers not applied to exception-handler ProblemDetails** (the handler rewrites the response after `UseSecurityHeaders`). Acceptable (JSON error bodies; CSP not security-relevant). If universal coverage wanted, emit headers via `Response.OnStarting` or move `UseSecurityHeaders` outermost. → security-agent, optional.
- **Optional (deferrable):** lift `IUserRegistrationService`/`UserRegistrationResult` into Application as a port (signatures are primitives→result) for stricter Web→Infrastructure purity — architecture-agent sign-off. Not required.

### ⚠️ Global auth contracts introduced in 1.3 — future milestone briefs MUST honor these
- **Fallback authZ is fail-closed:** every new endpoint requires authentication unless marked `[AllowAnonymous]`. Explicitly required later: **1.4** — the global exception handler's error output must be reachable anonymously; **1.7** — `/health` must be `[AllowAnonymous]`; **Phase 2** — public movie-browsing actions must be `[AllowAnonymous]`.
- **Global `AutoValidateAntiforgeryToken` (opt-out):** every state-changing POST/PUT/PATCH/DELETE must carry the token. **1.6** must configure the HTMX `RequestVerificationToken` header once in the shared layout; any webhook/non-browser POST needs `[IgnoreAntiforgeryToken]`. (OAuth callback is GET — unaffected.)
- **`email_verified` (F5)** relies on the `ClaimActions.MapJsonKey("email_verified", ...)` added in `AddGoogle`; if removed, external accounts become `EmailConfirmed=false` (fail-closed, safe). Add a test double once an external-scheme fake exists.

## From Milestone 2.2 fix-round review (2026-07-02)

Deferred findings from the catalog-persistence fix-round (two-committed-step model + its H-1 fix; see ADR 0008's 2026-07-02 Amendments). All Critical/High were fixed in-round — H-1 was the leftover `Added` genres re-flushing into the step-2 title save and re-tripping the unique `Genre.TmdbGenreId` index (fixed by `IAppDbContext.DiscardPendingChanges()` before step 2). The items below are Medium/Low and accepted trade-offs.

| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Med | Both genre-race catches treat ANY `DbUpdateException` as a benign unique-index race and reconcile, so a genuinely different `DbUpdateException` (e.g. a foreign-key violation) is masked as a race. Currently mitigated only by the Warning-level `CatalogLog.GenreInsertRaceReconciled` log. | Narrow both catches to genuine SQL unique-constraint violations (SQL Server error `2601`/`2627`) via a provider-agnostic exception-classifier port, so a non-race `DbUpdateException` propagates instead of being reconciled as a benign race. Target: Phase 3 (or when a third race-catch consumer appears). |
| Low | `SyncGenresResult` under-reports under a partial race: `Total` (`Inserted + AlreadyPresent`) can be less than the count of distinct incoming genres, and genres still missing after the race are not signalled to the caller. | Accepted — the sync is idempotent and re-run (startup/on-demand), so any still-missing genre is inserted on the next run. Add a `StillMissing` field / accurate `Total` only if a caller needs the exact partition. |
| Low | A partial genre-race in `EnsureTitleCached` can leave a permanently-missing `MovieGenre` link: if a referenced genre is still unresolvable after the reload, `LinkGenres` defensively skips it, the title is created, and every later touch returns `AlreadyCached` — so the skipped link is never re-added. | Accepted (rare: needs a genre-insert race concurrent with the first touch of a title referencing that exact genre). Revisit with the ADR-0008 aggregate-extraction trigger, or add a link-reconciliation pass. |
| Low | F3 conflates a TMDB 404 and a blank/unnamed title under one `EnsureTitleOutcome.NotFound` value — callers cannot distinguish "TMDB has no such id" from "TMDB returned a title with no resolvable name". Cosmetic granularity only. | Accepted. Split into two outcomes only if a caller must react differently. |
| Low / chore | The `GHSA-2m69-gcr7-jv3q` `NuGetAuditSuppress` (test-only SQLite native `SQLitePCLRaw.lib.e_sqlite3`, scoped to `tests/Cinora.Application.Tests/Cinora.Application.Tests.csproj`) is a temporary exception pending a patched native in the offline cache. | Remove the suppression and bump `Microsoft.EntityFrameworkCore.Sqlite` to `10.0.8` once a patched `SQLitePCLRaw` native is available. The machine now has internet, so this is feasible soon. |

Note: the near-duplicate genre-upsert between `SyncGenresCommandHandler` and `EnsureTitleCachedCommandHandler.EnsureGenresAsync` (a DRY item) is **already tracked** as ADR-0008 Phase-3 trigger 3 ("the duplicated genre-upsert becomes worth consolidating behind one seam"), so it is not repeated here.

## From Milestone 2.3 review (2026-07-02)

Deferred findings from the Home / discovery-rails milestone. All four gates (code+security, performance, UI ×2 iterations) closed at **0 Critical / 0 High**. The UI **High** (no rail failure state → infinite shimmer on a TMDB error) and its two Mediums (stuck `aria-busy`; edge-fade obscuring focus ring / rating badge) were **fixed in-milestone** (`DiscoveryController.Rail` try/catch → `_RailError` 200 partial with a CSP-safe `hx-get` retry; lazy containers switched to `hx-swap="outerHTML"` so the busy container is replaced; `.rail` edge-fade capped ≤55% / 0.5rem / starts below the badge row; regression test **T6**). The rest are Medium/Low.

| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Med | **`OutputCache` not wired** on the anonymous, no-vary rail GET — every `/discover` view re-runs the MVC pipeline + cache-deserialize + Razor render even on a warm TMDB cache (the network cost is already absorbed by `CachedTmdbClient`). | Wire `AddOutputCache`/`UseOutputCache` + a `DiscoveryRails` policy (short TTL ≤ the TMDB TTLs, `VaryByQuery("kind","media")`, tag for invalidation) at **Milestone 2.5**, so rails + details share one tested policy. Confirm the cached hit still carries the CSP header and doesn't collide with the rate limiter. |
| Med | **Keyboard focus is lost to `<body>` after a successful Retry** on a failed rail — the focused Retry button lives inside the `outerHTML`-swapped `[data-rail-error]` and no app-level focus-restoration handler exists. | Add an `htmx:afterSwap` focus handoff to the swapped-in rail root (`tabindex="-1"` + focus) or an `id`-based restore. frontend-agent. |
| Low | Magic-string Details route `"/discover/title/{media}/{tmdbId}"` in `_TitleCard.cshtml` duplicates the route contract across view + tests. | Centralize via `Url.Action`/a named route/shared constant when the **2.5** `Details` action exists (its absence is why `Url.Action` returns null today). |
| Low | `TitleCardVm.MediaType` vs `RailVm.Media`/`RailSlot.Media` — two names for one concept (matches the §10.2 spec). | Optional consistency cleanup. |
| Low | **CLS on skeleton→content swap** (~3–5px): skeleton text-bar heights/margins are slightly under the rendered title+year line boxes (the poster 2:3 box IS correctly reserved). | Match skeleton text-block height to the rendered lines, or give the card text block a fixed `min-height`. Confirm at 2.7 Lighthouse. |
| Low | **LCP hero fade-in:** `.animate-rise-in` fades the large `text-display` "Discover" H1 (likely LCP element) from `opacity:0` over 400ms, which can defer LCP. Reduced-motion unaffected. | Animate the hero entrance with `transform` only (no `opacity:0` start on the LCP element); measure at 2.7. |
| Low | **Motion durations** slightly over budget — poster hover-zoom 400ms + entrance 400ms vs micro-interactions 150–250ms / entrances ≤350ms. | Trim zoom→~250–300ms, entrance→≤350ms (Phase-6 polish). |
| Low | **Retry has no in-flight affordance** — a slow retry looks inert and can be double-fired (no `hx-indicator`/`hx-disabled-elt`). | Add `hx-disabled-elt="this"` + a CSS-only `.htmx-request` spinner. frontend-agent. |
| Low | **No JS-off fallback** — if HTMX fails to load, `hx-trigger="load"` never fires and rails stay skeletons (the no-JS analogue of the fixed High). | Add a `<noscript>` hint or a minimal server-rendered baseline. |
| Low | **No `srcset`/`sizes`** on poster cards — `w342` is slightly soft on large high-DPR screens (bytes otherwise optimal). | Optional: emit `srcset "w342 342w, w500 500w"` + `sizes`, weighed against the extra bytes. |
| Low | **Heading density** — each card title is an `<h3>` (~18–20 headings/page) for SR heading-nav. Defensible (titled cards). | Optional: render card titles as `<p>`/`<span>`, keep the section `<h2>`s. |
| Low (test) | Edge-fade vs focus-ring worst edge measured **3.02:1** (above the 3.0 threshold for non-text, but tight). | Lock it with a Playwright pixel/contrast check on a focused edge card. testing-agent. |
| Low / infra | **No response compression** (Brotli/Gzip) — `Program.cs` has no `AddResponseCompression`/`UseResponseCompression`; affects rail partials + CSS/JS payloads. App-wide infra, not 2.3-specific. | backend-agent — provision for the self-hosted profile (consider static-asset pre-compression too). |

## From Milestone 2.4 review (2026-07-03)

Three gates ran on Search — **security ✅ / code ✅ (0 Crit / 0 High)**; **UI 🔧→✅** (**1 High** + 2 a11y Med **fixed in-milestone**: deep-link/no-JS→JS box-clearing → Alpine `init()` seeds `query` from a `data-query` root attr; the whole-grid `aria-live` flooding the SR → replaced by a dedicated visually-hidden `#search-status` polite region carrying a concise announcement; focus lost after Retry → scoped `htmx:afterSwap` focus-restore to `#search-results` with `tabindex="-1"`).

**Fixed in-milestone (not deferred):** per-IP **`public-read` rate-limit policy** on the anonymous TMDB-proxying browse endpoints (security Med) — applied at the `DiscoveryController` class level so **every current + future action there (incl. 2.5 `Details`) inherits it**; **standing rule: any anonymous endpoint that proxies TMDB must carry `RateLimitingPolicies.PublicRead`**. Also `htmx.config.allowEval=false` (Low, defense-in-depth), clamped-page echo into the retry URL (Low), and the CSP `FUTURE:` comment refreshed for 2.4.

| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Low | **Restore-prompt-on-clear** — clearing the box below `MinQueryLength` leaves stale results + a mismatched "N results" summary (per §11.7 "optional polish"). | Optional UX nicety — fire the results endpoint (200 `_SearchPrompt`) or swap the prompt back on clear. frontend-agent. |
| Low | **Out-of-order live-search responses** — `htmx.ajax` per debounced keystroke doesn't abort a prior in-flight request to `#search-results`; a slow earlier response could overwrite a newer one (rare behind the 300 ms debounce). | Abort/supersede the prior request if it surfaces. frontend/performance. |
| Low | **Movie/Series toggle deferred** — `media` is wired for forward-compat but Search (and the Home rails, §10.1) are Movie-only. Now that CSP Alpine exists a toggle is feasible. | Recommend a small follow-up milestone (product/UX call). |
| Low | **`Search` full-page action has no try/catch** — a post-resilience TMDB failure on a no-JS/deep-link page yields a normal 500 (the 200-grammar covers only the HTMX partial path). By design; untested. | Accepted; add a friendly full-page error view if a no-JS error path matters. |
| Info | Bundle retains 2 **HTMX-origin** dynamic-eval sites (an `eval` call + the `Function` constructor) — dormant (no `hx-on`/`js:` in markup), CSP-blocked, and now also runtime-disabled (`allowEval=false`); the `@alpinejs/csp` build itself is eval-free (B1 met). | No action. |
| — (perf, carried) | **`OutputCache`** on the anonymous no-vary Discovery GETs — still open from 2.3. **Superseded by the Phase 2 exit decision below (DEFERRED, accepted).** | See exit section. |

## From Milestone 2.5 + Phase 2 EXIT review (2026-07-03)

**Phase 2 exit gate: PASSED at 0 Critical / 0 High across all five dimensions** — architecture ✅ + performance ✅ (holistic), security ✅ (holistic anonymous surface), code ✅ (2.5 Details), UI 🔧→✅ (2.5 Details: 2 blocking High **fixed in-milestone** — hero scrim AA contrast over bright backdrops; cast-rail keyboard operability WCAG 2.1.1). Build 0/0, 148 tests green.

**Fixed in-milestone (not deferred):** `Overview` whitespace-normalization in `GetTitleDetailsQuery.FromDetails` (VM "null-when-none" contract parity); `public-read` rate-limit Prod permit **60→120** (avoid false-positive 429 on an engaged browse + search-as-you-type session); strengthened the D1 render test (tagline/year/TMDB-vote). UI: hero scrim held opaque across the text band + opaque chip/pill own-fills + hero `ink-subtle`→`ink-muted`; cast `.rail-scroller` made focusable (`tabindex="0"` + accessible name).

**OutputCache — DEFERRED (accepted at the Phase 2 exit by architecture-agent).** The dominant cost — the TMDB network round-trip — is already absorbed by `CachedTmdbClient`; OutputCache would only save the cheap Razor render on repeat hits, and correct wiring is genuinely delicate against the pipeline: it can short-circuit before the `public-read` limiter and must not serve a response that skips `SecurityHeadersMiddleware`'s single CSP header (mis-ordering = a security regression, not just a perf miss). Not worth shipping unreviewed at the exit. Pull forward when a measured render-cost signal appears or multi-instance scaling lands (design the `IDistributedCache`/OutputCache story together then). Owner: performance-agent (+ security-agent for the ordering review).

| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Low | Full-page `Search`/`Details` reads (not the HTMX partials) have no graceful-degradation try/catch → a **sustained** post-resilience TMDB outage surfaces a 500 on a full-page nav (the HTMX partial paths already degrade to a 200 error/retry). | Optional: wrap the two full-page reads in the log-Warning-and-render-friendly grammar, or a discovery-unavailable view. Edge case (transient blips absorbed by resilience; a full-nav 500 is conventional). backend-agent. |
| Low | `Details` GET first-touch write (`EnsureTitleCachedCommand`) sits on the request path (idempotent/indexed/guarded/rate-limited today). | Move behind a background enqueue when **Hangfire** lands (Phase 3+/5), per ADR 0007. |
| Low | Genre-upsert logic duplicated across `SyncGenresCommand` + `EnsureTitleCachedCommand.EnsureGenresAsync`. | Extract per the ADR 0008 port-extraction trigger when a 3rd consumer / real DRY need appears. |
| Low (sec) | `CacheLog.ReadFailed/WriteFailed` log the full search cache key (embeds the normalized query) at Warning — low sensitivity (movie-title free text), only on rare cache-infra failure. Pre-2.3 component. | Redact/hash the query portion (or log only the key prefix). backend/performance-agent (`src/Cinora.Infrastructure/Tmdb/CacheLog.cs`). |
| Low (sec/deploy) | Rate-limit partition = `RemoteIpAddress` with no `UseForwardedHeaders`; behind a future reverse proxy all clients collapse into one bucket. Not exploitable on the local/self-hosted target. | Add `ForwardedHeaders` + trusted-proxy allow-list before `UseRateLimiter` **in the deployment plan** (plans-only) if a proxy is introduced. |
| Low (test) | No test proving the `Details` persistence-degradation branch (persist throws → page still 200 + Warning). Needs a fault-injecting `IAppDbContext`/decorator seam. | testing-agent — add the seam + test. |
| Low (ui) | Watchlist `disabled` stub isn't keyboard-focusable/discoverable; "Add to Watchlist Soon" slightly cryptic. Correct for a true Phase-2 inert stub. | Optional `aria-disabled="true"` + focusable + explicit label if roadmap discoverability is wanted. Revisit when wired (Phase 4). |
| Low (ui) | One-item "Back to Discover" wrapped in `nav[aria-label="Breadcrumb"]` — a one-item trail isn't really a breadcrumb. | Build a real `Discover › {Title}` trail, or drop the breadcrumb label and treat as a plain back link. |

## From Milestone 3.1 (Reviews CRUD) review (2026-07-03)

Three gates ran — **security ✅ / code ✅ (0 Crit / 0 High)**; **UI 🔧→✅** (0 Crit, **4 High fixed in-milestone**). Build 0/0, 170 tests.

**Fixed in-milestone (not deferred):**
- **UI H1** — own review rendered twice with a duplicate DOM id (pinned + in the public list) → `GetTitleReviewsQuery` now **excludes the signed-in viewer's own review** from the public list (appears only pinned in `#my-review`); anonymous unaffected.
- **UI H2** — focus lost to `<body>` after every review swap → `site.ts` afterSwap **focus-restore** extended to the review regions (swapped-in card / heading anchor after delete), scoped so it doesn't disturb `#search-results`.
- **UI H3** — silent no-op on write failure (429/400/404/500) → scoped `htmx:responseError` handler surfaces an inline `#review-error` (429 gets a specific message); `asp-validation-summary` added for the no-JS 400 path.
- **UI H4** — `aria-live` on the whole review regions flooded the SR → removed; concise announcements now route through one `#reviews-status` polite node (reusing the 2.4 mechanism).
- **UI M1** — lazy review reads (`ReviewsController.Mine`, `DiscoveryController.Reviews`) now degrade to a 200 `_ReviewsError` retry partial (2.3 rail grammar) instead of a 500; `aria-busy` on the lazy skeletons.
- **UI M2** — rating input restyled to the star-pill `N/10` grammar (kept a labelled, keyboard-operable, value-announced numeric control — not a mouse-only star widget).
- **Security Medium** — `app.UseRateLimiter()` moved **after** `UseAuthentication()` so the new per-user `social-write` partition actually reads the authenticated principal (was silently degrading to per-IP).
- **Code Low** — collapsed the dual `Users` author subqueries in `ReviewProjection` into one; **doc** — design §5.1 `MyReviewVm?`→`ReviewVm?`.
- Proactively added the **`social-write` per-user rate-limit policy** on the review write actions (design §10.4).

| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Low | `CreateReviewResult.AlreadyExisted` is computed but unused — a duplicate submit silently returns the existing card + drops the re-typed input, with no "you already reviewed this — edit instead" signal. | ux-agent — surface an "already reviewed" affordance from the flag. |
| Low | After deleting your review, `#my-review` empties (no write form) — you can't re-review without a reload; focus goes to the heading anchor (H2). | Follow-up: have delete (always from `#my-review` after H1) return/refresh the `_ReviewWriteForm` so re-review is one action. frontend-agent. |
| Low | No-JS write error → a raw ProblemDetails page instead of the form re-rendered with inline errors (the HTMX path is covered by `htmx:responseError`; `asp-validation-summary` is wired but only populates if the action re-renders the form on `ValidationException`). | Optional: catch `ValidationException` in Create/Update and re-render the form with `ModelState` errors for the no-JS path. backend-agent. |
| Low (test) | Coverage gaps: no positive owner-edit test (UpdatedAtUtc + returned card), no cross-user **DELETE**→403 (only EDIT is R3), no `EnsureTitleCached`→404 "review a title TMDB lacks" branch, no **same-timestamp** keyset collision exercising the `Id` tie-break, no failure-path test for the new `_ReviewsError` (200 + `data-reviews-error`). | testing-agent — add the cases. |
| Low (ui) | Delete confirm is native `window.confirm` (jarring vs the premium dark theme; accessible + CSP-safe, so acceptable now). | Optional styled inline "Delete? Confirm/Cancel" (CSP-safe Alpine) — Phase 6 polish. |
| Low (accepted, sec) | `GET /reviews/{id}/edit` returns 403 (exists-not-yours) vs 404 (missing), allowing id enumeration. | **Accepted by design (§2):** reviews are public, so 403-vs-404 leaks nothing beyond the anonymous list. No action. |
| Info | Load-more announces a count only if the backend list fragment carries a `data-review-announce` string; absent it, load-more is silently appended (initial load also silent — no flooding). | Optional: add `data-review-announce="N more reviews loaded"` to `_ReviewListPage`. |

## From Milestone 3.2 (Likes + Comments) review (2026-07-03)

Three gates — **security ✅ / code ✅ (0 Crit / 0 High)**; **UI 🔧→✅** (0 Crit, **3 High fixed in-milestone**). Build 0/0, 181 tests.

**Fixed in-milestone (not deferred):**
- **Code Medium** — `LikeReviewCommand` re-probes-or-rethrows in its `DbUpdateException` catch (was masking non-PK faults as a successful like); **Code Low** — `UnlikeReviewCommand` now guards `DbUpdateConcurrencyException` (double-unlike no-op instead of 500).
- **UI H1** — the 3.1 review-swap `site.ts` handler misfired on nested like/comment swaps (unlike announced "Review deleted"): handlers now **scoped by target element** with dedicated announcements. **UI H2** — like button given a stable `id` so HTMX restores focus in place; comment-delete focus routed to an anchor. **UI H3** — comment thread restructured to a cards-only `#comment-items` list with the load-more sentinel + a **CSS-driven `:empty` empty-state** as siblings, so a just-posted comment lands correctly and the empty-state returns after the last delete. **UI M4/M5/M6** — like count folded into the `aria-label`; footer comment-count kept fresh via `data-comment-count`; per-feature error messages incl. the `/comments/*` delete path. **UI M7/M8** — `aria-expanded` on the comments disclosure; anon "Sign in to comment" link.

**3.5 forward obligations (recorded; MUST honor in 3.5):**
- **[SECURITY] Razor-encode `Notification.Message`** in the inbox/toast/bell — it stores the actor's raw `DisplayName`, so a `<script>` display name becomes **stored XSS** at that render point if not `@`-encoded. A 3.5 security-gate check.
- **Like re-notify dedup** — a like→unlike→like re-notifies (no dedup). Skip the co-persist when a recent `ReviewLiked` for the same `(ActorUserId, TargetId=ReviewId)` already exists. (Rate-limited nuisance today, not a hole.)

| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Low (sec) | Public `GET /reviews/{id}/comments` carries no rate-limit policy (correctly not `social-write`; bounded — `take` clamped to 50, single indexed keyset read, requires a known Guid). | Optional per-IP fixed-window ("public-read"-style) throttle for defense-in-depth. security/performance-agent. |
| Low (test) | L6 seeds distinct timestamps, so the `(CreatedAtUtc, Id)` **tie-break** on tied rows is never exercised, and page-2 cursor is built from seed rather than the server-returned `NextCursor`/sentinel. Add a like→unlike→like notification test once dedup semantics are set. | testing-agent — add a same-timestamp collision + server-cursor assertion. |
| Low (ui) | **Multi-page comment ordering edge:** a just-posted comment appends inside `#comment-items` while a load-more page self-replaces the sentinel as a sibling after it — so in a rare >1-page thread a fresh comment can render before an already-loaded newer page. Single-page threads (the common case) are exact. | Accept (the OOB-append alternative was deliberately excluded to keep the fix frontend-only). Revisit if strict cross-page ordering is wanted. |
| Low (ui) | Comments disclosure is **one-way** (`aria-expanded` false→true on `click once`, no collapse). A legitimate expanded-disclosure pattern. | Optional: a name-only CSP-safe Alpine `x-data` toggle for collapse — Phase 6 polish. |
| Low (arch) | The whole reviews+comments section is **JS-gated** (lazy `hx-get` on load; the comments toggle needs JS; `CommentsController.Add` returns a bare `_CommentCard` with no `HX-Request` guard, so a no-JS POST yields a fragment as the whole document). Contradicts "core reading renders without JS" but is **inherited from the 3.1 lazy-load architecture**, not a 3.2 regression. | frontend/architecture — decide the no-JS baseline for reviews/comments (a later polish; the reading path is JS-enhanced today). |
| Low (ui, accepted) | The review author sees the comment write form on their own review. | **Accepted** — authors replying in their own thread is a reasonable product choice. No action. |

## From Milestone 3.3 (Friends + profiles) review (2026-07-03)

Three gates — **security ✅ / code ✅ (0 Crit / 0 High)**; **UI 🔧→✅** (0 Crit, **2 High fixed in-milestone**). Build 0/0, 188 tests.

**Fixed in-milestone (not deferred):**
- **UI H1 (F1)** — profile "Remove" swapped the control to empty (no re-add/focus/announce): `FriendsController.Remove` now **branches on the `HX-Target` header** — profile context re-renders `_ProfileFriendControl` (via `GetProfileQuery`, post-removal "Add friend" state); friends-list context keeps the empty-200 (card disappears). **UI H2 (F2)** — the friends surface had NO `site.ts` handler, so every accept/decline/add/remove dropped focus to `<body>`: added a scoped friends focus/announce handler mirroring the review one. **UI F3** — added persistent `#friends-status` polite nodes (Friends + Profile) for reliable, concise SR announcements. **UI F4** — friends/requests lists made **CSS-driven empty-states** + dropped the stale `(N)` heading count. **UI F5/F6/F7** — unified the outgoing label to "Request sent"; relationship-aware private-profile copy (keeps "private"); `title` on truncating name links.
- **Code Low** — `GetPendingRequestsQuery` dual `Users` subqueries folded into one (mirrors `FriendProjections.FriendUserRow`).
- **Code Medium (index)** — `IX_Friends_AddresseeId_Status` (+ `IX_Friends_RequesterId_Status`) added via the `FriendLookupIndexes` migration (applied to `CinoraTest`; **`CinoraDev` apply carried** with the `ReviewListIndex` caveat).

| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Low (test) | The **declined-re-request** path (delete prior declined row → insert fresh on the directed unique key; relies on EF's delete-before-insert reorder) is untested, as are `RemoveFriendCommand` (idempotent, accepted-only) and the `AlreadyFriends`/`AlreadyRequested` no-op outcomes (F3 covers only `ReciprocalPending`). | testing-agent — add F8 declined-re-request (fresh Pending, no unique-index exception) + F9 remove-friend (gone + idempotent 2nd DELETE) + the two no-op outcomes. |
| Low (ui/ux) | Profile header renders `@Model.Reviews.Count reviews` but `Reviews` is capped at `ReviewsPageSize=20` → ">20 reviews" shows "20". | Label it "Recent reviews" OR project a true `ReviewCount`. ux-agent call. |
| Low (sec, accepted) | **Reciprocal-request race (§15):** two concurrent sends for a pair can both pass the both-directions guard pre-commit → a rare `(A,B)`+`(B,A)` double + duplicate notification (directed unique index still blocks exact-duplicate directed rows; self-reconcilable). | **Accepted §15.** Tighten later with a canonical-pair unique index or a serializable re-check only if it matters. |
| Low (sec, accepted) | `GET /users/{guid}` is an existence + name/avatar oracle for any authed viewer (the intended §7.3 "limited" tier; Guids unguessable). | **Accepted** — intended limited tier. Revisit only if a username/handle scheme is added. |
| Low (ui) | Friend-write failures announce **politely** via `#friends-status` (not an assertive visible error like the review `#review-error`); newly-accepted friend not injected into the list (reload shows it). | Optional follow-ups (assertive friend error surface; live list injection). frontend/ux. |
| — (product, §15) | **CancelFriendRequestCommand** (retract an outgoing request) is out of the 3.3 route contract (outgoing = read-only "Request sent"). | Optional follow-up milestone. |

## From Milestone 3.4 (Activity feed) review (2026-07-03)

Gates — **performance ✅ / code ✅ (0 Crit / 0 High / 0 Med)**; **UI 🔧→✅** (0 Crit, **1 High fixed in-milestone**). No security gate (no new writes — read-only feed). Build 0/0, 192 tests.

**Fixed in-milestone (not deferred):**
- **UI H1** — the feed shipped with NO `site.ts` handler, so a successful `_FeedError` Retry swap dropped focus to `<body>`: added a scoped feed focus/announce handler (mirrors the search one; focus restores to the now-focusable `#feed-items`). **UI M2** — poster link given a distinct accessible name (`"{title} poster"`) vs the title link. **UI M3** — `#feed-items` now `<ul role="list">`/`<li>` with an sr-only "Recent activity" region heading (matches `_SearchResults`). **UI M4** — the inert `#feed-status` polite node is now written by the handler ("N more items loaded"/"You're all caught up" via `data-feed-announce`). **UI M5/M6/M8** — sr-only "· liked by you"; inline-link underline; warmer terminal copy + dropped the low-value no-friends "Your friends" secondary CTA.
- **Perf/Code Medium (index)** — `IX_Reviews_UserId_CreatedAtUtc` on `Review(UserId, CreatedAtUtc)` added via the `FeedIndex` migration (perf-review-confirmed shape: no INCLUDE; clustered `Id` appended → keyset tie-break index-supported). `CinoraTest` applied; `CinoraDev` carried.

| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Low (perf) | The feed pulls the full `Body` (nvarchar(4000)) DB→app for every row then truncates to ~200 chars in memory (~80 KB/page → ~4 KB rendered). The `FeedItemProjection` comment claiming `SUBSTRING` isn't translatable is **wrong** — EF's SQL Server provider translates `string.Substring(int,int)`/`.Length`. | Push the feed excerpt truncation into SQL (`Body.Length <= 201 ? Body : Body.Substring(0,201)`) + fix the comment. **Feed only** (the Details reviews list legitimately needs the full body). Measure first (marginal on co-located LocalDB). backend/performance-agent. |
| Low (test) | A1 seeds the friendship only requester-direction (addressee-direction branch of `AcceptedFriendIdsAsync` untested); A2 uses distinct timestamps so the **same-`CreatedAtUtc` keyset tie-break** is never hit; no test for the `_FeedError` 200 path or the `_FeedPage` terminal marker. | testing-agent — add viewer-as-addressee, same-timestamp tie-break, and terminal/error-path cases. |
| Low (code) | The accepted-friends predicate + "other id" selector are repeated across the three `FriendProjections` helpers (drift risk). | Optional: extract a shared `Expression<Func<Friend,bool>> AcceptedFor(userId)` + other-id selector. backend-agent. |
| Low (ui) | `AuthorAvatarFileKey` is projected on `FeedItemVm` but never rendered (card always shows the initial monogram — consistent with `_ReviewCard`). Avatar image upload is Phase 4 (`IFileStorage`). | Render the avatar when it exists (monogram fallback) OR drop the unused field until avatars ship (Phase 4). frontend/backend. |
| Low (ui) | `_FeedError`'s Retry button has no stable `id`, so focus is restored to `#feed-items` (robust interim) rather than the button in place. | Optional: give the Retry button a stable id for in-place focus restore. backend/frontend. |
| Info (code) | `review.UserId != me` is redundant (`friendIds` can't contain `me` — self-friend is domain-rejected). Harmless defense-in-depth. | Leave as-is. |
| — (product, ADR 0012 §3/§4) | Feed v1 = friends' **reviews only**; likes/comments as feed items (`UNION`) and a denormalized `ActivityEvent` fan-out table are **deferred** behind the ADR-0012 triggers. | Later — mixed feed / materialization when read-time cost or the §4 trigger warrants. |

## From Milestone 3.5 + PHASE 3 EXIT review (2026-07-03)

**Phase 3 exit gate: PASSED at 0 Critical / 0 High across all five dimensions** — architecture holistic ✅, security holistic ✅, performance holistic ✅, code ✅, UI 🔧→✅. Build 0/0, 201 tests.

**Fixed in-milestone (not deferred):**
- **UI H1** — the live unread badge toggled the `hidden` DOM *property* but visibility is governed by the `hidden` *class* → never appeared on 0→N. Fixed: `classList.toggle("hidden", …)`. **+ M1** live bell `aria-label` sync; **M2/M3** scoped notifications `htmx:afterSwap` handler (announce via `#notification-status` + focus-restore to `#notification-items`); **M4** toast burst-hardening (cap 3 + `overflow-y:auto` + pause-on-hover/focus + focus-move on dismiss); **L1** badge cap parity (`9+`).
- **Security Low** — `NotificationHub.OnConnectedAsync` now **aborts** a connection with a null `UserIdentifier` (defense-in-depth) instead of joining a shared `user-` group.
- **Code Low** — added **L7** (`Relike_after_unlike_does_not_create_a_second_notification`) guarding the headline 3.5 dedup obligation (the direct-double-like L1 short-circuits before the dedup branch).
- **Arch Low** — `Program.cs` `UseSerilogRequestLogging` comment now lists the `ForbiddenAccessException → 403` arm.

| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Low (arch) | The `Notification` **construction** (actor DisplayName lookup → `Notification.Create` message → `NotificationDto` build) is replicated across the 4 producing handlers (the *push* is DRY via `RealtimeNotificationDispatcher`, but the build is not). | Extract a shared `NotificationFactory.StageAndBuildDto(...)` so the message/DTO shape is single-sourced. backend-agent. (Coupling itself is sound.) |
| Low (perf) | The like-path dedup `AnyAsync` filters `(RecipientUserId, ActorUserId, TargetId, Type)`; no index covers `ActorUserId`/`TargetId`/`Type` → a per-like residual scan of the recipient's notifications. Bounded per-recipient. | Optional covering/filtered index if the like path proves hot. performance + database-agent. |
| Low (perf) | `MarkAllNotificationsReadCommand` materializes ALL unread rows (`ToListAsync`) then domain-mutates each. Bounded in practice. | Switch to set-based `ExecuteUpdateAsync(n.IsRead, true)` (accepting the domain-method bypass) if unread volumes grow. backend-agent. |
| Low (perf) | `NotificationBellViewComponent` runs one indexed unread `COUNT` on **every authenticated page render** (adds a DB round-trip to authed TTFB); on the inbox page it's a 2nd identical COUNT. | Short-TTL `IDistributedCache` per user (invalidate on create/mark-read), or render the badge empty and let `/notifications/unread-count` + SignalR seed it. Only if authed TTFB is measured to matter. |
| Low/Med (perf, frontend) | `@microsoft/signalr` (~56 KB min) rides the single shared `site-*.js` on **every** page (anonymous pages never open a hub — `connectNotifications()` early-returns). | Lazy-load via dynamic `import()` gated on `#notification-unread-badge`, or a separate authed-only bundle. frontend-agent. |
| Med (perf, carried from 2.3) | **No response compression** — `UseStaticFiles()` with no `AddResponseCompression`/`MapStaticAssets()`, so `site-*.js`/CSS serve uncompressed (Brotli est. ~184 KB → ~55 KB). Amplifies the SignalR-in-bundle item; a mobile-Lighthouse risk. | **Phase 6 / deployment:** switch to `MapStaticAssets()` (build-time gzip+brotli) or `AddResponseCompression` + Brotli. backend-agent (`Program.cs`). |
| Low (ui) | Inbox **mark-read/read-all** do a full **redirect** (resets scroll + focus to top) — a "single-row live swap is not wired". | Wire a single-row HTMX swap (in-place read state) or redirect to a `#notification-{id}` anchor. frontend-agent. |
| Low (ui) | Toast "View" link tap target ~18 px (< 24×24, WCAG 2.5.8); notification icon `aria-label` duplicates the message text (mild SR verbosity). | Pad "View" ≥24 px; consider `aria-hidden` on the icon chip. frontend-agent. |
| Low (test) | Per-handler push-failure assertion (N2 proves it through the like handler only — comment/friend safety is transitive via the shared dispatcher); + a test that a dispatcher `OperationCanceledException` propagates. Low value. | testing-agent, optional. |

### Outstanding manual / environment items (Phase 3 close — NOT code defects)
- **`CinoraDev` (localhost SQL Express) migration apply** — all 5 migrations are on `CinoraTest` (LocalDB, tests green) but NOT `CinoraDev` (Express unreachable this session). Apply the reviewed idempotent `artifacts/migrate.sql` (or `scripts/db-update.ps1`) when Express is up. Never auto-migrate on startup.
- **2.7 live TMDB smoke** — Home/Search/Details against the real key (set) in a browser; confirm posters render + repeat requests hit the in-memory cache.
- **3.5 live realtime smoke** — two browser sessions: A likes B's review → B sees a live toast + badge increment without reload; confirm the same-origin `wss://` connects under `connect-src 'self'`.

## From Milestone 4.1 (IFileStorage foundation + avatar upload/serving) review (2026-07-03)

**4.1 review gates: PASSED at 0 Critical / 0 High across all three gates** — security (the key gate for this upload-critical milestone) ✅, architecture ✅, code ✅. Build 0/0, 241 tests.

**Fixed in-milestone (post-review hardening — NOT deferred):**
- **Medium (all 3 gates) — mint↔serve `PublicBasePath` drift.** `MediaController` hardcoded `[Route("uploads")]` while `LocalFileStorage.GetUrl` built the URL from `FileStorageOptions.PublicBasePath`; a changed option would silently 404 every avatar with no boot error. Fixed: a shared `AvatarServingRoute` const (single source of truth for both sides) + `FileStorageRouteValidateOptions` (fail-fast at boot under `.ValidateOnStart()` when, for the `Local` provider, `PublicBasePath` no longer resolves to the controller route — loud > silent).
- **Low (security + code) — key-regex `$`→`\z`.** `AvatarStorageKey.KeyPattern` ended with `$`, which in non-Multiline .NET regex matches before a single trailing `\n`, so `avatars/{32hex}.jpg\n` satisfied `IsValid`. Not attacker-reachable (keys are server-minted + HMAC-gated), but it is the single key-grammar authority → tightened to `\z` + a regression test.
- **Low (code) — +6 edge tests** cementing load-bearing invariants: empty-file (0-byte) reject (no file left), exact-cap-boundary success (`total > max` inclusive), truncated-header reject ×2 (2-byte JPEG / 11-byte WebP short-stream guards), 304 conditional-GET (strong ETag → `If-None-Match` → 304), and the `\z` trailing-newline regression.

| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Low (perf) | `LocalFileStorage.TryReadAsync` reads the whole file into a `byte[]` and computes a SHA-256 content-hash ETag on **every** avatar GET — including conditional GETs that 304 — so the "cacheable avatar" goal is only half realized. Bounded by the 5 MB cap + the `PublicRead` per-IP limit (all 3 gates agreed this is NOT a security blocker). | **performance-agent (Phase 4/6 perf pass):** serve via `PhysicalFile`/`FileStreamResult` and derive a cheaper validator (cached hash, or a weak length+last-write-time ETag) so `If-None-Match` short-circuits before the read+rehash. |
| Low (doc) | ADR 0001's Web→Infrastructure-type carve-out list names only the **auth** vertical; the 4.1 media-serving seam (`MediaController` → Infrastructure `IAvatarContentSource`/`AvatarContent`) is a **second** deliberate, ADR-0013-justified carve-out but is undocumented in ADR 0001. | **documentation-agent:** one-line ADR-0001 amendment naming the media-serving seam as a sanctioned second Web→Infra carve-out; also note the mint↔serve `AvatarServingRoute` coupling + the boot validator in ADR 0013 §2.4 and any deploy runbook that documents `FileStorage:PublicBasePath`. |
| Low (test, cosmetic) | The **test-local** `KeyPattern` copy in `LocalFileStorageTests` still uses `$` (harmless — it only ever matches freshly minted, newline-free keys; the real authority `AvatarStorageKey` is `\z` and has a direct regression test). | Optional: align the test-local copy to `\z` for consistency. testing-agent. |
| Info (accepted, ADR 0013) | Magic-byte sniffing validates only the leading ≤12 bytes → a polyglot (valid image header + appended HTML/JS) is stored, but is neutralized by forced true content-type + `X-Content-Type-Options: nosniff` under `script-src 'self'` (cannot execute). Re-encode/thumbnail is a license-gated (no free codec) deferred follow-up. | No action — documented residual risk. |
| Info (deployment) | Rate-limit partitions key on `RemoteIpAddress` (all policies, pre-existing); behind a reverse proxy without `ForwardedHeaders`, `PublicRead` buckets all clients as one IP. | **Deployment plan:** enable `ForwardedHeaders` if 4.x is ever fronted by a proxy. Also: Production MUST supply `FileStorage:UrlSigningKey` via user-secrets/env (already fail-fast enforced). |
| Info (accepted, ADR 0013 §13) | Best-effort avatar delete orphans a file on delete failure (never fails the request). | An orphan-sweep chore is owed later (§13). Out of 4.1 scope. |

## From Milestone 4.2 (Profile edit + avatar upload + settings + app-wide _Avatar) review (2026-07-03)

**4.2 review gates: PASSED at 0 Critical / 0 High across all three gates** — security (key gate: upload + anti-forgery + CSP) ✅, UI ✅, code ✅. Build 0/0, 275 tests.

**Fixed in-milestone (post-review hardening — NOT deferred):**
- **UI Medium — duplicate radio `id`.** Both privacy radios auto-generated `id="IsProfilePublic"` (invalid document). → distinct explicit ids (`visibility-public`/`visibility-friends`), `asp-for` kept for binding.
- **Security+code Low (cross-gate consensus) — `MaxUploadBytes` ↔ `RequestSizeLimit` drift.** `[RequestSizeLimit]` needs a compile-time const (6 MiB) while the precise cap is `FileStorageOptions.MaxUploadBytes` (5 MB default); raising the option above the const would silently 413 valid uploads. → fail-fast boot guard in `Program.cs` asserting `MaxUploadBytes ≤ AvatarRequestSizeLimitBytes`.
- **Code Low — compensating avatar delete honored a cancelled token.** The best-effort/compensating `DeleteAsync` calls passed the request `ct`; a cancellation-triggered compensation could skip cleanup and orphan the just-saved file (latent — `LocalFileStorage.DeleteAsync` ignores `ct` today). → `CancellationToken.None` on the cleanup deletes.
- **Security Low — empty-file guard shape.** `BadRequest(string)` → `ValidationProblem` (RFC-7807, consistent 400).
- **Code Low — projection test gap.** +3 tests: `AvatarFileKey` flows into `FriendVm`/`PendingRequestVm`/`ProfileVm`.
- **UI Low — a11y polish.** `aria-describedby` now includes the validation span; `role="alert"` unhidden before `textContent` (settings + the mirrored Phase-3 review handler) so assertive announces fire; rune-safe monogram initial (`EnumerateRunes()`) so an emoji/surrogate-pair leading name isn't a broken half-glyph; a latent bug fixed (standalone monogram was silent to SRs) + the `Decorative` flag (defaults labelled/safe).

| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Low (ui, progressive-enhancement) | "Remove photo" is JS-only (`hx-delete` button), while avatar Upload has a no-JS `<form>` fallback — asymmetric. | **frontend-agent:** wrap Remove in a real `<form method="post" asp-action="RemoveAvatar">` and let HTMX enhance, matching Upload. Needs a no-JS controller branch. |
| Low (a11y) | Nav avatar is (correctly) labelled — it's the sole identifier on mobile — but on `sm:` the adjacent visible name span duplicates it → minor SR double-read on desktop. Do NOT make the avatar decorative (would silence the mobile chip). | **frontend-agent:** optionally `aria-hidden` the desktop name span. Minor. |
| Low (perf, ui) | Above-the-fold avatars (nav chip, settings/profile headers) use `loading="lazy"`; eager avoids a small paint delay (no CLS either way — the box is reserved). | **performance-agent:** allow an eager preset for header/nav placements. Polish. |
| Low (security, defense-in-depth) | `_Avatar.cshtml` catches only `ArgumentException` to degrade a bad key to the monogram. Sufficient today because `UrlSigningKey`/`PublicBasePath` are `ValidateOnStart`-checked so `CreateToken` can't throw otherwise at render time; if that boot validation were ever relaxed, a non-`ArgumentException` at render could 500 an anonymous page. | **security-agent:** optionally broaden to `catch (Exception)` (still degrade to monogram), or keep the boot invariant that guarantees it. |
| Low (test, optional) | No regression case for unique radio ids or an emoji-leading DisplayName monogram. | testing-agent, optional. |
| Note (deployment) | The friendly 413 copy in `site.ts` fires only if Kestrel returns a clean 413; if `MultipartBodyLengthLimit` trips a connection reset instead, HTMX surfaces a generic network error. | Confirm a clean 413 is emitted for oversize bodies (deployment/perf validation). |

## From Milestone 4.3 (watchlist status control on every title surface) review (2026-07-03)

**4.3 review gates: PASSED at 0 Critical / 0 High across all four gates** — security ✅ (CSP byte-unchanged / anti-forgery / XSS / IDOR), **performance ✅ (KEY gate: batch map N+1-free — one authed-only query/page, anonymous zero DB, no per-card fetch)**, code ✅ (+ a dedicated portal-lifecycle re-review), UI 🔧→✅. Build 0/0, 285 tests. Ran the four dimensions as parallel read-only review agents, then one coordinated single-writer fix pass.

**Found + fixed in-milestone (NOT deferred):**
- **UI CRITICAL (C1) — the Details `position:fixed` menu was re-trapped** by the title panel's containing block (`backdrop-filter` + a filled `animate-rise-in` `transform` each establish a containing block for `position:fixed` descendants) → mispositioned + clipped by the hero's `overflow:hidden`, breaking set/change-status on the primary single-title surface. → **portal the menu panel to `document.body` on open** (the viewport becomes the CB on every surface), un-portaled back into the control before every HTMX swap (capture-phase `htmx:beforeSwap`) so it is never orphaned. Re-reviewed clean by both UI and a dedicated code lifecycle re-review.
- **UI/Code — Tab / scroll-auto-close dropped focus to `<body>`** when a menu item held focus. → Tab `preventDefault`+`closeMenu(true)`; scroll/resize route focus through the trigger.
- **UI Medium — no-JS "Remove" was a dead button** (`type="button" hx-delete`, no form). → hidden unless JS (`html:not(.js) .js-only{display:none}` + a `.js` stamp); the three status options remain real no-JS submit buttons.
- **UI Low — single-select semantics** → `role="menuitemradio"`+`aria-checked` (was `menuitem`+`aria-current`).
- **Perf Low — non-passive listeners** → `{passive:true}` on the scroll/resize registrations (added per-open, removed on close — leak-free).
- **UI Low — touch targets** → `.wl-trigger`/`.wl-nudge-chip` padding bumped above the 24px WCAG 2.5.8 floor.
- **Doc — stale "lifts to position:fixed / .rail-scroller" comments** → updated to the portal model (`_WatchlistControl.cshtml`, `app.css`).

**Deferred:**
| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Low (code, naming) | `data-wl-announce`/`data-wl-trigger` markers diverge from the `data-<region>-*` convention (cf. `data-search-announce`) on the same element that carries the full-word `data-watchlist-control`. | **frontend-agent:** unify on `data-watchlist-*` (or shorten the container to `data-wl-control`). Cosmetic. |
| Low (a11y) | A nameless `<form>` sits between `role="menu"` and its `menuitem`/`menuitemradio` children (needed for the no-JS submit path). Items are still exposed (generic passthrough); `menuitemradio` without an explicit `role="group"` is an APG SHOULD, not MUST. | **ux-agent:** verify announcement with a real SR; add `role="group"`/`aria-owns` if an AT gap shows. Monitor. |
| Low (a11y, enhancement) | The portaled `role="menu"` is DOM-detached from its trigger; the relationship rests on focus + `aria-haspopup`/`aria-expanded` (the standard portaled-menu pattern). | **ux-agent:** optionally add `aria-owns`/`aria-controls` linking trigger ↔ portaled menu now that it lives in `<body>`. |
| Low (robustness, pre-emptive) | The feed/review/notification `site.ts` handlers scope by region containment and lack the explicit `isWatchlistControlSwap` guard the search handler has. No overlap today (no control nests in those regions). | **frontend-agent:** add the same early-return guard before any surface nests a `_WatchlistControl` inside `#feed-items`/`#reviews-list`/`#notification-items`. |
| Low (ux) | The menu dismisses on any scroll/resize rather than reflow-following. Deliberate — avoids per-scroll reflow; the perf gate endorsed it. | Accepted. Revisit only if reflow-follow is wanted. |
| Note (perf, pre-existing) | Response compression + output-cache on the anonymous rail/search partials are still unwired. | Phase 6 (already tracked). |
| Note (manual) | The 4.3 menu was closed on build+test+4-gate review but NOT eyeballed in a browser (no browser in the agent env). | **Manual smoke:** Details desktop+360px + a rail/search card + keyboard/SR + confirm no leftover `.wl-menu-panel` in `<body>` after set/remove/error. Joins the 2.7 (live TMDB) / 3.5 (realtime) manual-smoke list. |

## From Milestone 4.4 (watchlist page `/watchlist` + 2 perf indexes) review (2026-07-05)

**4.4 review gates: PASSED at 0 Critical / 0 High across all four gates** — security ✅ (owner-scoping/IDOR, CSP byte-unchanged), performance ✅ (KEY: keyset no-OFFSET, both indexes serve their keyset with no sort, counts one GROUP BY, grid N+1-free), code 🔧→✅ (1 High fixed), UI ✅ (orchestrator-run inline — the ux review agent failed repeatedly on API stalls). Build 0/0, 294 tests. Gates run as parallel read-only agents; one coordinated single-writer fix pass.

**Found + fixed in-milestone (NOT deferred):**
- **Code HIGH (H1) — in-place status *change* on the page didn't re-sync state.** Each card renders the full `_WatchlistControl` menu, but the 4.4 `site.ts` block only handled removal + load-more — so a status change left the filter-chip counts, the filtered-view membership, and the card root's `data-watchlist-status` stale (visibly wrong counts; a later removal then decremented the wrong chip). → **fixed:** a `data-wl-status` on the control root as the machine-readable new-status source + a page-scoped `handleWatchlistStatusChange` that re-buckets old−1/new+1 counts (All unchanged), rewrites the card attribute, and (in a filtered view) drops the now-non-matching card via a shared `dropWatchlistCardLi` helper. The 4.3 control handler owns the single announce; the 4.4 block owns focus (defer-guarded, no double-fire).
- **Code LOW (L1) — terminal "end of list" marker survived beside the revealed live-empty block.** → `revealWatchlistEmptyState` now strips any non-card marker `<li>` before revealing `[data-watchlist-live-empty]` (fixes it for both the removal and the status-change-filter-out paths).

**Deferred:**
| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Med/Low (feature gap) | `WatchlistSort.RecentlyUpdated` (design §6.1) + its test `WP3` were **not implemented** — only `RecentlyAdded` ships. `RecentlyUpdated` (`ORDER BY COALESCE(UpdatedAtUtc, AddedAtUtc)`) can't be served by the two `AddedAtUtc` indexes without a per-user scan+sort. | **backend + database-agent:** implement with a `(UserId, UpdatedAtUtc)`-class index (or a persisted `COALESCE` sort key), OR amend design §6.1 + WP3 to record the deferral. The sort `<select>` grows automatically as the enum grows. |
| Low (DRY) | `_WatchlistCard` mirrors `_TitleCard`'s poster scaffold (~12 lines) — a cross-folder `<partial>` can't resolve from `Views/Watchlist`. | **frontend-agent (optional):** extract just the shared frame (`poster-frame`/`poster-img`/vignette/title-year) into `Views/Shared/_PosterFrame.cshtml` that both cards compose; do NOT relocate `_TitleCard` wholesale (the cards differ). |
| Low (perf) | Every poster (incl. the first grid row = likely LCP) is `loading="lazy"`. Marginal; matches the rails/search baseline; CLS-safe. | **performance-agent:** once a Lighthouse mobile run confirms the LCP element, consider `loading="eager"` on the first row (the tag helper already supports it). |
| Low (perf, YAGNI) | `GetWatchlistCountsQuery` re-scans the user's index rows on every page load (no maintained counters). Fine for realistic list sizes. | Accepted. Revisit only if a hot-path measurement shows it. |
| Low (naming) | `data-wl-status` / `data-wl-announce` / `data-wl-trigger` keep the `wl` abbreviation while the container is full-word `data-watchlist-*` (carried from 4.3). | Fold into the 4.3 `data-watchlist-*` unification if actioned. Cosmetic. |
| Note (manual) | 4.4's live count re-bucket / filtered-out removal / live-empty / load-more `_WatchlistError` retry have no server round-trip to re-verify — only exercisable in a live browser. | **Manual smoke:** All-view status change (old chip −1 / new +1, All unchanged, card stays); filtered-view change (card leaves grid); remove last card → live-empty revealed, no terminal marker; load-more + Retry. Joins the manual-smoke list. |
| Note (infra) | `CinoraDev`/prod are **un-migrated** (the `WatchlistPageIndexes` + all prior migrations) — the `localhost` SQL instance has been unreachable. | **Human:** apply `artifacts/migrate.sql` (idempotent, reviewed) when the instance is up. Never auto-migrate. |

## From the Phase 4 EXIT GATE (holistic, 2026-07-06) — PASS 0 Crit/0 High across 5 dimensions

**Advisory Medium closed in-gate (not deferred):** the design-doc `RecentlyUpdated`-sort divergence — a dated deferral note was added to `docs/architecture/phase-4-watchlists-profile-design.md` §6.1 (the sort ships `RecentlyAdded` only; `RecentlyUpdated`/WP3 need a `(UserId, UpdatedAtUtc)` index; the `Sort` param is threaded but ignored — see the 4.4 row above).

**Deferred Lows (new to the exit gate):**
| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Low (security, accepted) | A bucketed avatar serving URL stays valid until the bucket rolls (≤ `SignedUrlBucketHours`) after a replace/remove; mitigated by best-effort disk delete (→ immediate 404), residual window only on a failed delete (orphan). Avatars are non-sensitive. | Accepted (ADR 0013 bucketed-expiry trade-off). The deferred orphan-sweep chore (ADR 0013 §13) is the long-term fix. |
| Info (security) | `GET /watchlist/control` + the watchlist-page GETs carry no rate-limit. | Accepted — `[Authorize]`, owner-scoped, index-backed, no TMDB proxy (the TMDB-proxying `Set` write IS `social-write`-limited). |
| Low (perf, micro-opt) | The two watchlist composite indexes aren't covering for `MovieId`, so the highlights/page projections incur bounded (≤ page size) key lookups. | Deferred — immeasurable at current scale. `INCLUDE(MovieId)` if a profiler ever shows it. |
| Note (perf, Phase 6) | No captured Core Web Vitals / Lighthouse for `/watchlist` + `/users/{id}` (they postdate the last Web-Vitals pass). Server data path is clean; client vitals unmeasured. | **Phase 6** perf pass — add both pages to the throttled-mobile Lighthouse targets (poster rails + lazy avatars are the elements to watch). |

**Carry-forward contract for Phase 5 (from the security gate):** any new avatar/media consumer MUST serve through the token-gated `MediaController` (never static-serve `App_Data`); any new user-scoped query MUST filter by `ICurrentUser` before pagination; keep the bare-name-Alpine + `htmx.config.allowEval=false` invariants (a switch to standard Alpine / `hx-on` would force a CSP change).

## From Milestone 5.0 (IRecommendationEngine port + Ollama adapter) review (2026-07-06)

**5.0 gates: PASSED at 0 Critical / 0 High** — architecture ✅, security ✅, code ✅. Build 0/0, 322 tests. **Ratified in-gate (not deferred):** the malformed/empty model output **degrades to an empty pick set** (not a throw) — ADR 0016 amended 2026-07-06; `RecommendationEngineException` reserved for transport/timeout.

| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Low (code) | `CapReason` truncates by UTF-16 index — a surrogate pair straddling char 240 leaves a lone surrogate (→ U+FFFD). Negligible (worst case one replacement glyph). | **5.1 owns it** — the authoritative ~140-char reason cap re-caps; make that cap rune-safe. Else a `char.IsHighSurrogate` walk-back in `CapReason`. |
| Low (code, log accuracy) | `AiLog.UnusableResponse` ("...not valid ranking JSON") fires for a well-formed `{"picks":[]}` (valid JSON, legitimately zero picks). Slightly misleading. | **backend-agent (optional):** a distinct "valid-but-empty" event or reworded message. Cosmetic. |
| Low (perf, handoff) | `AddChatClient`'s factory news up an `OpenAIClient` per resolution; if `IChatClient` is Scoped, a new connection pool per scope. Negligible today (engine resolved off-request in the 5.3 job). | **performance-agent (5.3):** confirm the intended `IChatClient` lifetime — a Singleton reuses `OpenAIClient`'s (thread-safe) connection pool. |
| Low (security, forward) | A future **credentialed cloud** LLM swap (Gemini/Groq) can't supply a key through the current wiring (`AddChatClient` hardcodes the dummy Ollama key; `AiOptions` has no `ApiKey`). Correct + secure for keyless Ollama. | **5.x (only if a cloud provider is introduced):** add an optional `AiOptions.ApiKey` from user-secrets/env (never `appsettings.json`); read it in the factory. Re-review then. |
| Contract (security, 5.2/5.4) | `RankedPick.Reason` is model-authored free text (the only model string reaching persistence + UI). Length-capped, NOT sanitized (correct — sanitization is a render concern). | **5.2 persist verbatim as untrusted; 5.4 render with default Razor encoding, NEVER `Html.Raw`.** A first-class Phase-5 contract. |

## From Milestone 5.1 (grounded candidates + rank + hallucination guard) review (2026-07-06)

**5.1 gates: PASSED at 0 Critical / 0 High** across all three (architecture ✅ 0C/0H/0M, performance ✅ 0C/0H, code 🔧→✅). Build 0/0, **336 tests** (Domain 46, Application 72, Infrastructure 93, Web.Integration 125). **Fixed in-gate (NOT deferred):** **H-1 (code, High)** — `DiscoverByGenreAsync` built `with_genres` with comma (AND) semantics vs. the port's "any of" (OR) contract → near-empty genre fan-out; fixed to `|` (OR) at `TmdbClient.cs` **and locked** with 3 new `TmdbClientTests` URL-capture tests (the mock suite structurally couldn't see AND vs OR). **L-1 (code, Low)** — favorite-genre ranking had no tiebreaker (non-reproducible profile → would destabilize 5.2's skip-unchanged hash); added `.ThenBy(GenreId)`. **arch L1 (Low)** — corrected an overstated `IOptions` "reload is reflected" doc comment. Also **RESOLVED** the 5.0-carried surrogate-safe reason-cap item — 5.1's `Truncate` walks back a lone high surrogate (code-gate verified).

| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Med (perf, M1) | `UserTasteProfileBuilder` favorite-genre query materializes the full seen-id list and ships it back as `Contains` (an avoidable client→server round-trip that grows with user activity; EF10 `OPENJSON` blunts the worst case). | **backend/perf, 5.2 or later (measure-first):** fold query-4's `Where` onto the `reviewedMovieIds`/`watchlistedMovieIds` `IQueryable`s as correlated subqueries — capture an EF query-log baseline for a heavy-user profile first. Non-urgent; correct at expected volumes. |
| Low (code/perf L-2) | Media-agnostic favorite-genre ids applied to both movie and TV discover (TMDB movie vs TV genre-id spaces only partly overlap; `Genre` has no `MediaType`). | **Largely self-heals now H-1 uses OR** (OR ignores foreign-media ids). Deeper fix = per-media genre scoping / add `MediaType` to `Genre` — **database-agent/architecture, Phase 5.x or accepted.** Degrades gracefully (top-up + guard). |
| Low (perf L1) | Query-3 seen-set read omits `.AsNoTracking()` (projects to a non-entity row, so zero tracking cost). | **Accepted / trivial** — consistency nit only; add `.AsNoTracking()` opportunistically. |
| Low (arch L2, doc) | Design-doc §3/§5.1 sketches trail the code: `CandidateTitle.PosterPath` (required by ADR 0017 §3) + the `UserTasteResult` composite (`SeedKeys`/`FavoriteGenreTmdbIds`/`HasSignal`). Both uphold ADR 0017 — no amendment. | **documentation-agent (Phase-5 doc pass):** one-paragraph note updating the §3 `CandidateTitle` + §5.1 builder-return sketches. |
| Low (arch L3, idiom) | `GenerateRecommendationsCommand : IRequest<RecommendationSet>` returns a value; once 5.2 makes it the writer and 5.3 dispatches it fire-and-forget, the return becomes vestigial for the job path. | **backend-agent, 5.2 handoff:** reconsider whether the command still returns the set once it persists + warms cache (or keep for the inline/test path). |
| Info (perf L3) | The seen-set read is intentionally uncapped (must be complete for correct exclusion); bounded by the user's own activity, index-seekable, 3 small fields. | **Accepted / documented** — deliberate, not an oversight. |
| Handoff (ai-agent) | Cross-media genre relevance + `with_genres` OR vs per-genre discover for candidate yield/quality. | **ai-agent, 5.x quality refinement** (pool-size/relevance tuning; measure). |

## From Milestone 5.2 (history persist + served cache + heuristic serve) review (2026-07-06)

**5.2 gates: PASSED at 0 Critical / 0 High** across all three (architecture ✅ 0C/0H + 1 Med fixed in-gate, code ✅ 0C/0H, performance ✅ 0C/0H). Build 0/0. **Fixed in-gate (NOT deferred):** **(Med, architecture) the never-500 fallback gap** — `HeuristicRecommender`'s TMDB fan-out was unguarded, so a cold-start user + TMDB-cache-miss + TMDB outage would 500 (violating ADR 0018 §6 / design §9 + `IHeuristicRecommender`'s own "empty set otherwise" doc); **fixed** by guarding the per-tier TMDB awaits at the source (partial/empty `Heuristic` set, never throws; new `HeuristicTmdbFailed` log) + a throwing-`ITmdbClient` locking test. **(Low, code L2)** envelope `TryParse` now `Enum.IsDefined`-checks `Media` (a tampered `"m":7` → unusable → heuristic) + test. **(Low, code L1)** added `GenerateRecommendationsCommandValidator` (`UserId NotEmpty`) for house-rule consistency + test.

| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Low (perf) | Tier-c heuristic **recomputes on every serve** for signal-users without a persisted AI set (≈6 bounded DB reads + ≤~11 cached TMDB calls); the query deliberately does NOT re-cache it (CQRS purity). | **Accepted / by design (§7 lists (c) as a serve tier).** The lever is **5.3 precompute coverage** — more users on warm tier-(a)/(b). Measure the tier-c hit rate once the job runs. |
| Low (code L3) | Audit row's `GeneratedAtUtc` is stamped by `AIRecommendationHistory.Create` via `DateTime.UtcNow` (the Phase-1 entity), while the envelope's `gen` uses the injected `TimeProvider` — two clocks; the row timestamp isn't test-controllable. | **Accepted seam (out of 5.2 scope — Phase-1 entity).** Thread `TimeProvider` into `Create` (optional `generatedAtUtc` param) **only if** deterministic row timestamps are ever needed — **backend/database-agent**. Harmless in prod (`TimeProvider.System ≈ DateTime.UtcNow`). |
| Low (arch) | Two wire formats for `RecommendationSet`: the served **cache** round-trips the full record (verbose PascalCase STJ); the durable **history column** uses the compact short-name `RecommendationSetEnvelope`. Both reconstruct the same object; defensible (unbounded fast cache vs 4000-char audit column). | **Accepted.** Add a one-line cross-reference comment on each so a future `RecommendationPick` field updates BOTH — else a new field silently drops on one serve tier. **backend-agent, opportunistic.** |
| Low (perf) | `RecommendationSetEnvelope.Serialize` budget loop is O(n²) worst case (re-serialize per dropped pick). | **Accepted** — `n ≤ MaxResults` (12), essentially never fires (12 reasons ≤140 chars fit 4000 with margin), off-request. |
| Low (perf) | Served cache stores the full `RecommendationSet` (verbose names), a larger payload than the compact envelope. | **Accepted** — in-memory `IDistributedCache`, negligible. |
| Info (perf, cross-ref) | Seen-exclusion read uncapped in `UserTasteProfileBuilder` (also hit on the tier-c serve). | **Same item as the 5.1 perf M1 / L3** above — measure-first; defensive `Take` only if power-user profiles emerge (capping risks recommending an already-seen title). |
| Handoff (5.3) | Skip-unchanged / taste-freshness dedup. | **5.3 job:** skip a user whose newest `AIRecommendationHistory.GeneratedAtUtc` is newer than their latest review/watchlist activity. |

## From Milestone 5.3 (Hangfire precompute job + admin-gated `/jobs`) review (2026-07-06)

**5.3 gates: PASSED at 0 Critical / 0 High** across all four (security ✅ 0C/0H/0M primary, architecture ✅ 0C/0H/0M, performance ✅ 0C/0H, code ✅ 0C/0H). Build 0/0, **no NU19xx** (Newtonsoft.Json 13.0.3 override audit-confirmed in both Infra+Web graphs). **Fixed in-gate (NOT deferred):** **(perf Med M1 + code Med)** the stale-user selection had no lookback window and re-dispatched persistently-empty users forever — **fixed** with a `PrecomputeLookbackDays` (default 45) window (bounds the nightly scan + bounds the empty-cohort re-attempt) + an honest idempotency docstring. **(security L1+L2)** admin matched on unverified email/free-form name claims — **fixed** to match on the server-assigned `NameIdentifier` (user-id) + email only, name-claim arms dropped. **(security L3)** added the `SameSite=Lax`-protects-dashboard-CSRF load-bearing comment. **(perf/arch L1)** corrected the `[DisableConcurrentExecution]` lock-hold comment. **(perf L3)** refined the per-user OCE catch so a leaked per-user timeout doesn't abort the batch. **(code L)** de-confounded the freshUser test + added a lookback-exclusion test.

| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| **HIGH (test-only, OUT-OF-SCOPE)** | `SQLitePCLRaw.lib.e_sqlite3` **2.1.11** (GHSA-2m69-gcr7-jv3q) pulled transitively into **`Cinora.Application.Tests`** by the Phase-2.2 SQLite in-memory test packages. **Test-only, NEVER shipped, outside the Hangfire/Web runtime graph** (the security audit confirmed Infrastructure+Web have zero vulnerable packages). | **testing/devops hygiene (pre-existing, not a Phase-5 regression):** bump the SQLite test package line (`Microsoft.EntityFrameworkCore.Sqlite`/`SQLitePCLRaw.bundle_e_sqlite3`) to a patched version; verify the SQLite-in-memory handler tests still pass. Not a product vulnerability. |
| Med (code, future-scale) | Persistently-empty users (thin profile / no candidates) are re-attempted each night **within the lookback window** (they write no history row, so timestamp-based skip can't mark them "done"). Bounded now by `PrecomputeLookbackDays` + documented; the design's proper fix is a durable **taste-hash / attempt marker**. | **Deferred (needs a schema change — empty runs write no history row by design; keeps 5.3 schema-stable).** Revisit if repeated empty-cohort work matters at scale — a per-user taste-hash or attempt store unions into the skip check. |
| Low (perf/db) | `MAX(UpdatedAtUtc)` in the selection aggregates isn't covered by `IX_Reviews_UserId_CreatedAtUtc` / `IX_Watchlists_UserId_AddedAtUtc` (only Created/Added are), so those reads can't be pure index stream-aggregates. | **database-agent (migration):** optional `INCLUDE (UpdatedAtUtc)` to make them covering. Largely moot now the lookback window shrinks the scan. Low priority. |
| Low (security/docs) | The strict global CSP **degrades but does not break** the admin Hangfire dashboard (inline styles/scripts blocked; same-origin `/jobs/*` assets allowed) — a more-secure, admin-only degraded render. No directive was widened. | **Accepted / documented.** **The global + For-You CSP (§13) MUST NEVER be relaxed to render `/jobs`.** If a fuller dashboard render is ever wanted, scope any relaxation to the `/jobs` path only. |
| Info (arch) | `AdminDashboardOptions.AdminEmails` has `[Required]` while defaulting to `[]` (empty passes `[Required]`, only rejects explicit `null`); the dashboard is mapped even under Testing (harmless — the fail-closed filter denies before any Hangfire storage access; keep the Testing admin allow-list empty). | **Accepted.** Fail-closed default holds. |
| Deferred (design §10) | **Event-driven per-user refresh** (`IRecommendationRefreshScheduler` + debounce after N ratings). | **Nightly-only ships** (design §10 default). Flagged future refinement; seen-exclusion already drops acted-on titles next run. |

## From Milestone 5.4 (For-You rail + `/recommendations` page + dismiss) review (2026-07-06)

**5.4 gates: PASSED at 0 Critical / 0 High** across all four (UI ✅ primary 0C/0H, security ✅ 0C/0H **no findings at any severity**, performance ✅ 0C/0H, code ✅ 0C/0H). Build 0/0, npm build ✅, **CSP byte-unchanged**, serve `engine.CallCount==0` (LLM-free by construction — no `IRecommendationEngine` in the controller/query/heuristic). **Fixed in-gate (NOT deferred):** **(perf+UI Med) `/home` rail CLS** — skeleton lacked the caption line (feed shifted down) + empty rail collapsed leaving dead margin (feed jumped up) → fixed (skeleton caption placeholder + clean empty-band collapse). **(code Med M2) no-JS dead dismiss** — the HTMX-only dismiss `<button>` wasn't `.js-only` (repo progressive-enhancement standard) → fixed. **(code Med M1) vacuous encoding tests** — plain-text seeds meant an `Html.Raw` (stored-XSS) regression would pass → fixed with an HTML-special-char `Reason`/`Title` seed asserting encoded-present + raw-`<script>`-absent. **(Lows) fixed:** "For you"→"For You" casing, sr-only `<h2>` heading order, `title="Dismiss"` hint, reveal empty-state on dismiss-all, tightened the AI-label test to the unique eyebrow.

| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Low (perf) | Cold-start `/recommendations` page serves the tier-c heuristic **synchronously** (a bounded, **cached** TMDB fan-out) on the request — the page is non-lazy (unlike the `/home` rail). LLM-free + design-sanctioned; usually a warm-cache hit. | **Accepted / measure-first.** If a throttled-mobile measurement shows it slow, lazy-load the page grid (as the rail does) or bound/parallelize the fan-out. Precompute coverage (5.3) keeps most users on the warm cache/history tiers. |
| Low (code, forward) | `POST dismiss [FromForm] MediaType media` defaults to `0==Movie` on a missing/garbage bind, so `Enum.IsDefined` can't reject a *missing* `media`. **Harmless in v1** (persists nothing). | **When durable `RecommendationFeedback` lands:** require `media` explicitly (nullable + validate) and add the ownership/IDOR check — the dismiss write becomes the first per-user recommendation state. |
| Deferred (design §11, ADR 0018) | **Durable "not interested"** — v1 dismiss is client-side only (empty-200, persists nothing); a reload re-shows a dismissed pick until the next nightly refresh (reviewing/watchlisting it excludes it). | **DEFERRED** (schema-stable Phase 5). The dismiss endpoint is the anti-forgery-protected + rate-limited seam a `RecommendationFeedback` entity (`UserId,TmdbId,MediaType,Kind,CreatedAtUtc`, unique per `(UserId,TmdbId,MediaType)`) unions into the §5.1 exclusion set — no UI change when it lands. |
| Accepted (design §5.1) | `_RecommendationCard` composes `WatchlistControlVm(Status: null)` (N+1-free) — a served pick is not-in-watchlist by construction. Minor staleness if a user watchlists a pick between refreshes (idempotent upsert; next refresh drops it). | **Accepted trade-off** (N+1 avoidance). |

## From the Phase 5 EXIT GATE (holistic, 2026-07-06) — PASS 0 Crit/0 High across 5 dimensions

**Exit gate PASSED** (architecture / security / performance / code / UI, all 0C/0H). Build 0/0, 380 tests, CSP byte-unchanged, shipped dependency graph clean. All residuals are Low or doc-only.

| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| ~~Med (arch, DOC-ONLY)~~ **RESOLVED (2026-07-06)** | ADR 0018 §5 + design §6/§10 described a **"taste hash"** skip-unchanged mechanism, but the shipped `RecommendationPrecomputeJob` uses a **timestamp comparison** (latest `AIRecommendationHistory.GeneratedAtUtc` vs latest activity). | **DONE (documentation-agent, build 0/0, no behavior change):** dated **ADR 0018 `## Amendment (2026-07-06)`** records the timestamp realization + user-id/email admin matching (taste-hash kept as the future refinement); design §3/§6/§8/§10/§13 carry dated "Shipped realization" notes; the 2 stale "taste hash" comments (`UserTasteProfileBuilder.cs`, `GenerateRecommendationsCommand.cs`) reworded to the timestamp mechanism. _(Minor follow-up if ever needed: the `openai-recommendations` SKILL card still paraphrases "taste hash" — reference-only, non-authoritative.)_ |
| Low (perf F1) | The latest-generation aggregate in `SelectStaleUsersAsync` (`db.AIRecommendationHistories.GroupBy(UserId).Max(GeneratedAtUtc)`) is NOT lookback-filtered — it scans the whole append-only history table each nightly run. Off-request, index-supported `(UserId,GeneratedAtUtc)`, documented. | **Accepted / micro-opt.** Filter to the active-user set or land the durable taste-hash if history ever grows large — measure-first, nightly-job only. |
| Low (code) | `ServedRecommendationCache.GetAsync` deserializes `RecommendationSet` with default STJ and does NOT re-validate `MediaType` via `Enum.IsDefined` (the durable `RecommendationSetEnvelope.TryParse` does). Safe today — process-local in-memory cache, only the trusted command writes it; worst case a `/discover/title/7/{id}` 404 (encoded, no injection). | **backend-agent — pre-req for any future Redis/distributed-cache swap** (then the cache becomes a real trust boundary). Add an `Enum.IsDefined` guard on cached picks. Non-blocking now. |
| Low (UI L1) | Residual `/home` skeleton↔rail height mismatch (~20–24px; est. CLS ≈ 0.02, within "good"): `.rail-scroller` reserves `py-5` but the skeleton reserves only top `mt-5`. | **frontend-agent (Phase 6.5 polish):** give the skeleton poster row matching vertical padding. Confirm with an instrumented CLS run. |
| Low (UI L2/L3) | Dismiss-all edge cases: on `/home` the rail heading orphans above an empty scroller (live-empty reveal is grid-scoped); on `/recommendations` the sr-only `<h2>` "Your picks" + "still learning your taste" copy read oddly after the user dismissed real picks. | **frontend-agent (Phase 6.5 polish):** collapse the rail region on rail dismiss-all; relabel/hide the sr-only h2 + give the live-empty stub distinct copy ("That's everyone for now"). Rare actions. |
| Handoff (measurement) | AA-contrast + CLS conclusions are token-math/layout-math estimates, not instrumented. | **Phase 6.4/6.6:** run throttled-mobile **Lighthouse (CLS/LCP/INP) + axe (contrast/focus over a bright poster)** on `/home` + `/recommendations` (incl. cold-start) to convert reasoned→measured. |
| Forward (security) | If the `IRecommendationEngine` is ever pointed at a **cloud** model, switch the endpoint to HTTPS + move the real key to user-secrets/env (current plain-HTTP is loopback-only/safe). A future "refresh now" endpoint MUST enqueue via Hangfire, never call `RankAsync` inline. | **Re-review when a cloud provider or on-demand refresh is introduced.** |

## From Milestone 6.1 (PWA foundation — manifest + SW + offline + versioned update) review (2026-07-06)

**6.1 gates: PASSED at 0 Critical / 0 High** across all four (security ✅ primary, architecture ✅, UI ✅, code 🔧→✅). Build 0/0, npm ✅, 388 tests, **CSP byte-unchanged**. **Fixed in-gate (NOT deferred):** **(code HIGH)** `clients.claim()` + unconditional `controllerchange` reload → every first visit reloaded unprompted (violated "never hijack an in-use page") → **fixed** with a consent-gated `updateAccepted` flag (only a user-clicked Refresh reloads, once). **(arch Med)** dev-`--watch` SW served stale `/dist/*` → **fixed** (SW registered only outside Development via a `cinora-sw` meta; Dev unregisters stale SWs). **(UI Med×2)** update-toast evictable by a notification burst → excluded `#sw-update-toast` from `transientToasts()`; `.toast-link` ~16px target → `min-height:24px`. **(security Low)** `/account/*` auth pages were relaxed to `no-cache` → **kept `no-store`** (+ regression test). **(Lows)** icon bytes folded into `__SW_VERSION__`; `await cache.put`; iOS apple-metas; dropped redundant `role="status"`; manifest `id`; `Program.cs` `/dist` comment corrected; misplaced TS directive removed. **(doc)** ADR 0019 `## Amendment (2026-07-06)` records the anti-forgery cache relaxation + exact offline scope — **RESOLVED** (documentation-agent).

| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Low (code, accepted) | SW `install` precache is atomic (`cache.addAll` rejects wholesale if any one URL is non-2xx → install fails → no offline). Safe today (URLs are build-guaranteed: hashed assets + 3 verified icons + anonymous `/offline`). | **Accepted.** Optional hardening: `Promise.allSettled` for the non-critical icons so a dropped icon can't disable the whole PWA. Keep `/offline` `[AllowAnonymous]` as an install invariant. |
| Info (security, forward) | `PwaCacheControlMiddleware` shapes only `text/html`. A FUTURE authenticated **navigable non-HTML** endpoint (`req.mode==="navigate"`, e.g. an authed export served as a full navigation) with no explicit `Cache-Control` would bypass the `no-store` and could be SW-cached. No such endpoint exists today (media is `<img>`-served). | **Forward-looking note:** any such future endpoint MUST self-set `Cache-Control: no-store`. |
| Handoff (perf → 6.4) | `/dist/*` hashed assets get ETag/304 only, not `Cache-Control: immutable`; no response/static compression yet. | **Milestone 6.4 (performance polish):** apply long-lived immutable caching + compression (the headline REVIEW_BACKLOG perf item). |
| Manual (carried) | 6.1's install/offline/update-toast/iOS/maskable behaviors need a real browser. | **Manual DevTools/Lighthouse smoke on a Release run:** installable; SW scope `/`; offline (visited `/discover`→cache, uncached→`/offline`, authed `/home`→`/offline`); **first visit = NO reload**, version-bump → toast → **single** reload; maskable icon un-clipped; iOS add-to-home. |

## From Milestone 6.2 (Web Push — ports + Device upsert + SW handlers + subscribe UX) review (2026-07-06)

**6.2 gates: PASSED at 0 Critical / 0 High / 0 Medium** across all three (architecture ✅ APPROVED, security ✅, code ✅ SHIP). Build 0/0, 428 tests, CSP byte-unchanged, shipped graph vuln-clean (WebPush 1.0.13 + Portable.BouncyCastle 1.9.0, MIT). Nothing to fix in-gate.

| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Low (security) → **FOLD INTO 6.3** | Push validators only check `NotEmpty`; an oversized `Endpoint` (>2048) / key (>256) → `SqlException` → **500** instead of a clean **400**. | **6.3 (reopens the Device validators for its migration):** add `MaximumLength(2048)` / `MaximumLength(256)` (+ optional base64url `Matches`) to `RegisterDeviceCommandValidator`/`UnregisterDeviceCommandValidator`. |
| Low (code) → **FOLD INTO 6.3** | `WebPushSender.SendAsync` catches only `WebPushException`/OCE/`HttpRequestException`/`InvalidOperationException`; a malformed key → `FormatException`/`ArgumentException` **escapes the port**, aborting the device loop + skipping the deferred `Gone`-prune `SaveChanges`. (Caught by `InlinePushDispatch` — no crash; self-heals next notification.) | **6.3 (touches `SendPushNotificationCommandHandler` for the pref gate):** broaden the final catch to `catch (Exception) when (ex is not OperationCanceledException) → TransientFailure`, OR make the handler's per-device send loop resilient so one bad device can't suppress the rest + the prune. |
| Low (code, DRY) | `SendPushNotificationCommand` does a 2nd point-read for `RecipientUserId` because `NotificationRow` omits it. | **backend-agent (opportunistic):** add `RecipientUserId` to `NotificationProjection.ToRow` → one round-trip. Cosmetic, off-request. |
| Low (security) | Unbounded `Device` rows per user (an authed user can register many fabricated distinct endpoints). Self-mitigating: `social-write` rate limit + `Gone`-prune on first real send; rows are the attacker's own. | **Accepted / optional:** cap devices-per-user in `RegisterDeviceCommandHandler` **only if abuse is observed.** |
| Info (arch/code) | `WebPushSender` (singleton) relies on the lib's lazy, unlocked `HttpClient` init — a cold-start burst could leak one benign GC'd client; orphaned browser subscription if `pushManager.subscribe` succeeds but `POST /push/subscribe` fails (self-corrects on next subscribe). | **Accepted** — negligible; optional: pass an explicit `HttpClient` to `WebPushSender`. |
| Deferred (design §3.3, 6.3) | Concurrent identical subscribe can duplicate a `Device` (the 6.2 in-memory `(UserId,Endpoint)` upsert isn't race-safe). | **6.3:** the `EndpointHash` + unique `(UserId, EndpointHash)` index in `Phase6PushAndPreferences` closes the race; the upsert then keys on the hash. |
| Manual (carried) | The RFC-8291/8292 real-browser push round-trip. | **Manual smoke:** `VapidHelper.GenerateVapidKeys()` → `WebPush:Subject/PublicKey/PrivateKey` in user-secrets → enable push in a real browser → 2nd user likes your review → OS notification w/ the correct deep link → click focuses/opens Details. |

## From Milestone 6.3 (Notification preferences — owned VO + migration + push gate + settings) review (2026-07-06)

**6.3 gates: PASSED at 0 Critical / 0 High** across all four (architecture ✅ APPROVE 0C/0H/0M, code ✅ SHIP 0C/0H/0M, UI 🔧→✅ — found **1 HIGH** contrast fail, fixed in-gate). Build 0/0, npm ✅, 456 tests, CSP byte-unchanged, **first migration since Phase 4 (`Phase6PushAndPreferences`) additive-only + `migrate.sql` regenerated**. **Fixed in-gate:** the owned `NotificationPreferences` VO + push-only gate + `/settings/notifications` surface + `Device.EndpointHash` hash-keyed upsert + the 2 folded 6.2 Lows; the UI **H1** (checked-row hint 3.76:1 → `ink-muted` 9.3:1, clears AA 1.4.3), **M1** (visible push-subscribe outcome + "blocked → unblock" guidance), **M2** (OFF-track ring `white/10`→`control-border`), + Lows (role=group, fallback copy, on-thumb visibility, `.btn:disabled` busy state).

| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Low (perf, all 3 gates concur) | `SendPushNotificationCommandHandler` does 3 reads (projected row + a 2nd point-read for `RecipientUserId` + the preference flags) — could be **one** projection. Off the request path (inline dispatch scope). | **6.4 perf pass / backend-agent:** surface `RecipientUserId` on `NotificationProjection.ToRow` + fold the preference read → one round-trip. Non-blocking. |
| Low (testing) | The `RegisterDeviceCommand` `DbUpdateException` race-reconcile branch (concurrent same-endpoint subscribe tripping the unique index) is only indirectly covered (unit context omits the index; integration exercises the sequential path). | **testing-agent:** add a concurrency-simulating test to lock the reconcile branch. |
| Low (app-wide, not 6.3-specific) | `onSettingsResponseError` handles `htmx:responseError` (HTTP non-2xx) but NOT `htmx:sendError` (network failure) → an offline settings save swaps + announces nothing. Shared across all HTMX regions. | **6.5 a11y/polish or accepted:** app-wide `htmx:sendError` affordance for offline writes. frontend-agent. |
| Runbook (arch L1) | The `Device.EndpointHash NOT NULL DEFAULT ''` + unique `(UserId, EndpointHash)` index assumes an **empty `Devices` table** (holds only while push is new). A DB with ≥2 same-user device rows would collide on `''` at `CREATE UNIQUE INDEX`. | **6.6 deployment runbook (plans-only):** an explicit pre-apply line — verify `Devices` empty (or ≤1/user) before applying `Phase6PushAndPreferences`; **truncating `Devices` is safe** (clients re-subscribe). |
| Manual (carried) | The push permission-prompt / subscribe / denied-blocked recovery UI needs a real browser. | **Manual/Playwright smoke:** the visible `[data-push-status]` line, the "Enabling…" busy state, and the denied/blocked recovery copy on `/settings/notifications`; mute a type → trigger from another user → no OS push but the in-app inbox/toast still arrive. |

## From Milestone 6.4 (performance polish) review (2026-07-06) — closes §6.7 backlog items

**6.4 gates: PASSED at 0 Critical / 0 High** across all three (security ✅ 0C/0H/0M — BREACH-airtight + avatar posture preserved, code ✅ 0C/0H/0M — 459 green, performance ✅ 0C/0H). Build 0/0, npm ✅, main bundle **−27%** (SignalR lazy-loaded off anonymous pages), CSP byte-unchanged. **Fixed in-gate:** **(perf Med MED-1)** the `/dist` `immutable` cache header was unconditional → broke dev hot reload (fixed `/dist` names in the watch loop); **fixed** by gating `immutable` on `!IsDevelopment()` (Dev → `no-cache`).

**§6.7 REVIEW_BACKLOG items CLOSED by 6.4:**
| Backlog item | Disposition |
|---|---|
| **No response compression** (2.3/3.5 Med — the headline item) | ✅ **CLOSED** — `AddResponseCompression` Brotli+Gzip, static-MIME-only (BREACH-safe, HTML never compressed, test-locked), `SecurityHeaders` upstream. |
| **SignalR (~56 KB) on every page** (3.5) | ✅ **CLOSED** — dynamic `import()` gated on `#notification-unread-badge` → a separate authed-only `/dist` chunk; main bundle 206→150 KB. |
| **Avatar ETag re-read+rehash on every GET incl. 304** (4.1) | ✅ **CLOSED** — `PhysicalFile` + weak `length+mtime` validator (write-once files → never stale); HMAC-verify-before-serve + nosniff/forced-type intact. |
| **Feed pulls full `Body` then truncates + wrong `SUBSTRING` comment** (3.4) | ✅ **CLOSED** — SQL-side `Substring(0,201)` (translates on SQL Server + SQLite); feed-only; comment fixed. |
| **`CacheLog` logs full search key** (2.5 sec Low) | ✅ **CLOSED** — `RedactKeyForLog` hashes the query in the cache-key logs. |
| **Motion over budget; LCP hero opacity fade; skeleton CLS** (2.3) | ✅ **CLOSED** — hover 300ms/entrance 350ms, `cinora-rise-in` transform-only (no LCP opacity), rec-skeleton rail-padding + `min-h-9` match; reduced-motion. |
| **Above-the-fold avatars use `lazy`** (4.2) | ✅ **CLOSED** — `_Avatar` `Eager` preset on nav/profile/settings headers. |
| **`/dist` not immutable-cached** (6.1 code-gate note) | ✅ **CLOSED** — `immutable` in Release (env-gated per MED-1). |
| 6.3 3-reads→1 push projection | ✅ **CLOSED** — `NotificationProjection.ToPushRow` (one read; drops the actor fetch push doesn't render). |

**New Lows from the 6.4 gates → deferred:**
| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Low (security, PRE-EXISTING) | `UseSerilogRequestLogging` logs `RawTarget` incl. the query string → a `?q=<term>` search still lands in the request log at Information (the same "search terms in logs" concern 6.4 closed for the cache key). NOT introduced by 6.4. | **✅ ACCEPTED (Phase 6 exit, 2026-07-06 — security + perf gates concur).** Search terms are low-sensitivity free text; the request log is **local, single-instance, 14-day-rolling** (no external sink). The cheap `Program.cs` scrub (emit `Request.Path` / strip `q`) stays available as an optional defense-in-depth enrich if a shared/exported log sink is ever introduced — until then the residual risk doesn't warrant the change. Disposition is now unambiguous: **accepted**, not open. |
| Low (code) | Avatar **mid-serve-delete TOCTOU**: `TryResolve` stats then `PhysicalFile` streams later; a concurrent "remove photo" in the sub-ms window → `FileNotFoundException` → **500** (was 404 pre-6.4). | **Accepted (single-instance, sub-ms window, avatars rarely deleted)** — optional: wrap the stream open to map missing→404. |
| Low (perf) | `font/woff`/`woff2` in the compression allow-list re-compress already-Brotli'd fonts (CPU for ~0 gain). | **Accepted / optional:** trim if fonts are self-hosted (immutable-cached → rare anyway). |
| Handoff (pwa) | Confirm `sw.ts` runtime-caches the new lazy `esm-*.js` (SignalR) chunk under a VERSION-scoped cache so it's purged on deploy (content-hashed → correctness holds; old chunks orphan). | **6.5/pwa-agent:** verify the `/dist/*` runtime cache eviction/VERSION-scoping for split chunks. |

**Measure-gated — correctly NOT shipped (no browser here), stay DEFERRED/ACCEPTED:** **OutputCache** (§6.2 — TMDB already cached by `CachedTmdbClient`; needs a Lighthouse/TTFB signal + security-ordering review); **notification-bell COUNT per render** (§6.5 — only if authed TTFB is measured to matter); **A3 `EnableRetryOnFailure` OFF** (accepted — the user-managed registration transaction forbids it; irrelevant for single-instance/LocalDB). **The Lighthouse/CWV measurement (§6.6, LCP<2.5s/CLS<0.1/INP<200ms/≥90) is a carried MANUAL browser step.**

## From Milestone 6.5 (a11y + animation/design polish) review (2026-07-06)

**6.5 gates: PASSED at 0 Critical / 0 High** across all three (UI ✅ clean-exit — measured contrast all ≥AA, security ✅ **no findings** — confirm-gate fail-closed + token-preserving, code ✅ SHIP — focus-trap correct). Build 0/0, 464 tests, **CSP byte-unchanged (SHA-256 identical)**. **All 4 a11y Lows FIXED in-gate** (both gates concurred): modal background `inert` while the confirm dialog is open; comment "Show more" polite announce on expand; the `htmx:sendError` offline toast routed through the shared pause-on-hover/focus-restore dismissal machinery + added to the `transientToasts()` eviction-exclusion set. **Nothing deferred from 6.5** except the standing carried browser/Playwright a11y smoke (keyboard walkthrough, focus-trap, reduced-motion, View-Transition render, offline-toast pause) — captured in `docs/deployment/runbook.md` §10. **Optional (testing-agent):** a Playwright assertion that a cancelled confirm issues zero network requests (locks the fail-safe the C# suite can't cover).

## From Milestone 6.6 (deployment readiness — PLANS-ONLY) + PHASE 6 EXIT review (2026-07-06)

**6.6 code (last Phase-6 implementation):** DP-protection key-ring persistence wired in `Program.cs` (221-235, `PersistKeysToFileSystem(App_Data/keys)` + `SetApplicationName`, guarded `if (!isTesting)`); the absent-VAPID one-shot startup `Log.Warning` (403-413); `docs/deployment/runbook.md` authored (plans-only). `dotnet publish -c Release` verified — `./publish/wwwroot/{manifest.webmanifest, sw.js, icons/*, dist/*}` all present, `/dist/*` carrying `.br`/`.gz` precompressed variants. Build 0/0, **464 tests green** (Domain 56, Infrastructure 110, Application 128, Web.Integration 170).

**Phase 6 EXIT gate: PASSED at 0 Critical / 0 High across ALL FIVE dimensions** (holistic over 6.1–6.6):
- **Architecture ✅ 0C/0H/0M** — dependency rule clean (WebPush vendor types confined to `WebPushSender`, ports speak Application records), ADR-0011 seam extended **once**, owned-VO → 4 `bit` columns, **exactly one** additive Phase-6 migration, CSP byte-identical, free-only intact (only new runtime pkg `WebPush` 1.0.13 MIT + `Portable.BouncyCastle` MIT).
- **Security ✅ 0C/0H** — **CSP byte-unchanged verified verbatim** (baseline string identical; no inline script/handler/`Html.Raw`-on-user-content anywhere in the phase), shipped graph **vuln-free** (`dotnet list package --vulnerable`), offline-privacy (authed HTML→`no-store`, SW refuses `no-store`), push has no IDOR (server-resolved actor), VAPID private key never committed/logged, DP-keys under `App_Data` (sibling of `wwwroot`, **not web-served**), delete-confirm re-issues byte-identical request + fails closed, BREACH-safe compression.
- **Performance ✅ 0C/0H** — all §6.7 wins present + regression-free (compression static-only + `SecurityHeaders` upstream, bundle −27%, avatar 304-before-read, feed SQL truncation, push read-fold 3→1), **zero synchronous LLM on any request** still holds, SW split-chunk runtime-cached under the VERSION-scoped cache + purged on activate (**closes the 6.4 pwa handoff**).
- **Code ✅ 0C/0H** — re-ran build **0/0** + **464 tests** + `tsc` strict (incl. `tsconfig.sw.json`) clean; no `.Result`/`.Wait()`/`async void`/`Database.Migrate()` in `src`; every Phase-6 guarantee has a non-vacuous test.
- **UI/UX ✅ 0C/0H, WCAG AA** — re-measured contrast (lowest text pair 5.04:1; `.btn-danger` 6.16:1), manifest/icons cohere with tokens (surface-0 = `#090b0f` = theme_color), delete-confirm rollout complete (0 `hx-confirm`/`window.confirm` in views), motion + reduced-motion + aria-live patterns hold.

**Doc reconciliations applied at the exit gate (architecture + code gates flagged doc-drift that *understated* shipped code — no code change, docs corrected):**
- `docs/deployment/runbook.md` §3.1 (VAPID startup Warning IS shipped), §4 (DP key-ring IS wired — dropped the "⚠️ Pending" box), footer (DP persistence shipped; only `ForwardedHeaders` remains plans-only).
- ADR-0021 verification note — DP key-ring bullet flipped "pending" → **shipped** ("all Decision §1 items now realized in code").
- ADR-0019 Amendment caveat — `/account/*` no-store exclusion flipped "not yet present" → **shipped**.

**§6.7 REVIEW_BACKLOG triage — COMPLETE (exit criterion):** the 6.4 §6.7 closure table CLOSED 8 perf/polish items + the 6.3 3-reads fold; the security L-1 (Serilog query-string) is now explicitly **ACCEPTED** above (was ambiguous); OutputCache + notification-bell COUNT + A3 `EnableRetryOnFailure` are **measure-gated/accepted-deferred** with recorded rationale. **No item silently open.**

| Sev | Finding | Decision / Target |
|-----|---------|-------------------|
| Low (ui) | Confirm dialog uses `role="dialog"`; APG recommends `role="alertdialog"` for a destructive confirmation (better SR urgency). It is already labelled/described/modal/focus-trapped — an AA-neutral best-practice refinement. | **frontend-agent (trivial):** `role="dialog"` → `role="alertdialog"` in `_ConfirmDialog.cshtml`. Not an AA gap. |
| Low (ui) | `/offline` "Try again" is a JS-only `<button data-offline-retry>`; a direct no-JS nav to `/offline` gets an inert button (the "Explore Cinora" anchor is a real no-JS fallback, so marginal). | **frontend-agent (optional):** render retry as an `<a href="/">`-style reload, or note the JS dependency. |
| Low (ui, accepted) | Precached anonymous `/offline` shows the anonymous header (no avatar/bell) even for a signed-in user who goes offline. Inherent to precaching one public `/offline` (design §2.3). | **Accepted** — not misleading about offline status. |
| Low (ui, cosmetic) | `apple-touch-icon` reuses the 192 icon (no 180×180); no maskable-192. Both acceptable for install. | **Accepted / optional (pwa-agent):** add a 180×180 apple-touch-icon if iOS polish is wanted. |
| Manual (carried) | The Phase-6 browser/live-model smokes remain the human's (no browser/Ollama here): 5.5 live Ollama For-You; 6.1 PWA install/offline/update DevTools; 6.2 real push RFC-8291 round-trip; 6.3 push-settings + muted-type; 6.4/6.6 Lighthouse/CWV (LCP<2.5s/CLS<0.1/INP<200ms, ≥90); 6.5 keyboard/reduced-motion/View-Transition. | **Deployer sign-off — `docs/deployment/runbook.md` §10.** |
| Manual (carried) | `CinoraDev` / SQL Express may lag the migrations applied to `CinoraTest`. | **Apply `artifacts/migrate.sql` (or `scripts/db-update.ps1`) when the instance is up, after the §5.3 empty-`Devices` pre-apply check.** Never auto-migrate. |
| Deferred (testing) | Playwright E2E flow suite (auth/search/review/watchlist/notification + dark-mode/reduced-motion projects) **authored** at 6.6 under `e2e/`; the **run** needs a live Release app + browser (carried CI/manual). Also the `RegisterDeviceCommand` reconcile-branch concurrency unit test (6.3) + the cancelled-confirm zero-request assertion (6.5). | **testing-agent / CI:** run `e2e/` against a Release app; add the two remaining unit/E2E assertions. |
