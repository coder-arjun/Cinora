# Cinora — Progress Log

Running log of completed milestones. Newest first. Maintained by the orchestrator / documentation-agent.

## ▶️ RESUME HERE — 🎉 **CHAT — read-receipt TICKS + timezone re-fix BUILT, GREEN & DEPLOYED (2026-07-15, follow-up).**
Two more user asks after the 6-improvement batch:
- **Delivery/read TICKS (WhatsApp-style):** ✓ sent · ✓✓ grey delivered · ✓✓ **blue** read, on my own sent bubbles. Server computes `ChatMessageVm.IsReadByOthers` (all other members' `LastReadAtUtc >= SentAtUtc`) for the on-load state; realtime upgrades via new `ChatHub.MarkDelivered` → `IChatClient.MessageDelivered` (double-grey) and existing `ConversationRead` (blue). `chat.ts` `setChatTicks()` (textContent + `text-sky-400` toggle, CSP-safe); read is now **visibility-gated** (a background tab is "delivered", not "read"); `#chat-root` carries `data-current-user-id` so a client ignores its own echoes.
- **Timezone RE-FIX (root cause):** the server rendered `SentAtUtc.ToString("o")` but EF loads `datetime2` as **Unspecified** → the ISO string had **no `Z`** → `new Date()` parsed it as LOCAL (no conversion). Fixed: `chat.ts` `toUtcDate()` appends `Z` when zone-less; the Razor `<time datetime>` now emits `…Z` (`ToString("s")+"Z"`). Realtime was already correct (fresh `Kind=Utc` → had `Z`).
**NO schema change** (read state is computed). Build 0/0; **546 tests green** (Web 243; +3 chat: read-state, +the batch's push/share); `npm run build` clean (chat.js 15.0kb; `text-sky-400` in CSS verified). **DEPLOYED (file-only, no migration, no logout):** FTPS-uploaded 4 Cinora DLLs + `/dist` (app-f0c248551f.css / site-6ESE53B3.js / chat-VPP7CNDL.js / manifest, +.br+.gz) + `sw.js`; SHA-verified Web/Application.dll + chat.js; smoke `/health` 200 + assets 200 + `/chat` 302. **Carried (user):** two-device check that ticks go ✓→✓✓→blue; confirm times now match the local clock.

## 🎉 **CHAT — 6 USER-REQUESTED IMPROVEMENTS BUILT, GREEN & DEPLOYED (2026-07-15).**
**DEPLOYED:** user applied `migrate.sql` (`AddDataProtectionKeys` + `AddChatMovieShare`) to `db58983`; agent FTPS-uploaded the 4 Cinora DLLs + the **new `Microsoft.AspNetCore.DataProtection.EntityFrameworkCore.dll` + updated `Cinora.Web.deps.json`** (new package dependency) + current `/dist` (app-87af7670c2.css / site-LJ3UO2MJ.js / chat-A56YS2UM.js / esm / chunk / manifest, +.br +.gz) + `sw.js`; SHA-256 verified Web.dll + the new DP-EF dll + app.css byte-identical; smoke: `/health` 200 Healthy (app booted with DB-persisted keys → the new dependency resolved), `/` 200, new css+chat.js 200, `/chat`+`/discover` unauth 302. One-time logout expected on this deploy (key-ring source switch). Checklist: `docs/deployment/deploy-2026-07-15-chat-improvements.md`. **Carried (user):** on-device push-notification grant + a real new-message alert; rotate `db58983`+FTP passwords.
User asked for 6 fixes/features on top of the shipped chat. All done; build **0/0**; **544 tests green** (Domain 65, Application 128, Infrastructure 110, Web 241 — 30 chat tests incl. new movie-share ×2 + chat-push ×1); `npm run build` clean. **TWO new additive migrations** (`AddDataProtectionKeys`, `AddChatMovieShare`) in `artifacts/migrate.sql`. Details:
1. **Logged-out-often FIX (adopted DailyPilot/ExpenseTracker):** Data-Protection key ring now persists to the **DB** (`PersistKeysToDbContext<CinoraDbContext>` — CinoraDbContext implements `IDataProtectionKeyContext`; migration `AddDataProtectionKeys`) instead of the filesystem (which the free host wipes on redeploy → regenerated keys → mass logout); **force-persistent sign-in** (`OnSigningIn` sets `IsPersistent=true` + 30-day expiry so the mobile PWA session cookie survives a close); cookie 14→**30 days**. ⚠️ This deploy logs everyone out ONCE (old filesystem keys abandoned), then they stay signed in durably.
2. **Chat privacy — VERIFIED** (already enforced: participation-checked reads → 403; H1 removed-member live cut-off; M2 no pre-join history). Movie-share path uses the same gate.
3. **Movie sharing:** share a title to a friend/group via chat. New `Message` shared-movie columns (migration `AddChatMovieShare`) + `Message.CreateSharedMovie` + `ShareMovieToConversationCommand` + `/chat/share` picker page + a "Send to a friend" button on the movie Details page + a movie-card render in the thread (server `_ChatMessage` + realtime `chat.ts`).
4. **Emoji picker redesigned:** professional tabbed picker (9 category tabs, ~400 emojis, sticky headings, hover-scale, multi-insert) in `chat.ts` + `data/emoji.ts`.
5. **Mobile push on new message:** `SendChatPushCommand` + `IPushDispatch.EnqueueChatMessage` (reuses `IPushSender`/Device pipeline, no inbox row) enqueued from `SignalRChatNotifier` — friendly title (sender / "sender · group"), body preview, `/chat/{id}` deep link, `chat-{id}` tag; recipients = members except sender.
6. **Timezone FIX:** `chat.ts` `localizeTimestamps()` converts all `[data-chat-time]` (server UTC) to the viewer's local time on load + after HTMX swaps.
**⏭️ Deploy (human-gated, same split as before — this env can't reach prod SQL :1433):** (1) apply `artifacts/migrate.sql` to `db58983` (adds `DataProtectionKeys` + the `Messages` share columns — additive); (2) agent FTPS-uploads the changed DLLs + `/dist` + `sw.js`. Checklist: `docs/deployment/deploy-2026-07-15-chat-improvements.md`.

## 🎉 **IN-APP CHAT (1:1 + GROUP + EMOJI + TYPING + READ RECEIPTS) — BUILT, REVIEWED & DEPLOYED (2026-07-15).**
User opted into chat (reverses ADR 0010); scope chosen via question: **friends-only 1:1 + group messaging, MVP + typing indicators + read receipts, built-in emoji picker.** Built subagent-driven/direct (no-git; ledger `docs/superpowers/plans/2026-07-12-chat-progress.md`). **Design:** `docs/architecture/chat-design.md`; **plan:** `docs/superpowers/plans/2026-07-12-chat.md`; **new ADR 0024** (chat realtime + participation-as-authorization; supersedes ADR 0010). Delivered C1–C5:
- **C1 — data model:** 3 additive entities `Conversation`/`ConversationMember`/`Message` + 3 EF configs + **`AddChat` additive migration** (3 tables; unique `(ConversationId,UserId)`; keyset `(ConversationId,SentAtUtc)`; Restrict FKs to `Users`). Soft-delete tombstones; denormalized `LastMessageAtUtc`; `LastReadAtUtc` drives unread + receipts.
- **C2 — verticals (CQRS, hand-rolled mediator):** start-DM (find-or-create, friends∧¬blocked), send (membership + DM block re-check + best-effort push), history (keyset), conversations list (N+1-free unread/preview/other-member), mark-read, create-group (+validator, friends-only, creator=Admin), add/remove(admin)/leave(admin-promote-earliest)/rename(admin)/delete(sender-only, tombstone). Actor server-resolved (ADR 0009); every op participation-gated in the handler.
- **C3 — realtime:** `ChatHub` (`[Authorize]` fail-closed; membership-checked `conversation-{id}` joins + per-user `chat-user-{id}`) + `IChatClient` + `IChatMembership`/`ChatMembership` (thin scoped) + `SignalRChatNotifier` (best-effort try/catch→Warning) + `Program.cs` wiring + `MapHub("/hubs/chat")`. **NO CSP change** (same-origin `wss`, `connect-src 'self'`).
- **C4 — premium `/chat` UI:** two-pane list+thread, message bubbles (`_ChatMessage`, styled sender-only delete), composer, **built-in emoji picker** (`data/emoji.ts` 8 categories, inserted at caret), new-group modal, group management (rename/add/remove/leave), `_ConversationRow` unread badge, `vc:chat-nav` nav entry + badge (`GetChatUnreadCountQuery`), "Message" button on friend cards. Client `Scripts/components/chat.ts` (lazy-loaded on `#chat-root`; SignalR join/receive-**dedupe**/scroll, Enter-to-send, auto-grow, modals) — all CSP-safe `addEventListener`, no inline JS. XSS: `@`-encode + `textContent` only, no `Html.Raw`.
- **C5 — live signals (folded into chat.ts):** typing indicator (debounced `Typing` → `UserTyping`, auto-clear) + read receipts (mark-read on open + `ConversationRead` → "Seen").
- **C6 — offline Web Push: DEFERRED** (out of chosen MVP+ scope; the `IPushDispatch` pipeline is keyed to a `Notification` inbox row per push — reusing it per message would pollute the inbox; the `IChatNotifier`/`ChatHub` seam leaves room to add it later. See ADR 0024.)
**Final review (read-only code-review-agent) — 3 findings, ALL FIXED + regression-tested** (details in the chat ledger): **H1 (High)** a removed group member kept receiving live messages via the stale `conversation-{id}` group → fan message pushes to each *current* member's `chat-user-{id}` group (resolved live at send); **M2 (Med)** a newly-added member could read pre-join history → `GetMessages` now bounds to `SentAtUtc >= my JoinedAtUtc`; **L1 (Low)** DM block re-check misfired on a 2-member group → gate on `Type == Direct`. No schema change.
**Verified-green (2026-07-14):** `dotnet build Cinora.sln -c Release` **0/0** (TWAE on); `dotnet test Cinora.sln -c Release` → **542 passed / 0 failed** (Domain 65, Application 128, Infrastructure 110, **Web.IntegrationTests 239** — incl. **~36 chat tests** across 8 suites, with H1/M2 regression tests); `npm run build` clean (`tsc` strict + esbuild; `chat.js` split chunk 8.3 kb); **CSP byte-unchanged** (`SecurityHeadersTests` green). **One additive migration** `AddChat` — applied to `CinoraTest` (LocalDB) + regenerated into `artifacts/migrate.sql`; **NOT yet applied to prod `db58983`.** Also fixed 2 pre-existing `AuthenticationFlowTests` that asserted the old post-login `/home` target → now `/discover` (the redirect change the user requested earlier this session).
**✅ DEPLOYED 2026-07-15** (`docs/deployment/deploy-2026-07-15-chat.md`): user applied `migrate.sql` (`AddChat`) to `db58983` via SSMS (this env can't reach prod SQL :1433); agent FTPS-uploaded the changed DLLs + `/dist` assets + `sw.js` to `site78342.siteasp.net` (app_offline dance, SHA-256-verified byte-identical, `App_Data/` + prod secrets preserved). Smoke: `/health` 200 Healthy, `/` 200, new css+`chat.js` 200, `/chat` unauth 302→login.
**⏭️ Remaining (human):** **(1)** git commit (agents never touch git). **(2) ⚠️ ROTATE the `db58983` + FTP passwords** (shared in chat this session) + delete throwaway test accounts. **(3)** final manual smokes: log in → click chat → confirm `/chat` opens (no 500); two-browser realtime (message + typing + "Seen"). **(4)** local `CinoraDev` already has `AddChat` (applied this session).

---

## 🎉 **POST-PHASE-6 "FRIENDS & SOCIAL OVERHAUL" + PWA IMAGE FIX + LOGIN-ONLY — COMPLETE & VERIFIED (2026-07-11).**
User-requested overhaul (brainstormed → spec → plan → executed subagent-driven). **Design:** `docs/architecture/friends-social-overhaul-design.md`; **plan + ledger:** `docs/superpowers/plans/2026-07-10-friends-social-overhaul.md` (+ `-progress.md`); **new ADRs 0022** (user-blocking model) **& 0023** (authenticated-only access). Delivered, all 15 tasks:
- **Unit A — PWA poster fix (B0):** movie thumbnails were blank on the deployed free host because posters went through the same-origin `/tmdb-img` proxy that fetched `image.tmdb.org` **server-side** (the free host can't reach it). Fixed by routing posters through the free **`images.weserv.nl`** image-proxy CDN (`TmdbImageUrlBuilder` wraps the TMDB URL; CSP `img-src` adds `https://images.weserv.nl`); the old server-side `TmdbImageController`/`/tmdb-img` proxy is **deleted**. WHY weserv and not direct `image.tmdb.org`: the first attempt emitted direct TMDB URLs, but **TMDB's image CDN (`image.tmdb.org`) is unreachable from the deployed MonsterASP host AND blocked on the client's network/region** — so BOTH the old same-origin server proxy AND a direct browser load fail (metadata via `api.themoviedb.org` still works). weserv fetches TMDB server-side (its network reaches TMDB) and serves the (WebP-optimized) image from a globally-reachable edge. Verified live: CSP updated, rail renders `images.weserv.nl/?url=image.tmdb.org/...`, that URL returns `200 image/webp`. Deployed 2026-07-12 (DLL-only redeploy, SHA-verified). `NSubstitute` wired into `Web.IntegrationTests`.
- **Unit B — blocking + search + hub + login-only:** NEW `UserBlock` entity (directional, separate from `Friend`) + `AddUserBlock` **additive migration** (unique `(BlockerId,BlockedUserId)` + `(BlockedUserId)`, Restrict FKs). Block = **full cut-off + mutual-hide + silent** (removes friendship both ways; owner-blocks-viewer→404; viewer-blocks-owner→`BlockedByMe` Unblock-only), enforced fail-closed through one `BlockQueries` seam (search / profile / friend-request / feed). **Exact-match user search** by username (`User.Normalize`) OR email (new `IUserDirectory` port → `UserManager.FindByEmailAsync`), self/block-excluded, **no-leak** (blocked & not-found are identical empty results). Redesigned **premium `/friends` hub**: Find-people search + WhatsApp invite → Requests (with new **Cancel** outgoing) → Friends → **Blocked** (Unblock). Home/Friends "find friends" CTAs repointed off movie-Discovery. **Login-only (ADR 0023):** `[AllowAnonymous]` removed from `DiscoveryController` + `CommentsController.List`; allowlist kept (landing/auth/tour/pwa/media-token/health/diagnostics); 29 anonymous-browse tests converted to authenticated.
- **Unit C — chat (1:1+group+emoji):** the user OPTED IN (reverses ADR 0010) — **NOW BUILT (2026-07-14); see the top "IN-APP CHAT" block + ADR 0024.** Built on this blocking model + exact-match search + Phase-3 SignalR, as anticipated.
**Verified-green (2026-07-11):** `dotnet test Cinora.sln -c Release` → **508 passed / 0 failed** (Domain 58, Application 128, Infrastructure 110, **Web.IntegrationTests 212**); build **0/0** (TWAE on); `npm run build` clean; **CSP byte-unchanged** (test-verified). **One additive migration** (`AddUserBlock`) — applied to `CinoraTest`; **remote `db58983` migrated by the user (2026-07-12) and the app DEPLOYED** via FTPS to `site78342.siteasp.net` `/wwwroot` (agent-executed with the user's explicit "deploy it": `app_offline.htm` dance → 238 files clean + `app-6126237582.css` SHA-256-verified byte-identical → `app_offline` removed → live; `appsettings.Production.json` + `App_Data/` preserved). Post-deploy browser smoke (health/login-gate/posters/friends) is the user's to run.
**⏭️ Remaining (human-gated, NOT done by agent):** **(1)** git commit (agents never touch git); **(2)** DEPLOY to MonsterASP.NET — `dotnet publish -c Release` → FTP output to `site78342.siteasp.net`, and apply `artifacts/migrate.sql` (now includes `AddUserBlock`) to remote `db58983` (per `docs/deployment/monsterasp-filezilla-deploy.md`); **(3)** rotate the DB/FTP passwords shared in chat; **(4)** design + build **Unit C chat** if wanted. Run the review gates (`/review-security` etc.) for a deeper pass if desired — per-task reviews were done during execution.

---

## 🎉 **PHASE 6 (Polish & Deployment) COMPLETE — ALL 6 PHASES DELIVERED. Cinora is feature-complete.** **Phase 6 EXIT GATE PASSED at 0 Critical / 0 High across ALL FIVE dimensions** (holistic over 6.1–6.6): architecture ✅ 0C/0H/0M, security ✅ 0C/0H (**CSP byte-unchanged verified verbatim** + shipped graph **vuln-free** + offline-privacy/push-IDOR/DP-keys-not-web-served/delete-confirm confirmed), performance ✅ 0C/0H (all §6.7 wins present + regression-free, zero-LLM-on-request holds), code ✅ 0C/0H (re-ran build 0/0 + 464 tests + `tsc` strict), UI ✅ 0C/0H **WCAG AA** (re-measured contrast, manifest/icons cohere, delete-confirm rollout complete). **6.6 (deployment readiness — PLANS-ONLY) COMPLETE:** DP-key-ring persistence (`PersistKeysToFileSystem`→`App_Data/keys`, guarded `if (!isTesting)`) + the absent-VAPID one-shot boot `Log.Warning` wired in `Program.cs` (221-235, 403-413); `docs/deployment/runbook.md` authored (plans-only); `dotnet publish -c Release` verified — PWA artifacts + `/dist/*.br/.gz` present. **Playwright E2E suite AUTHORED** under `e2e/` (5 flows: auth/search/review/watchlist/notification + dark-mode/reduced-motion projects; installs + compiles + 16-skip graceful-precondition — the **run** is carried CI/manual). **§6.7 REVIEW_BACKLOG triaged to fixed-or-accepted** (Serilog query-string closed as Accepted; 4 UI Lows recorded). **Doc-drift reconciled** — runbook §3.1/§4/footer + ADR-0021 + ADR-0019 now match shipped code (DP-key persistence, VAPID Warning, `/account/*` no-store all flipped "pending"→"shipped").
**Last verified-green state:** build **0/0**, **npm build ✅**, **464 tests** pass (Domain 56, Application 128, Infrastructure.Tests 110, Web.IntegrationTests 170), **CSP byte-unchanged** (SHA-256 identical since Phase 2), **shipped graph vuln-clean**. Phase 6 un-deferred by the user 2026-07-06. **Phase 5 EXIT GATE PASSED** (5.0–5.4). Phases 1–4 done. **⚠️ `CinoraDev`/Express still carries the Phase-6 migration apply** (Express-unreachable — apply `artifacts/migrate.sql` / `db-update.ps1` when up; **verify `Devices` empty first**). **Deferred/manual (non-blocking):** 5.5 live-Ollama smoke; the 6.1 PWA browser smoke; the 6.2 real push round-trip; the 6.3 push-settings browser smoke; the 6.5 keyboard/reduced-motion/View-Transitions a11y smoke; **the 6.4/6.6 Lighthouse/CWV measurement** — all carried in `docs/deployment/runbook.md` §10.

**⏭️ NO NEXT PHASE — the 6-phase `Implementation_Plan.docx` is fully delivered.** Remaining work is all NON-CODE / carried (none blocks; all in `docs/deployment/runbook.md §10`): **(1) ⚠️ apply the Phase-6 migration to `CinoraDev`/Express** — `artifacts/migrate.sql` (idempotent) or `scripts/db-update.ps1` when the `localhost` SQL instance is up; **verify `Devices` is empty/≤1-per-user first** (the new unique `(UserId, EndpointHash)` index would collide on the `''` default). Never auto-migrate. **(2) Carried browser/live-model smokes:** 5.5 live-Ollama For-You (`ollama serve` + `ollama pull llama3.2` → trigger `/jobs`); 6.1 PWA install/offline/update DevTools; 6.2 real push RFC-8291 round-trip; 6.3 push-settings + muted-type; 6.4/6.6 Lighthouse/CWV (LCP<2.5s / CLS<0.1 / INP<200ms / ≥90 Perf+A11y+BP+SEO); 6.5 keyboard/reduced-motion/View-Transition. **(3) Run the `e2e/` Playwright suite** against a Release app + migrated DB (`cd e2e && npm install && npx playwright install chromium && npx playwright test`). **(4) Optional accepted backlog Lows** (`_ConfirmDialog` `role=alertdialog`; no-JS `/offline` retry; Serilog `?q=` scrub). **Optional user inputs:** free Google OAuth creds (the sign-in path stays unverified without them). **Free-only throughout: Ollama local, NO paid OpenAI, NO Docker, plans-only deploy.**
- **⚠️ Pending manual browser + env smokes (non-blocking, carried):** **(4.3)** the watchlist menu on Details desktop+360px / rail / search; **(4.4)** the `/watchlist` page live status-change count re-bucket / filtered-out removal / live-empty / load-more retry; plus **2.7** live-TMDB and **3.5** realtime; and **(5.5)** Ollama live recommendations. **`CinoraDev`/prod still un-migrated** — apply `artifacts/migrate.sql` when the `localhost` SQL instance is up.

**Phases 1–4 COMPLETE** (each exit gate PASS). **Phase 4: 4.1 ✅, 4.2 ✅, 4.3 ✅, 4.4 ✅, 4.5 ✅ — exit gate PASS (2026-07-06, all 5 dimensions 0 Crit/0 High).** Now **Phase 5 (AI recommendations)** — DESIGNED (`docs/architecture/phase-5-ai-design.md` + ADRs 0016–0018), implementation starting at 5.0. **Phase 6 (Polish & Deployment)** — DESIGNED (`docs/architecture/phase-6-polish-deployment-design.md` + ADRs 0019–0021), deferred (STOP after Phase 5 per user directive).

**Phase 4 in flight (design: `docs/architecture/phase-4-watchlists-profile-design.md` + ADRs 0013/0014/0015):** free-local `IFileStorage` avatar uploads (local FS behind the port, magic-byte validation, HMAC bucketed-expiry signed URLs = SAS stand-in — **NOT Azure Blob, no image library**); watchlist status from every title surface; profile edit/settings; privacy-shaped public-profile highlights. **No new entity/column** (index-only, §10). Build order (§14): **4.1 ✅ → 4.2 ✅ → 4.3 (watchlist control everywhere) → 4.4 (watchlist page + 2 indexes) → 4.5 (profile highlights)** → Phase 4 exit gate. **NEXT: 4.3** — `SetWatchlistStatusCommand`/`RemoveFromWatchlistCommand`/`GetWatchlistStatusQuery`/`GetWatchlistStatusMapQuery` (the N+1-killer batch map); `WatchlistController` reusing the ADR-0008 `EnsureTitleCached`→command orchestration; `_WatchlistControl` on Details (replace the 2.5 stub) + `_TitleCard` (rails/search, anchor-nesting restructure §5.3) via the batch map; Watched→review nudge; tests W1–W6; **no migration** (existing unique indexes serve it). Perf gate (no N+1 across a rail) is the key gate.

**Phase 3 delivered (3.1–3.5):** the `ICurrentUser` seam + resource-ownership→403; Reviews CRUD; Likes + flat Comments; Friends (directed-pair guard) + privacy-shaped Profiles; the read-time keyset Activity feed at `/home`; and Notifications + **self-hosted SignalR** realtime (best-effort push behind `IRealtimeNotifier`, `[Authorize]` hub group-per-user, inbox + live toast/badge via a locally-bundled `@microsoft/signalr`). 5 migrations (all index-only): `InitialCreate` → `ReviewListIndex` → `FriendLookupIndexes` → `FeedIndex` → `NotificationInboxIndex`. **No new entity** — everything fit the Phase-1 schema. In-app chat **deferred** (ADR 0010; PRD-only).

**⚠️ OPEN ITEMS to close before/at deployment (none block further feature work):**
1. **`CinoraDev` NOT migrated** — the `localhost` SQL Server Express instance was unreachable all session, so the **5 migrations are applied only to `CinoraTest` (LocalDB)** where the 201 tests run green. Apply `artifacts/migrate.sql` (idempotent, reviewed) or `scripts/db-update.ps1` to `CinoraDev` (and prod) when Express is up. Never auto-migrate on startup.
2. **2.7 live TMDB smoke** — Home/Search/Details against the real key (already set in user-secrets) via a browser; confirm posters render + repeat requests hit the in-memory cache. Manual.
3. **3.5 live realtime smoke** — two browser sessions: A likes B's review → B sees a live toast + badge increment without reload; confirm `connect-src 'self'` admits the same-origin `wss://`. Manual.
4. Deferred Medium/Low findings across Phase 2–3 accumulate in `REVIEW_BACKLOG.md` (notably: **response compression** for the JS/CSS bundle — Phase 6 perf; the feed body SQL-truncation; the notification-build DRY factory; a handful of additive tests). None are Critical/High.

**Next: Phase 4 (Watchlists & Profile)** per `Implementation_Plan.docx` / `.claude/phases/` — watchlist from every surface + statuses + a watched→review nudge; profile edit + **avatar upload via the free `IFileStorage` port (Azurite/local filesystem, NOT Azure Blob)**; settings + privacy-respecting queries. Reuse the established seams: `ICurrentUser`, `IRealtimeNotifier`, `EnsureTitleCached`, the keyset/projection pattern, the region-scoped `site.ts` a11y plumbing, `social-write` rate-limit, `TestAuthentication`. Run `/run-phase 4` or `/orchestrate` to begin.

**Two carried 3.5 obligations (MUST honor — from 3.2/3.3 gates):** (1) **[SECURITY] Razor-encode `Notification.Message`** in the inbox/toast/bell — it stores the actor's raw `DisplayName` (stored-XSS otherwise) — a 3.5 security-gate check; (2) **like re-notify dedup** — in `LikeReviewCommand`, skip the co-persist when a recent `ReviewLiked` for the same `(ActorUserId, TargetId=ReviewId)` already exists (so like→unlike→like doesn't spam).

**Forward obligations for 3.5 (from 3.2/3.3 gates — MUST honor):** (1) the notification inbox/toast **MUST Razor-encode `Notification.Message`** (stores actor's raw DisplayName → stored-XSS otherwise) — a 3.5 security-gate check; (2) like→unlike→like **re-notify dedup**. Notification co-persist is ALREADY wired in the like/comment (3.2) + friend-request/accept (3.3) handlers — 3.5 adds the `IRealtimeNotifier` push on top + the inbox.

**Forward obligations for 3.5 (from 3.2 gates — MUST honor):** (1) the notification inbox/toast **MUST Razor-encode `Notification.Message`** (stores the actor's raw DisplayName → stored-XSS risk otherwise) — a 3.5 security-gate check; (2) add like→unlike→like **re-notify dedup** (skip co-persist when a recent `ReviewLiked` for the same `(ActorUserId, TargetId)` exists).

**Done since the pause (2026-07-02):**
1. **2.0–2.2 robustness fix round — ✅ COMPLETE.** F1 concurrency-safe `SyncGenres`; F2 `EnsureTitleCached` two-committed-step (isolated genre upsert) + the **H-1** follow-up (`IAppDbContext.DiscardPendingChanges()` so a genre race can't poison the title `SaveChanges`); F3 blank-title→`NotFound`; F4 tolerant numeric JSON; F5 deterministic race test (SQLite-in-memory; EF InMemory removed). Details in the 2.2-fix-round milestone line below.
2. **ADR 0008 doc pass — ✅ COMPLETE (2026-07-02, documentation-agent).** `docs/adr/0008-tmdb-persistence-via-commands.md` verified complete (decision: two `ISender` commands — `SyncGenresCommand` + `EnsureTitleCachedCommand` — writing via `IAppDbContext` directly, **no `ITitleCatalog` port**; port deferred to a Phase-3 trigger, now incl. an explicit 4th trigger — `Movie`/`Genre` acquiring genuine invariants → aggregate root per §7; cross-links to ADR 0006/0007). Grep `docs/` for `ITitleCatalog` confirms every remaining mention is either canonical (ADR 0008) or a dated 2026-07-02 amendment/reconciliation pointer (ADR 0006 Amendments + Related; ADR 0007 Amendments + Related; `phase-2-discovery-design.md` reconciliation note + §1 struck-through `TitleCatalog` row). REVIEW_BACKLOG **A5 closed** (ADR-0002 example already `Database=CinoraDev`; no stale `Database=Cinora` in `docs/`). Doc-only (no build impact).
3. **2.3 Home / discovery rails — ✅ COMPLETE.** `/discover` public rails (Trending/Popular/TopRated), lazy HTMX per-rail, premium dark cards; four gates clean (found + fixed a UI **High**: no rail failure state). **Live-smoked** against real TMDB into `CinoraDev`. Details in the 2.3 milestone line below.
4. **Next — user directive (2026-07-03): complete Phase 2 → Phase 3 autonomously.**
   - **2.4 Search — ✅ COMPLETE (2026-07-03).** See the 2.4 milestone line below. Build 0/0, **143 green**. Review gates: security ✅ / code ✅ (0 Crit/High); UI found **1 High** (deep-link box-cleared by `x-model`) + 2 a11y Med — all fixed & verified. `@alpinejs/csp@3.15.12` swap confirmed eval-free.
   - **2.5 Details (NEXT)** (design→build): `GetTitleDetailsQuery` + `DiscoveryController.Details` ([AllowAnonymous]) dispatching `EnsureTitleCachedCommand` after the read query; honor the `/discover/title/{media}/{tmdbId:int}` route the 2.3/2.4 cards already link to; backdrop hero+scrim, cast, community-rating placeholder, add-to-watchlist stub; fold in `OutputCache` (REVIEW_BACKLOG 2.3 Med) → **Phase 2 exit gate**. Design in §6 + §9.2.5 of `phase-2-discovery-design.md`.
   - Then **Phase 3 (Reviews & Social)** — **DESIGN DONE (2026-07-03):** `docs/architecture/phase-3-social-design.md` + ADRs **0009** (resource-ownership + `ICurrentUser` seam, enforced in the command handler → 403), **0010** (in-app chat DEFERRED), **0011** (self-hosted SignalR behind an `IRealtimeNotifier` port, no Azure/backplane), **0012** (read-time keyset activity feed). 5 milestones, all pinned with per-feature contracts + test plans + `Verify` commands (§16 build order): **3.1** current-user seam + Reviews CRUD → **3.2** Likes+Comments → **3.3** Friends+profiles → **3.4** Activity feed (`/home`) → **3.5** Notifications+realtime. `ICurrentUser`/`ForbiddenAccessException` is the 3.1 prerequisite (doesn't exist yet). Resource-based authZ + plain-text-UGC/Razor-encode (no `Html.Raw`) first-class. **In-app chat: DEFERRED** (PRD-only; absent from schema/Implementation_Plan/Prompt.txt — proceed WITHOUT unless the user opts in).
   - Then **Phase 4 (Watchlists & Profile)** — watchlist from every surface + statuses + watched→review nudge; profile + **avatar upload via the free `IFileStorage` port (Azurite/local filesystem, NOT Azure Blob; local time-limited URLs stand in for SAS)**; settings + privacy-respecting queries.

**User inputs:** **TMDB v4 key — ✅ SET + live-verified (2026-07-02)** in user-secrets (`Tmdb:ApiKey`; v4 Read Access Token, Bearer auth confirmed against `/3/genre/movie/list`). Still open (non-blocking until noted): optional free **Google OAuth** creds — to verify that sign-in path; **in-app-chat** scope decision — needed before Phase 3.


## Environment (verified 2026-07-02; refreshed after local-machine setup)
- .NET SDK **10.0.301** (resolves per `global.json`; 10.0.204 also installed); ASP.NET Core 10 runtime 10.0.8; **dotnet-ef 10.0.8**
- Node **v24.14.0**, npm **11.9.0**
- SQL Server: **app DB now on `localhost`** (SQL Server 2022 Developer, default instance `MSSQLSERVER`, Windows auth) — `appsettings.Development.json` + `CinoraDbContextFactory` point here; `CinoraDev` created (20 tables, 27 genres). LocalDB `(localdb)\MSSQLLocalDB` still hosts the `CinoraTest` integration DB.
- **Docker: NOT installed and NOT to be used** (user constraint)
- Not a git repository (per project rules, agents never touch version control)

## Hard Constraints
- **FREE ONLY, NO DOCKER** (user, 2026-07-02): zero-cost software/free tiers only; no paid services. Substitutions: LocalDB/Express (SQL), `IDistributedCache` in-memory (cache, Redis optional via Memurai), Azurite/local filesystem behind `IFileStorage` (blob), Ollama/free-tier LLM behind `IRecommendationEngine` (AI, not paid OpenAI), self-hosted SignalR, Hangfire OSS, self-VAPID Web Push, TMDB free tier, free Google OAuth. Authoritative table: `CLAUDE.md`.
- **LOCAL ONLY, NO SUBSCRIPTIONS** (user, 2026-07-02): everything runs on this machine; no cloud accounts/subscriptions/hosted services; deployment is local/self-hosted and plans-only. **devops-agent NOT used** — its local duties (build/run scripts, dev env, Options wiring, `/health`) go to backend-agent.
- **No git actions by agents.**

## Open Decisions (flagged to user)
- **In-app chat**: listed in PRD, absent from schema and Implementation_Plan. Scope decision needed before Phase 3. Does not block Phase 1.
- **Google OAuth credentials**: full "sign in with Google" verification needs a real (free) client id/secret (user-provided via user-secrets). Email/password sign-in is fully verifiable without it.
- **AI provider (Phase 5)**: default recommendation **Ollama** (local, free). Alternative free-tier cloud (Gemini/Groq). Revisit at Phase 5.
- **Test assertion library (finalize at 1.1)**: FluentAssertions v8+ is commercially licensed → **banned** under free-only. Use xUnit built-in `Assert` (default) or a free lib (Shouldly / AwesomeAssertions). testing-agent finalizes.
- **Mediator library — RESOLVED (1.4, 2026-07-02)**: use a **hand-rolled minimal mediator** (`ISender`, `IRequest<T>`, `IRequestHandler<,>`, `IPipelineBehavior<,>`) in Application. Rationale: zero third-party/licensing exposure (fully free forever; nothing that can go commercial like MediatR did), ~100 lines matching the `cqrs-mediatr` skill's patterns, behind `AddApplication()` so swappable. Rejected: MediatR (now commercial); martinothamar `Mediator` (MIT/free but an added dependency). **FluentValidation** (Apache-2.0) and **Serilog** (Apache-2.0) are free — kept.

## Design Artifacts
- `docs/architecture/solution-structure.md` — authoritative Phase 1 layout, references, packages, concern map, milestone build order 1.0–1.7 (each with exact verify command).
- `docs/adr/0001` layering · `0002` LocalDB/no-Docker · `0003` ApplicationUser placement · `0004` free/local-only infrastructure · `0005` hand-rolled mediator. (0004 + 0005 written 2026-07-02 in the Phase 1 doc reconciliation; ADR 0001 §3 amended to carve out the auth-vertical Web→Infrastructure exception, ADR 0002 example DB names corrected to `CinoraDev`/`CinoraTest`.)

## Milestones

### Phase 6 — Polish & Deployment  ✅ COMPLETE (2026-07-06; user un-deferred, overrides STOP-after-5) — EXIT GATE PASS
- **Phase 6 EXIT GATE — ✅ PASSED at 0 Critical / 0 High across ALL FIVE dimensions (2026-07-06)** — holistic review over the whole 6.1–6.6 surface, each dimension verifying its slice of the exit criteria. **Architecture ✅ 0C/0H/0M** — dependency rule clean (WebPush vendor types confined to `WebPushSender`, ports speak Application-owned records, Hangfire Infra+Web only), the ADR-0011 seam extended **exactly once**, owned-VO → 4 `bit` columns (no join/entity), **exactly one** additive Phase-6 migration (`Phase6PushAndPreferences`), CSP byte-identical, free-only intact (only new runtime pkg `WebPush` 1.0.13 MIT + transitive `Portable.BouncyCastle` MIT). **Security ✅ 0C/0H** — **CSP byte-unchanged verified verbatim** (baseline string identical; repo-wide sweep: no inline script/handler/`Html.Raw`-on-user-content), shipped graph **vuln-free** (`dotnet list package --vulnerable` — WebPush/BouncyCastle clean; the only advisory is the known **test-only** SQLite one), offline-privacy (authed HTML→`no-store` + SW refuses `no-store` + BREACH-safe compression), push has **no IDOR** (server-resolved actor, recipient from the persisted row), VAPID private key **never committed/logged**, DP-keys under `App_Data` (**sibling of `wwwroot`, not web-served**), delete-confirm re-issues the byte-identical request + fails closed. **Performance ✅ 0C/0H** — all §6.7 wins present + regression-free (static compression, bundle −27%, avatar 304-before-read, feed SQL truncation, push read-fold 3→1), **zero synchronous LLM on any request still holds**, and the SW split SignalR chunk is runtime-cached under the VERSION-scoped cache + purged on activate (**closes the 6.4 pwa handoff**). **Code ✅ 0C/0H** — re-ran build **0/0** (TWAE) + **464 tests green** + `tsc --noEmit` strict (incl. `tsconfig.sw.json`); no `.Result`/`.Wait()`/`async void`/`Database.Migrate()` in `src`; every Phase-6 guarantee has a non-vacuous test (PWA offline-privacy, push upsert/Gone/muted-gate, BREACH, delete-anti-forgery, migration-applies). **UI/UX ✅ 0C/0H, WCAG AA** — re-measured contrast (lowest text pair 5.04:1; `.btn-danger` 6.16:1), manifest/icons cohere (surface-0 = `#090b0f` = `theme_color`), delete-confirm rollout complete (0 `hx-confirm`/`window.confirm` in views), motion+reduced-motion+aria-live patterns hold. **Doc-drift reconciled** (runbook §3.1/§4/footer + ADR-0021 verification note + ADR-0019 Amendment caveat flipped "pending"→"shipped" to match `Program.cs`/middleware). **§6.7 REVIEW_BACKLOG triaged to fixed-or-accepted** — the Serilog `?q=` item explicitly **Accepted** (low-sensitivity, local single-instance log); 4 UI Lows (`role=alertdialog`, no-JS `/offline` retry, precached anon header, apple-touch-icon) recorded; OutputCache/bell-COUNT/`EnableRetryOnFailure` measure-gated/accepted. **No item silently open.** **Carried (human/CI, not agent):** the browser/live-model smokes (§10) + the `e2e/` Playwright run + the `CinoraDev`/Express migration apply.
- **6.6 Deployment readiness (PLANS-ONLY) — ✅ COMPLETE (2026-07-06)** — orchestrator-verified build **0/0**, **npm ✅**, **464/464** (unchanged — 6.6 adds composition-root wiring + docs + E2E scaffolding, no new app behavior/tests). **Code (last Phase-6 implementation):** **DP-protection key-ring persistence** in `Program.cs` (221-235) — `AddDataProtection().PersistKeysToFileSystem(ContentRoot/App_Data/keys).SetApplicationName("Cinora")`, guarded `if (!isTesting)` so the `WebApplicationFactory` host keeps ephemeral keys (parallel test hosts never share a folder), directory created at startup → **auth cookie + anti-forgery tokens now survive an app restart** on the single self-hosted instance (the 170 integration tests prove the Testing-host skip). The **absent-VAPID one-shot startup `Log.Warning`** (403-413) — `IsConfigured==false` ⇒ a boot-log signal so a deployer isn't puzzled by silent push-off (mirrors the environment-name guard; CA1848-safe static Serilog). **Runbook:** `docs/deployment/runbook.md` authored (plans-only, 11 §§: build/DB targets/secrets/DP-keys/migration+pre-apply-check/publish-verify/env-exactness/health/operational-couplings/manual-checklist) — **NOTHING executed by an agent**; noted divergences recorded (`ConnectionStrings:DefaultConnection` key; `FileStorage:UrlSigningKey` required in prod). **Publish:** `dotnet publish src/Cinora.Web -c Release -o ./publish` succeeded — verified `./publish/wwwroot/{manifest.webmanifest, sw.js, icons/{192,512,maskable}, dist/*}` present, `/dist/*` carrying `.br`/`.gz` precompressed variants. **Playwright E2E scaffolding** authored under a new isolated top-level `e2e/` (own `package.json`/`playwright.config.ts`/`node_modules`, no entanglement with esbuild or the xUnit projects): 5 flow specs (auth/search/review/watchlist/notification, selectors read from real Razor markup + anti-forgery via the real forms + fresh-account-per-test), a `chromium` project + `dark-mode` (`colorScheme:'dark'`) + `reduced-motion` (`reducedMotion:'reduce'`) projects (design §6.5), `README.md` + support helpers. **Validated honestly:** `npm install` + `npx playwright install chromium` succeeded; all 16 tests across 3 projects **compile + load + skip gracefully** (no running app/DB here → the intended authored-but-carried outcome; **no fabricated green run**). **Deferred → REVIEW_BACKLOG:** the E2E functional run (carried CI/manual — first run will need minor selector tweaks, esp. the watchlist disclosure + debounced search). Free/local only — WebPush MIT, Playwright MIT, no Docker, plans-only deploy.
- **6.5 Accessibility + animation/design polish — ✅ COMPLETE (2026-07-06)** — orchestrator-verified build **0/0**, **npm ✅**, **464/464** (Web.Integration 165→170: +5 `UiPolishTests`), **CSP byte-unchanged (SHA-256 identical)**. **View Transitions** (`@view-transition { navigation: auto }`, root cross-fade ≤180ms, `prefers-reduced-motion`→`none`, pure-CSS progressive enhancement — no CSP impact). **Styled delete-confirm (backlog 3.1)** — replaced native `hx-confirm` on all 5 destructive controls (delete review/comment, remove friend×2, remove avatar) with a shared dark-glass `@alpinejs/csp` dialog gated via **`htmx:confirm`** (`preventDefault`→`issueRequest(true)` on confirm): **security-verified fail-closed** — cancel/superseded/dead-component all → no request; the anti-forgery token is re-attached downstream at `htmx:configRequest` so the re-issued request is **byte-identical** (endpoint/verb/token); focus-trap + Esc + focus-restore + **background `inert`**; `window.confirm` fallback never issues unconfirmed. **One-way comments-collapse (backlog 3.2)** — real `<button aria-expanded>` shown only on genuine overflow, expand focuses the full text + polite-announces, no-JS shows full body. **`htmx:sendError` offline-write affordance** — a write-scoped **singleton** polite toast (routed through the shared dismiss machinery, eviction-protected). **Empty/error/loading consistency** across `/offline` + push-denied + settings. **Three review gates, all 0 Crit/0 High:** UI ✅ clean-exit (**measured contrast** — `.btn-danger` 6.16:1, all pairs ≥AA; dialog scrim guarantees contrast over any artwork) / security ✅ (**no findings** — confirm-gate never unconfirmed, token preserved, `textContent`/Razor-encoded, CSP unchanged, server anti-forgery+authz untouched+tested) / code ✅ SHIP (focus-trap wraps correctly, singleton toast, strict TS). **4 a11y Lows (both gates) FIXED in-gate:** modal `inert`, comment-expand announce, offline-toast dismissal-parity + eviction-protection. **⚠️ Live keyboard/SR/reduced-motion/View-Transition behavior = carried Playwright/manual smoke.** No CSP/endpoint change; view/CSS/TS only.
- **6.4 Performance polish — compression + avatar validator + feed SQL truncation + SignalR lazy-load + redaction + motion/CLS — ✅ COMPLETE (2026-07-06)** — orchestrator-verified build **0/0**, **npm ✅**, **459/459** (Web.Integration 162→165: +3 `CompressionTests`), **CSP byte-unchanged**, **main JS bundle −27%** (205,997→150,475 B; `@microsoft/signalr` ~56 KB split to an authed-only `/dist` chunk). **Closes 8 §6.7 REVIEW_BACKLOG perf items** (measure-driven, ADR 0021). **Compression** — `AddResponseCompression` Brotli+Gzip, **static-MIME-only allow-list (excludes `text/html` → BREACH-safe**, replaces the framework default; `EnableForHttps`); `UseResponseCompression` below `SecurityHeaders` (upstream, CSP stamped on every response) + before `UseStaticFiles`; **immutable `/dist` caching** env-gated (`no-cache` in Development for hot reload — the in-gate MED-1 fix). **Avatar** — `MediaController` now `PhysicalFile` + a cheap weak `length+mtime` validator so `If-None-Match` **304s before any read** (was a full `byte[]` read + SHA-256 on every GET); the ADR-0013 HMAC-verify-before-serve + forced-type + nosniff + containment posture is **intact** (write-once files → the weak validator never 304s stale). **Feed** — SQL-side `Body.Substring(0,201)` (translates on SQL Server + SQLite; only the excerpt crosses the wire; feed-only). **SignalR lazy-load** — dynamic `import("@microsoft/signalr")` gated on `#notification-unread-badge` (anonymous pages never fetch it) via esbuild `splitting`; catch→polling fallback. **`CacheLog` redaction** — search query hashed in the cache-key logs. **Motion/LCP/CLS** — hover 300ms/entrance 350ms, `cinora-rise-in` transform-only (no LCP-hero opacity fade), rec-skeleton rail-padding + `min-h-9` match; reduced-motion. **Eager above-fold avatars** (`_Avatar.Eager` on nav/profile/settings). **Push read-fold** — `NotificationProjection.ToPushRow` collapses the 6.3 3-reads→1. **Three review gates, all 0 Crit/0 High:** security ✅ (**BREACH-scope airtight** — HTML/JSON never compressed, test-locked; avatar HMAC-before-serve non-regression; CSP unchanged; CacheLog redaction) / code ✅ SHIP (459 green; avatar seam + `ToPushRow` fold + async dynamic-import all behavior-preserving) / performance ✅ (all 7 changes correct + net-positive, no N+1, SQL translates; **found MED-1** dev-immutable-cache hot-reload regression → **fixed in-gate**). **Deferred → REVIEW_BACKLOG:** Serilog request-log query-string (pre-existing, search terms), avatar mid-serve-delete 404→500 TOCTOU (accepted single-instance), font re-compression (optional), the SW split-chunk VERSION-scoping (pwa handoff). **Measure-gated, correctly NOT shipped:** OutputCache + notification-bell COUNT (no browser to measure); **A3 `EnableRetryOnFailure` stays OFF (accepted).** **⚠️ Lighthouse/CWV measurement = carried MANUAL (§6.6).**
- **6.3 Notification preferences — owned VO + first migration since Phase 4 + push-only gate + `/settings/notifications` — ✅ COMPLETE (2026-07-06)** — orchestrator-verified build **0/0**, **npm ✅**, **456/456** (Domain 46→56, Application 116→128, Web.Integration 156→162; +28), **CSP byte-unchanged**. **Un-defers ADR 0015** (ADR 0020 §4/§5). **The FIRST schema migration since Phase 4** — `Phase6PushAndPreferences`, **additive-only** (`Up()` = 4× `bit NOT NULL DEFAULT 1` pref cols on `Users` via an EF **owned type** + `Device.EndpointHash char(64) NOT NULL` + unique `(UserId, EndpointHash)`; `Down()` reverses; no drops/renames in Up), `artifacts/migrate.sql` regenerated (idempotent); auto-applies to `CinoraTest`, **`CinoraDev`/Express carried** (verify `Devices` empty first). **Model:** `NotificationPreferences` owned VO (4 default-`true` flags + `IsPushEnabled(type)` + `With` + fresh `Default` — no shared owned instance) on `User` + `UpdateNotificationPreferences`; `Device.EndpointHash` = BCL SHA-256 hex → the upsert now `(UserId, EndpointHash)`-keyed (**race-safe**, `DbUpdateException`-reconciled; replaces 6.2's in-memory match). **Enforcement (§5.2) in ONE place, push-only:** `SendPushNotificationCommandHandler` reads `IsPushEnabled(type)` (one `AsNoTracking` projection) **before** loading devices → muted returns early; **the 4 producing handlers + `RealtimeNotificationDispatcher` are UNCHANGED → the in-app inbox row + SignalR toast stay unconditional** (proven). `GetNotificationSettingsQuery` + `UpdateNotificationPreferencesCommand` (owner-scoped, `ICurrentUser`, no id). **Surface:** `/settings/notifications` (4 per-type toggles + "Enable push on this device" subscribe control, HTMX partial + `#settings-status` announce + anti-forgery + `SocialWrite`) — replaces the Phase-4 placeholder + the 6.2 EditProfile control (now a signpost link). **Folded the 2 6.2-gate Lows:** `MaximumLength(2048/256)` push validators (500→400); per-device send-loop resilience + broadened `WebPushSender` catch (a bad device no longer aborts the batch or skips the `Gone`-prune). **Four review gates, all 0 Crit/0 High:** architecture ✅ APPROVE (owned-VO mapping sound in all `User`-mapping contexts, `Default` fresh-per-access, migration strictly additive, gate one-place push-only, in-app invariant intact) / code ✅ SHIP (gate maps each type correctly, muted-not-pushed-but-inbox-persists proven, hash-keyed upsert dedups+reconciles, folded Lows closed, `HasDefaultValue` INSERT-omit-trap verified safe) / UI **🔧→✅** (found **1 HIGH** — checked-row hint 3.76:1 fails WCAG 1.4.3 → **fixed** `ink-muted` 9.3:1; + M1 visible push-outcome + blocked-guidance, M2 OFF-track ring `white/10`→`control-border`, Lows). **Deferred → REVIEW_BACKLOG:** the 3-reads→1-projection perf nit (6.4), the reconcile-branch concurrency test, app-wide `htmx:sendError` offline-write affordance, the `EndpointHash` empty-table runbook line (6.6). **⚠️ Manual push/settings browser smoke carried.**
- **6.2 Web Push — VAPID + `IPushSender`/`IPushDispatch` ports + `Device` upsert + SW push handlers + subscribe UX — ✅ COMPLETE (2026-07-06)** — orchestrator-verified build **0/0**, **npm ✅**, **428/428** (Application 98→116, Infrastructure 93→110, Web.Integration 151→156; +37), **CSP byte-unchanged**, **shipped graph vuln-clean**. **No migration** (in-memory `Device` upsert; `EndpointHash`+index is 6.3). **New package:** `WebPush` **1.0.13** (MIT, Infrastructure-only) + transitive `Portable.BouncyCastle` 1.9.0 (MIT) — audit-clean, no Newtonsoft/override. **Ports (Application, no vendor leak):** `IPushSender.SendAsync(PushSubscription, PushPayload, ct)→PushSendResult{Delivered|Gone|TransientFailure}` + `IPushDispatch.Enqueue(notificationId)` over Application-owned records. **Adapters (Infrastructure, singleton — arch-confirmed safe/preferable, no captive dep):** `WebPushSender` (RFC-8291/8292 via the lib, VAPID from `WebPushOptions`, 404/410→`Gone`, **degrade-if-absent** — no keys ⇒ push off, never crashes boot; disposes its client) + `InlinePushDispatch` (fire-and-forget on a background `Task` in its **own** `IServiceScopeFactory` scope, 30s bounded timeout, catch-all-logs, ADR-0008 orchestrator — never `ISender` in a handler). **The ADR-0011 seam extended ONCE:** `RealtimeNotificationDispatcher` gained an `IPushDispatch` param, enqueues push **after** the SignalR try/catch (so a closed tab — exactly when push matters — still fires), threaded through the 4 producing handlers (like/comment/friend-req/friend-accept) — not duplicated. **`SendPushNotificationCommand`** reloads via `NotificationProjection` → builds payload (`NotificationDeepLink.Resolve` — the inbox↔push DRY consolidation, byte-identical grammar) → sends per device → **prunes `Gone` devices**. `RegisterDeviceCommand`/`PushController` (`[Authorize]`+`SocialWrite`+anti-forgery; `POST /push/subscribe` the first `[FromBody]` JSON; server-resolved actor, no IDOR) + `Device.UpdateSubscription` key-rotation. SW `push`/`notificationclick` handlers (plain-text via `showNotification` options, focus-or-open same-origin `data.url`) + progressive-enhancement subscribe UX in `/settings`. **Three review gates, all 0 Crit/0 High/0 Med:** architecture ✅ APPROVED (port boundary, seam reuse, **singleton lifetime explicitly confirmed safe+preferable**, no drift) / security ✅ (**VAPID private key user-secrets-only, never logged/committed**; `[FromBody]` anti-forgery NOT bypassed — authed-no-token→400 proven; no IDOR; push body plain-text; CSP unchanged; RFC-8291 = display name E2E-encrypted) / code ✅ SHIP (fire-and-forget safe, upsert correct, deep-link parity). **6.2-gate Lows → fold into 6.3** (`MaximumLength` push validators 500→400; per-device send-loop resilience + broadened `WebPushSender` catch). **Deferred → REVIEW_BACKLOG:** per-user device cap (self-mitigating), `RecipientUserId` in `NotificationProjection` (drop a 2nd read), `HangfirePushDispatch` durable swap (registration-only). **NO preference gate** (6.3). **⚠️ Real-browser push round-trip = carried MANUAL smoke** (needs VAPID keys in user-secrets + a browser). Mock-first (faked `IPushSender`).
- **6.1 PWA foundation — manifest + service worker + `/offline` + versioned update — ✅ COMPLETE (2026-07-06)** — orchestrator-verified build **0/0**, **npm build ✅**, **388/388** (Web.IntegrationTests 143→151: +8 `PwaTests` incl. the `/account/login` no-store scope lock), **CSP byte-unchanged** (ADR 0019, design §2). **No migration.** **Manifest** (`wwwroot/manifest.webmanifest`, `application/manifest+json`, `id`/`start_url`/`scope` `/`, `display:standalone`, `theme/background_color #090b0f` = `--color-surface-0`) + **icons** (192/512/maskable PNG, gold "C" on `#090b0f`, maskable mark provably inside the 80% safe zone; dependency-free `zlib` generator). **Service worker** `Scripts/sw.ts` → **`wwwroot/sw.js`** (root scope, stable non-hashed name) via a **second `build.mjs` esbuild entry** built AFTER the main bundle, with **`__SW_VERSION__`** (SHA over the hashed-asset manifest **+ icon bytes** → a deploy or icon change auto-rotates the version) + **`__PRECACHE__`** injected as `define`s; lifecycle install-precache/activate-purge/`clients.claim`/consent-gated-`skipWaiting`; **per-resource fetch** (non-GET bypass, cache-first hashed assets, cache-first capped poster LRU, network-first navigations → `/offline`, **never caches a `no-store` response**, `wss` untouched). **Privacy-correct offline scope via `PwaCacheControlMiddleware`:** the anti-forgery token on every page makes the framework stamp `no-cache, no-store` on all HTML → the middleware **relaxes anonymous public GET HTML to `no-cache`** (SW-cacheable for offline) while **authenticated HTML stays `no-store`** (never cache another user's pages on a shared device) and **`/account/*` stays `no-store`** (auth-form pages out of offline scope); ADR 0019 amended to record this. **Versioned-update toast** (reuses `#toast-host`, CSP-safe `@alpinejs/csp`, consent-gated `SKIP_WAITING` → single `controllerchange` reload). SW **registered only outside Development** (a `cinora-sw` meta flag; Dev unregisters stale SWs — fixes hot-reload staleness). **Four review gates, all 0 Crit/0 High:** security ✅ (the cache-policy deviation **fails closed, leaks no user data** — per-browser SW cache, revalidation, network-first; offline-privacy exit-criterion holds; found the `/account/*` scope Low → fixed) / architecture ✅ (build.mjs cache-keying sound + auto-invalidating; CSP-neutral; dependency-rule clean; found the dev-`--watch` SW-staleness Med → fixed) / UI ✅ (measured contrast ≥4.5:1, maskable safe-zone; found the update-toast-eviction + `.toast-link` target-size Meds → fixed) / code **🔧→✅** (found **1 HIGH** — `clients.claim()` + unconditional `controllerchange` reload → **every first visit reloaded unprompted**, violating "never hijack an in-use page"; **fixed** with a consent-gated `updateAccepted` flag so only a user-clicked Refresh reloads; + await-`cache.put`, nits). **Deferred → REVIEW_BACKLOG:** atomic-precache-vs-`allSettled` for icons (accepted), a forward-looking "future authenticated navigable non-HTML must self-set `no-store`" note, `/dist/*` immutable-caching + compression (→ 6.4 perf). **⚠️ Manual browser smoke (needs a Release run — carried):** installability/Lighthouse, SW scope `/`, real offline (visited `/discover` from cache, uncached → `/offline`, authenticated `/home` → `/offline`), version-bump → toast → **single** reload (first visit = NO reload), maskable icon under a circle mask, iOS add-to-home apple-metas.

### Phase 5 — AI Recommendations  ✅ COMPLETE — EXIT GATE PASS (2026-07-06)
- **Phase 5 EXIT GATE — PASS (2026-07-06).** Holistic whole-phase review (5.0 engine port → 5.4 For-You UI), build **0/0**, **380/380**. Five dimensions, all **0 Crit / 0 High** (run as parallel read-only agents): **architecture** (all 7 whole-phase invariants — dependency-scan-confirmed zero AI/Hangfire/vendor types in Application/Domain; CQRS command-sole-writer / query-engine-free / job-as-orchestrator; schema-stable — no Phase-5 migration, `AIRecommendationHistory` reused; free-only intact — AI packages Infra-only, Hangfire Infra+Web, Newtonsoft 13.0.3 override; seams coherent end-to-end) + **security** (**CSP byte-unchanged**; dashboard fail-closed; **prompt has zero user free-text → no injection vector**; untrusted-`Reason` encode-on-render chain; owner-scoped serve no-IDOR; anti-forgery+rate-limit on dismiss; **`dotnet list --vulnerable`: shipped graph clean**, NU1903 cleared) + **performance** (zero-LLM-on-request a **compile-time impossibility** — engine reachable only from the off-request writer; never-500 fallback; serve = cache→indexed-TOP(1)→bounded-cached-fan-out; precompute set-based+lookback-bounded; async-clean) + **code** (every exit-criterion traced to a **falsifying test** — guard/one-row+tokens/CallCount==0/never-500/idempotent-precompute/`<script>`-encoded-reason/dashboard-401; full suite 380 green) + **UI** (all 5.4 in-gate fixes cohere; heading order, focus-rehome, reduced-motion, AA contrast, target sizes, honest labeling/empty states). **Exit criteria MET:** precompute-then-serve (no LLM on page load), every run audited with token usage + hallucinated titles filtered, fallback verified with the AI client disabled (no 500), CSP unchanged, `/jobs` admin-gated, 0 Crit/High across all gates. **The ONE exit criterion not verifiable here = the live-model smoke (5.5) — DEFERRED** (no Ollama; mock-first covers the rest). **Deferred → REVIEW_BACKLOG (all Low/doc):** the skip-unchanged-is-timestamp-not-taste-hash **doc reconciliation** (ADR 0018 amendment + design §3/§6/§10/§13 — the arch Medium, doc-only) + 2 stale "taste hash" code comments; the history-aggregate lookback micro-opt (perf F1); the served-cache `Enum.IsDefined` hardening (pre-req for a future Redis swap); UI CLS/dismiss-all polish (L1–L3); pre-Phase-6 instrumented Lighthouse/axe pass. **⚠️ 5.5 live-Ollama smoke = the lone deferred milestone.**
- **5.4 For-You rail on `/home` + `/recommendations` page + dismiss — ✅ COMPLETE (2026-07-06)** — orchestrator-verified build **0/0**, **npm build ✅**, **380/380** (Web.IntegrationTests 137→143: +6 `RecommendationsUiTests`), **CSP byte-unchanged**. **The For-You surfaces (design §11):** `RecommendationsController` `[Authorize]` — `GET /recommendations` (page), `GET /recommendations/rail` (the `/home` lazy `hx-trigger="load"` rail partial), `POST /recommendations/{tmdbId:int}/dismiss` (`SocialWrite` + anti-forgery, **empty-200, persists nothing** — the durable-feedback seam); **every read dispatches only the LLM-free `GetMyRecommendationsQuery`** (no `IRecommendationEngine` dependency anywhere in the serve graph — the "page load makes no LLM call" exit criterion, proven by `engine.CallCount==0` in all 6 tests). Reuses the gate-approved `_TitleCard`/`_WatchlistControl`/rail-lazy-load/`site.ts`-a11y grammar → **no CSP widening, no new anti-forgery plumbing** (the global `RequestVerificationToken` header handler covers dismiss). `_RecommendationCard` = poster + "why this" caption (**`@Model.Reason` default Razor-encoded — untrusted model text, never `Html.Raw`**) + `_WatchlistControl` overlay (`Status:null` by construction, N+1-free) + pure-HTMX dismiss. Honest **`Source` labeling** ("For You" AI / "Popular picks" heuristic) + honest empty states (never an error/500). **Four review gates, all 0 Crit/0 High:** UI ✅ (primary — contrast all ≥5.85:1 measured, dismiss/announce/focus scoping, controls outside the anchor; found the `/home` rail **CLS** Med → fixed: skeleton caption placeholder + clean empty-band collapse) / security ✅ (**no findings at any severity** — XSS-encode contract holds incl. the `data-rec-announce`→`textContent` carrier, owner-scoped no-IDOR serve, anti-forgery+rate-limit on dismiss, CSP byte-unchanged, not an enumeration oracle) / performance ✅ (zero-LLM-on-serve structural; no N+1) / code ✅ (found the **no-JS dead dismiss** Med → `.js-only`; and the **vacuous encoding test** Med → **fixed + empirically proven**: flipping the view to `Html.Raw` makes the new HTML-special-char test fail). **Fixed in-gate:** CLS (skeleton + empty band), `.js-only` dismiss, HTML-special-char encoding test, casing, sr-only `<h2>`, `title` hint, reveal-empty-on-dismiss-all, tightened AI-label test. **Deferred → REVIEW_BACKLOG:** durable "not interested" (`RecommendationFeedback` entity — the dismiss endpoint is its anti-forgery/rate-limited seam), cold-start non-lazy `/recommendations` heuristic latency (measure-first), require-`media`-explicitly on the future durable dismiss write. Mock-first (faked engine over real `CinoraTest`).
- **5.3 Hangfire OSS precompute job + fail-closed admin `/jobs` dashboard — ✅ COMPLETE (2026-07-06)** — orchestrator-verified build **0/0**, **374/374** (Web.IntegrationTests 126→137: +11 — 7 `AdminAccessPolicy` unit + job stale/fresh/idle/idempotent/lookback + dashboard anon→401), **no NU19xx**, **no migration** (reads existing entities; Hangfire auto-creates its own `HangFire` SQL schema). **Redeems ADR 0007's Hangfire deferral** (ADR 0018 §5, design §10). **Packages (CPM):** `Hangfire.Core/AspNetCore/SqlServer` 1.8.17 (**LGPLv3 — free OSS, the CLAUDE.md-approved choice**) + **`Newtonsoft.Json` 13.0.3** pinned **directly in both Infra+Web** to override Hangfire's transitive vulnerable 11.0.1 (**NU1903 cleared**, `dotnet list package --vulnerable` audit-confirmed). **Program.cs wiring (env-aware):** `AddHangfire(UseSqlServerStorage(DefaultConnection), PrepareSchemaIfNecessary=!isTesting)`; `AddHangfireServer()` + `RecurringJob.AddOrUpdate(Cron.Daily(4) UTC)` **gated off under Testing** (no background server polls `CinoraTest`); `MapHangfireDashboard("/jobs").AllowAnonymous()` (the `.AllowAnonymous()` suppresses only the MVC fallback's 302→login so Hangfire's own filter is the sole gate → 401). **`RecommendationPrecomputeJob`** (Infrastructure/Jobs, `[DisableConcurrentExecution(300)]`+`[AutomaticRetry(3)]`): set-based stale-user selection (per-user activity high-water-mark vs latest `AIRecommendationHistory.GeneratedAtUtc`, bounded by a **`PrecomputeLookbackDays=45` window**), **fresh DI scope per user → `ISender.Send(GenerateRecommendationsCommand(userId))`** (ADR 0008 job-as-orchestrator — never `ISender` in a handler; explicit userId, never `ICurrentUser`), per-user try/catch (leaked-OCE-safe), idempotent. **Fail-closed admin gate:** pure testable `AdminAccessPolicy` (anonymous denied; matches the server-assigned **`NameIdentifier` user-id** or email — name-claim arms dropped; Development-only convenience never loosens Testing/Production; **no admins configured ⇒ everyone denied, incl. Production**) behind the thin `AdminDashboardAuthorizationFilter`; `AdminDashboardOptions` (`AdminAccounts`, `ValidateOnStart`). **Four review gates, all 0 Crit/0 High:** security ✅ (primary — allow-list built `OrdinalIgnoreCase`, `.AllowAnonymous()` not a hole, **CSP not widened**, NU1903 audit-clean, job args a bare Guid, PII-free logs; found L1/L2 unverified-email/name-claim escalation → **fixed** to user-id+email matching) / architecture ✅ (Hangfire Infra+Web only, never Application/Domain; job-as-orchestrator; no captive dep; no schema drift) / performance ✅ (set-based selection, no N+1; found the no-lookback M1 → **fixed** with the 45-day window) / code ✅ (stale-detection correct/idempotent; found the empty-cohort-re-dispatch Med → **bounded** by the lookback + honest docstring). **Fixed in-gate:** lookback window, user-id admin matching, `SameSite=Lax`-CSRF comments, lock-semantics comment, leaked-OCE catch, de-confounded freshUser test. **Deferred → REVIEW_BACKLOG:** the **test-only** `SQLitePCLRaw` High in `Application.Tests` (Phase-2 SQLite test dep, never shipped — build hygiene), a durable taste-hash for persistently-empty re-attempts (needs schema), an `INCLUDE(UpdatedAtUtc)` covering index, event-driven refresh (nightly-only ships). **⚠️ Live precompute run vs a real model = 5.5 (deferred — no Ollama).** Mock-first: faked engine/TMDB, job driven via direct `RunAsync` over real `CinoraTest`.
- **5.2 History persistence + served cache + LLM-free 3-tier serve + never-500 heuristic — ✅ COMPLETE (2026-07-06)** — orchestrator-verified build **0/0**, **363/363** (Application 72→98: +26; Web.IntegrationTests 125→126: +1; no migration — the existing `IX_AIRecommendationHistories_UserId_GeneratedAtUtc` serves it). **The write/read split becomes real** (ADR 0018, design §2/§6/§7/§9). **Write path** — `GenerateRecommendationsCommand` (the sole writer) now persists **exactly one** `AIRecommendationHistory` row on a non-empty AI set (model + **`AiUsage` token counts**, PII-free `InputSummary`, a compact self-contained `RecommendationSetEnvelope` `OutputSummary` bounded to the 4000-char column so the fallback renders without a TMDB call) then warms the cache; every empty outcome (thin/no-candidate/engine-outage/zero-grounded) writes **nothing**. `RankedPick.Reason` persisted **verbatim** (untrusted; encoding is 5.4's render concern). **Cache** — `IServedRecommendationCache` port + `ServedRecommendationCache` `IDistributedCache` adapter (key `cinora:ai:recs:{userId}`, TTL `ServedCacheTtlHours=24` + ±10% jitter, **graceful-degrade — never throws**, all mechanics in the adapter per the `CachedTmdbClient` precedent). **Serve path** — `GetMyRecommendationsQuery` (owner-scoped via `ICurrentUser`, **no `IRecommendationEngine` dependency — zero LLM on serve by construction**, never writes): tier (a) cache → (b) latest history row (backward-index-seek `TOP(1)` `AsNoTracking` projection, corrupt envelope falls through) → (c) the deterministic **heuristic** (`IHeuristicRecommender` — origin-tagged fan-out reusing the taste profile, honest per-tier reasons, excludes seen, caps to `MaxResults`, non-empty for a brand-new user via global-popular, **never 500s**). **Three review gates, all 0 Crit/0 High:** architecture ✅ (CQRS purity — sole writer / read-only query, port placement, no captive dep; **found 1 Med** — the tier-c heuristic's TMDB fan-out was unguarded → a cold-start user + TMDB outage could 500, violating the never-500 contract; **fixed in-gate** at the source: per-tier `SafeFetchAsync` guard → partial/empty `Heuristic` set + `HeuristicTmdbFailed` log, locked with throwing-`ITmdbClient` tests) / performance ✅ (zero-LLM-on-serve verified structurally; serve = one cache read → one indexed `TOP(1)` → bounded cached fan-out; no N+1) / code ✅ (persist-exactly-one + token ledger + empty-no-write, envelope lossless round-trip + 4000-char budget guard, 3-tier order + IDOR-safe owner-scoping, non-vacuous tests; **fixed in-gate** L2 envelope `Enum.IsDefined` media check + L1 `GenerateRecommendationsCommandValidator`). **Deferred → REVIEW_BACKLOG:** tier-c recompute cost (driven down by 5.3 coverage), the audit-row clock seam (L3), two `RecommendationSet` wire formats (cache vs envelope — cross-ref comment), the O(n²) budget loop (accepted). **Skip-unchanged/taste-freshness is a 5.3 handoff.** Mock-first (faked `IRecommendationEngine`+`ITmdbClient` over real LocalDB `CinoraTest`).
- **5.1 Grounded candidate generation + rank + hallucination guard — ✅ COMPLETE (2026-07-06)** — orchestrator-verified build **0/0**, **336/336** (Application 62→72: +10 unit tests; Infrastructure.Tests 90→93: +3 TMDB URL-capture tests; Web.IntegrationTests 124→125: +1 end-to-end grounding test). **The correctness core** (ADR 0017): Cinora generates the candidate universe from real TMDB titles grounded in the user's own taste; the LLM only ranks + explains; the handler enforces the authoritative guard. **`ITmdbClient` extension:** `GetRecommendationsAsync` + `DiscoverByGenreAsync` (port + `TmdbClient` adapter + `CachedTmdbClient` decorator — new keys `cinora:tmdb:recs:*`/`cinora:tmdb:discover:*`, TTL factors 144/72 ≈ 12h/6h, discover key sorts ids, never caches empty, degrades). **Application seams (both in Application, DI-registered explicitly):** `IUserTasteProfileBuilder` → `UserTasteResult` composite (`TasteProfile` prompt shape + `SeedKeys` + `FavoriteGenreTmdbIds` + `SeenExclusions` + `HasSignal` cold-start flag; ~6 bounded `AsNoTracking` reads, N+1-free, server-side `Take`/`ORDER BY`) + `IRecommendationCandidateSource` (recs+discover fan-out → dedup by `(TmdbId,Media)` → exclude-seen → cap to `MaxCandidates`; genre id→name one cached call per media; returns `CandidateTitle` only — no TMDB DTO leak). **`GenerateRecommendationsCommand` + handler:** thin-profile/empty-candidate/engine-outage/zero-grounded all degrade to an empty set (no rethrow, no persistence — 5.2 wires those); **the authoritative hallucination guard** — keep only picks whose `(TmdbId,Media)` ∈ candidate set, drop off-list (counted+logged), dedup, cap to `MaxResults`, card metadata from the **candidate** (only `Reason` from the model, surrogate-safe trimmed to `MaxReasonLength`). **Caps via the `IUploadPolicy` precedent:** new `RecommendationOptions` (Infra, `[Range]`-validated, `ValidateOnStart`) surfaced to Application through the `IRecommendationPolicy` port (no Application→Options coupling). `CandidateTitle.PosterPath` added (required by ADR 0017 §3 — card metadata from the candidate). **Three review gates:** architecture ✅ (0C/0H/0M — vendor-leak sweep clean, `IRecommendationPolicy` a faithful `IUploadPolicy` mirror, no captive dep, CQRS pure, refinements uphold ADR 0017) / performance ✅ (0C/0H — 6 bounded reads, no N+1, capped+cached ~13-call TMDB fan-out, O(n) guard) / code **🔧→✅** (found **1 High H-1**: `DiscoverByGenreAsync` used comma=AND `with_genres` vs the "any of"=OR contract → gutted genre fan-out; **fixed** to `|` (OR) + **locked** with 3 URL-capture tests the mock suite couldn't otherwise catch; + fixed L-1 favorite-genre tiebreaker for 5.2 hash reproducibility, arch-L1 doc comment). **RESOLVED** the 5.0-carried surrogate-safe reason cap (5.1 `Truncate` walks back a lone high surrogate). **Deferred → REVIEW_BACKLOG:** perf M1 (favorite-genre seen-id round-trip, measure-first); media-agnostic genre ids (self-heals with the OR fix; deeper = per-media `Genre.MediaType`); design-doc drift note (`CandidateTitle.PosterPath`/`UserTasteResult`); the command-returns-value idiom (revisit at 5.2). **No persistence/UI/LLM-call in 5.1** (mock-first: faked `IRecommendationEngine` + `ITmdbClient` over real LocalDB `CinoraTest`).
- **5.0 `IRecommendationEngine` port + Ollama adapter + AI package pin — ✅ COMPLETE (2026-07-06)** — orchestrator-verified build **0/0**, **322/322** (Infrastructure.Tests 65→90: +25 AI tests — 15 `OllamaRecommendationEngineTests` + 10 `AiOptionsValidationTests`). **Package gate PASSED** (the design-flagged risk): `Microsoft.Extensions.AI` **10.5.0** + `Microsoft.Extensions.AI.OpenAI` **10.5.0** + `OpenAI` **2.10.0** — all **MIT**, native **net10.0**, resolved from cache, pinned in `Directory.Packages.props`, referenced **Infrastructure-only**; the OpenAI client points at the local Ollama `/v1` (`http://localhost:11434/v1`, dummy `"ollama"` key, **never `api.openai.com`**) — the ADR-0016 provider-agnostic path (a Gemini/Groq swap is registration-only). **Application:** `IRecommendationEngine` port (`RankAsync(RecommendationRequest, ct)→RecommendationEngineResult`) + the `Common/Ai` read-models (`RecommendationRequest`/`TasteProfile`/`RatedTitle`/`CandidateTitle`/`RankedPick`/`AiUsage`/`RecommendationEngineResult`) + `RecommendationEngineException` — **Cinora/Domain types only** (only `MediaType` from Domain; no vendor type leaks). **Infrastructure:** `OllamaRecommendationEngine` (PII-free prompt, `Temperature=0.3`/JSON/`MaxOutputTokens`, linked-CTS per-request timeout, tolerant parse with code-fence/prose extraction, off-list-drop + `(TmdbId,Media)` dedup, `response.Usage` token capture) + `AiLog` (source-gen, **metrics-only** — model/counts/tokens/response-length, never prompt/response body) + `AddAi` DI (`IChatClient` from `AiOptions`, `AddScoped<IRecommendationEngine>`) + `AiOptions.ValidateOnStart()` flipped **ON**. Mock-first (hand-rolled `FakeChatClient`, no NSubstitute). **Three review gates, all 0 Crit/0 High:** architecture ✅ (dependency rule clean — AI packages Infra-only; provider-agnostic; DI no captive dep; **ratified the malformed→degrade deviation** → ADR 0016 amended 2026-07-06) / security ✅ (**prompt PII-free** — titles/genres/ratings + candidate TmdbId/title/year/genres only, no id/name/email; logs metrics-only; no secret committed; injection mitigated by id-keyed candidate mapping) / code ✅ (**timeout-vs-cancellation discriminator correct** — linked CTS, caller's original token as discriminator, both directions tested; degrade-no-throw parse paths, 6 non-vacuous cases; null-safe usage; no captive dep). **Deviation ratified:** malformed/empty model output degrades to an empty pick set (→ no history row → heuristic fallback), NOT a throw; `RecommendationEngineException` reserved for transport/timeout (ADR 0016 amendment). **Deferred → REVIEW_BACKLOG:** surrogate-safe reason truncation (5.1's authoritative ~140-char cap owns it); the "not valid JSON" log fires for a valid-but-empty `{"picks":[]}` (reword); `IChatClient` singleton-vs-scoped connection reuse (perf, confirm at 5.3); an optional `AiOptions.ApiKey` (from user-secrets) if a credentialed cloud provider is ever swapped in; treat `RankedPick.Reason` as untrusted model output at 5.2 persist / 5.4 render (default Razor encoding, never `Html.Raw`). **⚠️ Live-model smoke deferred to 5.5** (needs a running Ollama — not available here; 5.0–5.4 are fully mock-first).

### Phase 4 — Watchlists & Profile  ✅ COMPLETE — exit gate PASS (2026-07-06)
- **Phase 4 exit gate — PASS (2026-07-06).** Holistic whole-phase review (4.1 avatars → 4.5 highlights), build **0/0**, **297/297**. Five dimensions, all **0 Crit / 0 High** (run as parallel read-only agents; the UI dimension run by the orchestrator inline after the review agent repeatedly stalled): **architecture** (dependency rule compiler-clean; ports speak only primitives/Stream/domain enums, `IFormFile` unwrapped in the controller, `IAvatarContentSource` carve-out correctly off the Application port; CQRS pure — repo-wide sweep: `ISender` never inside a handler; **one coherent privacy model** across 3.3/4.4/4.5; index-only schema, no drift; free-only intact — banned packages only in absence-confirming comments; DRY cross-milestone seams) + **security** (the 4.5 private-highlights-never-fetched check passes — a private owner's Watched list never reaches the wire; whole upload/serving chain sound — magic-byte/streamed-cap/outside-wwwroot/404-on-token-fail/constant-time-HMAC/no-key-leak; all user-scoped queries filter by the server id before any cursor — no IDOR; anti-forgery on every write; **CSP byte-unchanged across the entire phase**; fail-closed authz; no `Html.Raw`/XSS) + **performance** (4.5 highlights = backward ordered index scan + TOP(8), no sort; keyset never OFFSET; counts one GROUP BY; batch map N+1-free/anonymous-zero-DB; avatar delivery cacheable — strong ETag + bucketed URL + async; **`_Avatar` seam N+1-free by construction**; zero sync-over-async) + **code** (4.5 privacy gate + query correct, PH1–PH3 non-vacuous, 3.3 FriendProfile regression green, cross-milestone coherent) + **UI** (4.5 strip reuses gate-approved rail/card styling, accessible/quiet-omit; whole-phase UI consistent). **One advisory Medium (architecture) closed by a doc amendment:** the design doc §6.1 promised a `RecentlyUpdated` sort that 4.4 deferred — recorded a dated deferral note in `phase-4-...design.md` §6.1 + REVIEW_BACKLOG. **Deferred Lows → REVIEW_BACKLOG:** bucketed-URL staleness window (ADR 0013); watchlist-GET rate-limit (accepted); avatar ETag full-read (Phase 6); optional `INCLUDE(MovieId)` covering index; `/watchlist` + `/users/{id}` Core Web Vitals (Phase 6). **Outstanding (manual/env):** the carried browser smokes (4.3 menu, 4.4 live re-sync, 2.7 TMDB, 3.5 realtime) + `CinoraDev`/prod migration apply (Express/localhost down).
- **4.4 Watchlist page (`/watchlist`) + 2 perf indexes — ✅ COMPLETE (2026-07-05)** — orchestrator-verified build **0/0**, **294/294** (Application 57→62: +5 handler unit tests; Web.IntegrationTests 117→121: +4 `WatchlistPageTests` WP1/WP2/WP4/WP5). **Backend:** `GetMyWatchlistQuery` (owner-scoped via `ICurrentUser`, optional status filter, **keyset `(AddedAtUtc DESC, Id DESC)` `Take+1`/HasMore + opaque cursor** — mirrors the 3.4 feed; one `AsNoTracking` query joining `Movies` by PK, N+1-free) + `GetWatchlistCountsQuery` (one `GROUP BY Status`); `WatchlistController` `[Authorize]` `Index` (counts + page-1 inline) + `Page` (keyset load-more partial, lenient status/sort parse, graceful **200 `_WatchlistError`** on failure). **Sort shipped `RecentlyAdded` only** — `RecentlyUpdated` deferred (would force a per-user scan without a `(UserId, UpdatedAtUtc)` index; design §6.1 + test WP3 → backlog). **DB (index-only, no entity/column):** migration `WatchlistPageIndexes` → `IX_Watchlists_UserId_AddedAtUtc` + `IX_Watchlists_UserId_Status_AddedAtUtc` (both keyset-serving, no residual sort — perf-gate-confirmed); applied to `CinoraTest`, `artifacts/migrate.sql` regenerated; **`CinoraDev` carried** (localhost SQL down). **Frontend:** premium poster grid (mirrors `_TitleCard` — cross-folder partials can't resolve, so mirrored not reused; L2 dup → backlog), filter chips w/ counts + `aria-current`, `.select-field` sort, keyset load-more sentinel, **three empty states** (none / empty-filter / JS-revealed live-empty), and the `--- Watchlist page a11y ---` `site.ts` block (load-more announce/focus + **live removal + live status-change re-sync**). **Four review gates, all 0 Crit/0 High:** security ✅ (owner-scoping/IDOR — `UserId==me` applied before/independent of the cursor so a forged cursor can't cross users; CSP byte-unchanged; error partial leaks nothing) / **performance ✅ (KEY: keyset no-OFFSET + `datetime2(7)`/`"o"` round-trip lossless, both indexes serve their keyset with no sort, counts one GROUP BY, grid N+1-free, live-removal O(1))** / code **🔧→✅** (found **1 High**: an in-place status *change* left the chip counts / filtered membership / card `data-watchlist-status` stale → **fixed** with a page-scoped `handleWatchlistStatusChange` that re-buckets old−1/new+1 counts, rewrites the card attribute, drops a filtered-out card via a shared helper, and coordinates with the 4.3 handler — 4.3 owns the single announce, 4.4 owns focus; + **L1** terminal-marker-beside-empty-state fixed via `revealWatchlistEmptyState`; re-reviewed clean) / UI **✅** (orchestrator-run inline after the review agent failed repeatedly — card = gate-approved `_TitleCard` mirror, `_WatchlistError` = `_FeedError` mirror, `.select-field` + chips meet contrast, `aria-current`/keyboard, three empty states, responsive 2→5 cols, reduced-motion). **Deferred → REVIEW_BACKLOG:** `RecentlyUpdated` sort (needs an index) + WP3; the `_WatchlistCard`↔`_TitleCard` ~12-line mirror; lazy first-row poster (LCP); unmaintained counts scan (YAGNI). **⚠️ Manual browser smoke pending** (status-change count re-bucket, filtered-out removal, live-empty, load-more retry — no browser in the agent env).
- **4.3 Watchlist status control on every title surface — ✅ COMPLETE (2026-07-03)** — orchestrator-verified build **0/0**, **285/285** (no test delta in Step B — the Step-A `WatchlistTests` W1–W6 pass unmodified against the elevated markup). **Step A (backend slice):** `SetWatchlistStatusCommand`(upsert+race-reconcile)/`RemoveFromWatchlistCommand`(idempotent)/`GetWatchlistStatusQuery`/`GetWatchlistStatusMapQuery`(the one-query N+1-killer batch map); `WatchlistController` `[Authorize]`+`SocialWrite` reusing the ADR-0008 `EnsureTitleCached`→command orchestration (`POST`/`DELETE /watchlist`, `GET /watchlist/control` fallback); `_WatchlistControl`+`WatchlistControlVm`; the batch map composed into rail/search via `RailViewModel`/`SearchResultsViewModel` (rail/search queries kept `ITmdbClient`-pure); Details stub replaced; Watched→review nudge; `#watchlist-status` region in `_Layout`. **Step B (frontend elevation):** premium `@alpinejs/csp` name-only `watchlistMenu` component (`Scripts/components/watchlistMenu.ts`) layered over the native `<details>`/`<form>` no-JS baseline; **portaled-to-`document.body`, clip-safe `position:fixed`** menu — the load-bearing fix (a naive popover is clipped by the rail's `overflow-x:auto`, and on Details by the title panel's `backdrop-filter`+filled-`animate-rise-in` `transform`, both of which establish a fixed containing block; portaling makes the viewport the CB on every surface, un-portaled back into the control before every HTMX swap so it is never orphaned); the `--- Watchlist control a11y ---` `site.ts` block (announce/focus-restore/inline `#watchlist-error`, scoped to `/watchlist`, plus a guard so a watchlist swap inside `#search-results` doesn't misfire the search handlers); elevated Watched→review nudge + anonymous "sign in to save"; a `.js`-stamp so the JS-only Remove isn't a dead no-JS button. **CSP byte-unchanged** (bare-name Alpine, CSSOM style setters, no eval/`hx-on`). **Four review gates ran in parallel, all closed 0 Crit/0 High:** security ✅ (CSP/anti-forgery/XSS/IDOR); **performance ✅ (KEY gate — batch map = one authed-only query/page, anonymous zero DB, no per-card fetch, listeners per-open + leak-free, ~5 KB deferred bundle delta, no CLS)**; code ✅ (+ a dedicated re-review of the portal lifecycle: no orphan/leak on any swap path, per-open listeners, anti-forgery form travel intact); **UI 🔧→✅ — found + fixed 1 Critical (C1: the Details `position:fixed` menu re-trapped by the title panel's containing block → clipped; fixed via the portal) + folded Lows (no-JS dead Remove hidden, Tab/scroll focus-drop→trigger, `{passive:true}` listeners, `menuitemradio`+`aria-checked`, touch-target sizing) — re-reviewed clean.** **Deferred → REVIEW_BACKLOG:** `data-wl-*` marker naming; the `<form>` between `role=menu` and its items (structural, monitor); optional `aria-owns` trigger↔portaled-menu; a pre-emptive `isWatchlistControlSwap` guard on the feed/review/notification handlers before any control nests there; response-compression/output-cache (→ Phase 6). **⚠️ Manual browser smoke pending** (Details desktop+360px + rail/search + keyboard/SR — no browser in the agent env; logic/spec-verified only). **No migration** (the existing `(UserId,MovieId)` unique index serves it).
- **4.2 Profile edit + avatar upload + settings + app-wide `_Avatar` seam — ✅ COMPLETE (2026-07-03)** — orchestrator-verified build **0/0**, **275/275** (Application 31→52: +21 handler/validator unit tests; Web.IntegrationTests 99→112: +6 `ProfileSettingsTests` P1–P6, +4 `AvatarProjectionTests`, +3 avatar-projection regressions from hardening). Built in three steps + a hardening pass. **Step A (backend slice):** `UpdateProfileCommand`/`ChangeAvatarCommand`(→`ChangeAvatarResult`)/`RemoveAvatarCommand` (+FluentValidation validators via the **`IUploadPolicy`** port — friendly 400 before the stream is read; domain `User.Rename`/`SetAvatar`/`SetProfileVisibility` stay authoritative), `GetMyProfileSettingsQuery`; **`SettingsController` `[Authorize]`** (`GET/POST /settings/profile`, `POST/DELETE /settings/profile/avatar` — multipart, `SocialWrite` + `RequestSizeLimit`, server-resolved actor via `ICurrentUser`, **no bound id**, no 403 arm); the **`ChangeAvatarCommand` carries a `Stream`** (unwrapped from `IFormFile` in the controller — `IFormFile` never crosses into Application) and the handler orchestrates the **§2.5 save→`SetAvatar`→best-effort-delete-old** ordering (step-2 failure → best-effort delete the just-saved key + rethrow; cleanup failures `LogWarning` only, never fail the request) — unit-proven with a fake `IFileStorage`. **Step B (avatars app-wide):** the read-model projections already carried `AvatarFileKey` (only `NotificationVm`/`NotificationProjection` needed the actor sub-query extended — one query, no N+1); the shipped **`_Avatar` seam** (`Views/Shared/_Avatar.cshtml`, `AvatarViewModel(DisplayName, AvatarFileKey?, AvatarSize, Decorative)`) wired into 8 surfaces (nav via a new `CurrentUserAvatarViewComponent`, profile header, review/comment/feed/friend/pending/notification cards), retiring the Phase-3 monogram-only render; the seam calls `IFileStorage.GetUrl` only on a non-null key and catches `ArgumentException`→monogram (never 500s a render, even anonymously). **Step C (frontend/ux):** luxury glass elevation of the settings screen, the multipart avatar control with client-side size/type hints (server authoritative), the **inline `#settings-error` (`role="alert"`) surface** closing the HTMX-won't-swap-a-400 gap (scoped `site.ts` `htmx:responseError` — never raw ProblemDetails), region-scoped focus-restore + polite announce, a premium radio-card privacy toggle, the inert "Notification preferences (Phase 6)" stub, owner "Edit profile" link; also fixed a latent a11y bug (standalone monogram was silent to SRs) + added the `Decorative` flag (defaults to labelled/safe). **Review gates (all 0 Crit/0 High):** **security** (key gate — `[RequestSizeLimit]` caps before buffering, magic-byte gate authoritative through the UI path, no mass-assignment, anti-forgery intact on all 3 writes, **CSP byte-unchanged**/no inline handlers, seam degrades safely, `Stream` never logged) / **UI** (all contrast pairs ≥4.5:1 measured against the composited glass, decorative-vs-labelled `alt` correct on all 8 surfaces, radio-card keyboard/SR, inline alert announces, reduced-motion) / **code** (§2.5 ordering, layering, no N+1, validators mirror the domain guard, non-vacuous tests). **Post-review hardening applied + re-verified:** compensating avatar deletes now pass `CancellationToken.None` (orphan-prevention on cancellation, code Low); a fail-fast **boot guard** `MaxUploadBytes ≤ AvatarRequestSizeLimitBytes` in `Program.cs` (the security+code cross-gate drift consensus); empty-file guard → `ValidationProblem` (RFC-7807, security Low); +3 friend/pending/profile avatar-projection tests (code Low); UI Medium **duplicate radio id** → distinct explicit ids; + aria-describedby wiring, alert unhide-before-textContent (settings + review handlers), rune-safe monogram initial. **Deferred → REVIEW_BACKLOG:** no-JS "Remove photo" form parity, nav-chip desktop SR double-read, eager-loading header avatars (perf), broadening the `_Avatar` catch beyond `ArgumentException` (guarded by boot validation today). **No migration** (`User.AvatarFileKey`/`IsProfilePublic` since Phase 1).
- **4.1 `IFileStorage` foundation + avatar upload/storage/token-serving — ✅ COMPLETE (2026-07-03)** — orchestrator-verified build **0/0**, **241/241** (Infrastructure.Tests 30→65 net across the phase; +6 vs the pre-hardening 235: Infrastructure Storage 30→35, Web.IntegrationTests Media 6→7). **The free-local realization of the brief's "avatar upload to Azure Blob (SAS delivery)"** (ADR 0013, no Azure/Docker/image library). **Application ports:** `IFileStorage` (`SaveAsync(stream, declaredContentType, ct)→server key`, **pure** `GetUrl(key, ttl)→url` so views may call it, idempotent `DeleteAsync`) + `IUploadPolicy` (surfaces `FileStorageOptions` cap/allow-list to the Application validator without Application→Infrastructure Options). **Infrastructure `Storage/`:** `LocalFileStorage` (the authoritative upload gate — cap enforced *while copying*, magic-byte gate, server-minted `avatars/{Guid:N}{ext}` key, `FileMode.CreateNew` never overwrites, partial-file cleanup on failure; writes under `App_Data/uploads` **outside `wwwroot`**; resolves to BOTH `IFileStorage` and the serving `IAvatarContentSource` as one singleton), `ImageContentInspector` (dependency-free JPEG `FF D8 FF`/PNG 8-byte-sig/WebP `RIFF..WEBP` sniff, rejects SVG+everything else), `AvatarStorageKey` (the single key-grammar authority, now `\z`-anchored), `AvatarPaths` (canonical `Path.GetFullPath` containment re-check — path-traversal safe), `AvatarUrlSigner` (HMAC-SHA256 over `{key}|{expiryUnix}`, 24h **bucketed** expiry = deterministic/cacheable, `CryptographicOperations.FixedTimeEquals`, `TimeProvider`-driven), `AvatarServingRoute` (**shared const** = single source of truth for the mint↔serve route), `UploadPolicy`, `UploadStorageInitializer` (IHostedService creates the upload root at boot). **Web:** `MediaController` `[AllowAnonymous]`+`PublicRead` (`GET /uploads/avatar?t={token}` — verify HMAC+expiry+key-regex → **404** on any failure; forced true content-type + `nosniff` + `inline` + `private` cache + strong ETag/304). **Options/DI:** `FileStorageOptions` extended (`UrlSigningKey` secret, `SignedUrlBucketHours`); `.ValidateOnStart()` flipped **ON** with a `PostConfigure` ephemeral-key generator (Dev/Testing) + `FileStorageProductionValidateOptions` (fail-fast on a missing Production key) + `FileStorageRouteValidateOptions` (fail-fast if `PublicBasePath` drifts from the controller route — the post-review Medium fix). **Review gates (all 0 Crit/0 High):** **security** (the key gate — attacked content-type spoof/path-traversal/static-serving/DoS/token-forgery/replay/key-mgmt/CSP/info-leak; polyglot neutralized by forced-type+`nosniff`+`script-src 'self'`; key-A→key-B replay structurally impossible; **CSP byte-identical**, no widening) / **architecture** (port in Application speaks only Stream/string/TimeSpan; `IAvatarContentSource` deliberately off the Application port as local-provider-specific; no captive deps) / **code** (magic-byte guards, streamed cap, HMAC order + fixed-time compare, DI lifetimes all correct). **Post-review hardening applied + re-verified:** the mint↔serve **`PublicBasePath` drift** (Medium, all 3 gates) → shared `AvatarServingRoute` const + fail-fast boot validator; the key-regex **`$`→`\z`** end anchor (Low, 2 gates — `$` matched before a trailing `\n`); **+6 edge tests** (empty-file reject, exact-cap-boundary success, truncated-header reject ×2, 304 conditional-GET, `\z` regression). **Deferred → REVIEW_BACKLOG:** the per-request full-read+SHA-256 ETag cost (performance-agent); ADR-0001 amendment naming the media-serving Web→Infra carve-out + the ADR-0013 mint↔serve-coupling note (documentation-agent); the test-local `KeyPattern` copy still `$` (cosmetic). **No migration** (index-only phase; `User.AvatarFileKey`/`IsProfilePublic` exist since Phase 1).

### Phase 3 — Reviews & Social  ✅ COMPLETE — exit gate PASS (2026-07-03)
- **Phase 3 exit gate — PASS (2026-07-03).** Holistic full-loop review across all five milestones, build **0/0**, **201/201**. Five dimensions, all **0 Crit / 0 High**: **architecture** (dependency rule compiler-clean; `ICurrentUser`/`IRealtimeNotifier` ports in Application, `CurrentUser` in Infrastructure, SignalR adapter+hub in Web per ADR 0011; CQRS pure with handler-enforced ownership→403; notification co-persist coupling sound — domain-events NOT warranted; no MediatR/FluentAssertions/Azure/Redis/Docker; ADRs 0009–0012 realized; chat absent per 0010; index-only schema, no drift) + **security** (the XSS obligation confirmed — `Notification.Message` Razor-encoded AND the SignalR toast uses `textContent`; hub `[Authorize]` fail-closed + group-per-user; recipient-scoped, no IDOR; push can't fail the write; CSP unchanged; WebSocket-hijack mitigated by `SameSite=Lax`+no-CORS) + **performance** (every Phase-3 keyset query matches its index; push off the commit path; inbox N+1-free; thin hub) + **code** (dedup/push wiring, recipient scoping, N1–N6 all correct) + **UI** (notifications inbox/toast/bell — **1 High fixed**: the live badge toggled the wrong `hidden` mechanism → `classList.toggle`; + a11y Mediums). **Fixes applied at the exit:** the UI High + a11y batch (badge/bell aria/afterSwap-announce+focus/toast burst-hardening); the security Low (`NotificationHub` aborts a null-`UserIdentifier` connection instead of joining a shared group); the stale `Program.cs` exception-mapping comment; and the **L7 dedup guard test** (like→unlike→like → exactly one notification). Deferred Medium/Low → REVIEW_BACKLOG (response compression, feed body SQL-truncation, notification-build DRY factory, additive tests). **Outstanding (non-blocking, manual/env):** `CinoraDev` migration apply (Express down), the 2.7 live-TMDB smoke, and the 3.5 two-browser realtime smoke.
- **3.5 Notifications + realtime (self-hosted SignalR) — ✅ COMPLETE (2026-07-03)** — orchestrator-verified build **0/0**, **201/201** (Web.IntegrationTests 85→94: +8 `NotificationTests` N1–N6 + 2 extras, +1 `LikeCommentTests` L7 dedup). **The `Notification` rows were already co-persisted** (3.2/3.3); 3.5 added the **push + inbox + the two carried obligations**. **`IRealtimeNotifier` port** (`Application/Common/Interfaces`) + `NotificationDto` (`Common/Realtime`) + a shared `RealtimeNotificationDispatcher` (DRY **best-effort push** wired into all 4 triggering handlers AFTER their co-persist commit — try/catch→Warning, **never fails the write** — N2). **[OBLIGATION 1] like re-notify dedup** in `LikeReviewCommand` (skip the co-persist when a `ReviewLiked` for `(recipient, actor, review)` already exists — so like→unlike→like doesn't spam; L7 guards it). **[OBLIGATION 2] `Notification.Message` Razor-encoded** in the inbox + **`textContent`** in the toast (no stored-XSS from the actor's raw DisplayName). **`NotificationHub : Hub<INotificationClient>`** `[Authorize]` (fail-closed; aborts a null-`UserIdentifier` connection), group-per-user `user-{UserIdentifier}` + `MapHub("/hubs/notifications")`; **`SignalRRealtimeNotifier`** (Web adapter over `IHubContext`) + `AddSignalR()` + registration in `Program.cs`. **Inbox verticals** (`Features/Notifications/`): `GetNotificationsQuery` (recipient-only keyset), `GetUnreadCountQuery`, `MarkNotificationReadCommand` (recipient-only → non-owned=404, N5), `MarkAllNotificationsReadCommand`; `NotificationsController` [Authorize] (inbox + feed + read/read-all + unread-count). **Client:** `@microsoft/signalr@8.0.17` (MIT) **bundled locally** via esbuild (NO CDN, eval-free) + `withAutomaticReconnect` + on-brand toast + **live unread badge** in `_Layout`. **Stub/deferred (ADR 0011 §4):** FeedHub live-append, Redis/Azure backplane, Web-Push/`Device` (Phase 6). **Migration:** `NotificationInboxIndex` → `IX_Notifications_RecipientUserId_CreatedAtUtc`; `CinoraTest` applied, `CinoraDev` carried. **CSP unchanged** (`connect-src 'self'` covers the same-origin hub WS — N6). Gates: see the exit-gate line above. Tests N1–N6 green (co-persist recipient/actor/target, push-failure-safe, hub-auth fail-closed, recipient keyset, no cross-user mark-read, CSP).
- **3.4 Activity feed — ✅ COMPLETE (2026-07-03)** — orchestrator-verified build **0/0**, **192/192** (Web.IntegrationTests 81→85: +4 `ActivityFeedTests` A1–A4). The authed **`/home` is now the friends feed** (ADR 0012 read-time, reviews-only v1). `GetActivityFeedQuery(FeedCursor?, Take=20)→ActivityFeedVm` (deps `IAppDbContext`+`ICurrentUser`): resolves accepted-friend ids both directions via the new `FriendProjections.AcceptedFriendIdsAsync`, reads `Reviews WHERE UserId IN friendIds AND != me`, **keyset `(CreatedAtUtc DESC, Id DESC)`** `Take+1`/HasMore, **one `AsNoTracking` N+1-free query** joining `Movies` + correlated like/comment/`LikedByMe` subqueries → `FeedItemVm` (author, title+poster, rating, ~200-char excerpt, counts); opaque `(lastUtc,lastId)` cursor. `HasFriends` flag splits the two empty states. `HomeController.Index` (feed shell, page-1 inline) + `GET /home/feed?cursor=` (`_FeedPage`, 2.4 sentinel + graceful `_FeedError` 200). Feed cards **display-only** (no like/comment actions — ADR 0012 §3); author→`/users/{id}`, title→`/discover/title/{media}/{tmdbId}`; w185 lazy posters. **Migration:** `FeedIndex` → `IX_Reviews_UserId_CreatedAtUtc(UserId, CreatedAtUtc)` (perf-confirmed shape, no INCLUDE — clustered `Id` appended → keyset tie-break index-supported); `CinoraTest` applied, `CinoraDev` carried. **Review gates:** performance ✅ (0 Crit/High — keyset no-overlap A2-proven, single round-trip/page, all 5 subqueries index-backed; the index was the one Medium handoff) / code ✅ (0 Crit/High/Med — feed correctness, both-direction friend resolution, layering all verified) / **UI 🔧→✅** — **1 blocking High fixed:** the feed shipped with **no `site.ts` handler** so a `_FeedError` Retry swap dropped focus to `<body>` → added a scoped feed focus/announce handler + `<ul role="list">` semantics + distinct poster label + `#feed-status` announce + polish. Deferred Lows → REVIEW_BACKLOG (SQL-side excerpt truncation; addressee-direction/tie-break/error-path tests; avatar render; friend-predicate DRY). Tests A1–A4 green (friends-only/excludes self+non-friend, keyset no-overlap, auth→302, no-friends empty state).
- **3.3 Friends + profiles — ✅ COMPLETE (2026-07-03)** — orchestrator-verified build **0/0**, **188/188** (Web.IntegrationTests 74→81: +7 `FriendProfileTests` F1–F7). **Verticals** (`Features/Friends/`, `Features/Profiles/`): `SendFriendRequestCommand` (**directed-pair guard §7.2** — loads both directions once → `AlreadyFriends`/`AlreadyRequested`/`ReciprocalPending`/`Created`, never a mirror row; `Friend.Request` rejects self→400; co-persists `FriendRequest` Notification to the addressee), `RespondToFriendRequestCommand` (**addressee-only → 403**; `Accept()`/`Decline()`; accept co-persists `FriendAccepted` Notification to the requester), `RemoveFriendCommand` (idempotent, accepted-only, either direction), `GetFriendsQuery`/`GetPendingRequestsQuery` (both-directions, N+1-free), `GetProfileQuery` (**privacy-shaped §7.3 — privacy enforced at the DATA READ, not the template:** owner/accepted-friend=full incl. reviews+friends; signed-in non-friend + `IsProfilePublic`=true=public(+reviews); =false=**limited (name/avatar only, reviews NEVER fetched)**; reuses `ReviewProjection`). `FriendsController` [Authorize] (`/friends` + request accept/decline/remove) + `ProfilesController` [Authorize] (`GET /users/{id:guid}`, Guid routes); review/comment **author names now link to `/users/{id}`**; "Friends" nav. `social-write` rate-limit on friend writes. **Migration:** `FriendLookupIndexes` (`IX_Friends_AddresseeId_Status` + `IX_Friends_RequesterId_Status`, redundant single-column FK index dropped) — `CinoraTest` applied; `CinoraDev` carried. **Review gates:** security ✅ (0 Crit/High — the privacy-at-read check passes) / code ✅ (0 Crit/High; Medium = the index migration, + a `GetPendingRequestsQuery` subquery-fold Low) / **UI 🔧→✅** — **2 blocking High fixed:** F1 profile "Remove" swapped to empty → `FriendsController.Remove` **branches on `HX-Target`** to re-render `_ProfileFriendControl` (Add-friend) on the profile vs empty-200 in the list; F2 the friends surface had **no** `site.ts` handler so swaps dropped focus to `<body>` → added a scoped friends focus/announce handler + persistent `#friends-status` + CSS-driven empty-states + label/copy/`title` polish. Deferred Lows → REVIEW_BACKLOG (declined-re-request/remove-friend tests; profile review-count label; reciprocal-race §15-accepted; cancel-outgoing-request follow-up). Tests F1–F7 green (request+notify, self→400, reciprocal-no-2nd-row, addressee-only-403 + accept-notifies-requester, private-hides-reviews, anon→302, friends-page render).
- **3.2 Likes + Comments — ✅ COMPLETE (2026-07-03)** — orchestrator-verified build **0/0**, **181/181** (Application 26→31, Web.IntegrationTests 68→74: +6 `LikeCommentTests` L1–L6 + 5 validator cases). **Likes** (`Features/Reviews/`): `LikeReviewCommand`/`UnlikeReviewCommand` → `LikeToggleResult(LikeCount, LikedByMe)`, idempotent via the `(ReviewId, UserId)` composite PK (Like reconciles a `DbUpdateException` race by **re-probe-or-rethrow**; Unlike guards `DbUpdateConcurrencyException`); **self-like allowed but never notifies**. **Comments** (`Features/Comments/`, **FLAT** oldest-first): `AddCommentCommand`(+validator ≤2000)/`DeleteCommentCommand` (author-only delete → **403**), `GetReviewCommentsQuery` (public keyset `(CreatedAtUtc, Id)` ASC, N+1-free), `CommentVm`/`CommentProjection`/`CommentCursor`. **Notification co-persist** in the like/comment handlers' own `SaveChanges` (atomic, row-only — **no push until 3.5**, skipped for self-events; `ReviewLiked`/`CommentAdded` with recipient=author, actor=me, target=ReviewId). **Web:** `ReviewsController` +Like/Unlike (→`_LikeButton`); new `CommentsController` [Authorize] (`POST /reviews/{id}/comments`→`_CommentCard`, `DELETE /comments/{id}`, `[AllowAnonymous] GET /reviews/{id}/comments`→`_CommentList` keyset); `_LikeButton`/`_Comment*`/comment-write-form partials + a lazy comments section on `_ReviewCard`. `social-write` rate-limit on the new writes. **Review gates:** security ✅ (0 Crit/High) / code ✅ (0 Crit/High; **1 Medium fixed** — Like now re-probes-or-rethrows instead of masking non-PK faults; + Unlike concurrency guard) / **UI 🔧→✅** — **3 blocking High fixed:** the 3.1 review-swap `site.ts` handler misfired on the nested like/comment swaps → **scoped by target element** with dedicated announcements ("Liked/Like removed, N likes", "Comment added/deleted"); like button given a stable `id` for in-place focus restore; comment thread restructured to a cards-only `#comment-items` list with the sentinel + **CSS-driven empty-state** as siblings (fixes just-posted-comment-placement + empty-state restore) — no migration needed. Deferred Lows → REVIEW_BACKLOG; **3.5 obligations recorded** (notification-message encoding; like re-notify dedup). Tests L1–L6 green (idempotent like + single notify, self-like no notification, `<script>` comment encoded, cross-user delete 403, public keyset no-overlap).
- **3.1 Current-user seam + Reviews CRUD — ✅ COMPLETE (2026-07-03)** — orchestrator-verified build **0/0**, **170/170** (Application 12→26, Web.IntegrationTests 60→68: +8 `ReviewTests` R1–R7 + 14 validator cases). **Foundation:** `ICurrentUser` port (`Application/Common/Interfaces`) + `CurrentUser` adapter (`Infrastructure/Identity`, reads `IHttpContextAccessor`→`NameIdentifier`; Infrastructure gained a `FrameworkReference Microsoft.AspNetCore.App` per ADR 0009) + `AddHttpContextAccessor()`; `ForbiddenAccessException` + **403 arm** on `GlobalExceptionHandler`; `Unit` (mediator void). **Verticals** (`Features/Reviews/`): `Create`/`Edit`/`DeleteReviewCommand` (+validators; ownership enforced in the handler → **403**, missing → 404; Create reconciles the `(UserId,MovieId)` unique race via `DbUpdateException`), `GetTitleReviewsQuery` (**public keyset** `(CreatedAtUtc DESC, Id DESC)`, N+1-free correlated-subquery counts, **excludes the signed-in viewer's own review** so it shows only pinned), `GetMyReviewForTitleQuery`→`ReviewVm?`, + `GetReviewCardQuery` / `Features/Catalog/GetCachedMovieIdQuery` (DRY helpers). **Web:** `ReviewsController` [Authorize] (create/edit/delete/card/edit-form/mine) with the **ADR-0008 `EnsureTitleCached`→`CreateReview` orchestration** (its first real consumer); public reviews-list on `DiscoveryController`; review UI embedded in `Details.cshtml` (replaces the 2.5 placeholder). Bodies plain-text, **Razor-encoded, no `Html.Raw`**, `white-space: pre-wrap`; server-resolved actor (no bound author id). **Migration:** `IX_Reviews_MovieId_CreatedAtUtc` (redundant standalone `IX_Reviews_MovieId` dropped) — applied to `CinoraTest`; **`CinoraDev` apply pending** (Express unreachable this session; `artifacts/migrate.sql` ready). **Review gates:** security ✅ (1 Med fixed: `UseRateLimiter` moved after `UseAuthentication` so the new per-user `social-write` policy isn't degraded to per-IP) / code ✅ (author-subquery collapse) / **UI 🔧→✅** — **4 blocking High fixed:** H1 duplicate DOM id (own review shown twice → excluded from public list), H2 focus lost after swaps (site.ts afterSwap focus-restore extended to review regions), H3 silent write-failure no-op (scoped `htmx:responseError` + inline `#review-error` + no-JS validation summary), H4 `aria-live` SR flooding (→ concise `#reviews-status`), + M1 graceful `_ReviewsError` retry partial for the lazy reads, M2 star-pill rating input. Proactively added the **`social-write` per-user rate-limit policy** on the review writes (§10.4). Deferred Lows → REVIEW_BACKLOG. Tests R1–R7 green (server-resolved actor, cross-user edit 403, cascade delete, public keyset no-overlap, `<script>` encoded, rating bounds 400).

### Phase 2 — Discovery  ✅ COMPLETE — exit gate PASS (2026-07-03)
- **Phase 2 exit gate — PASS (2026-07-03).** Whole-Discovery-surface holistic gate, build **0/0**, **148/148**. Five dimensions, all **0 Crit / 0 High**: **architecture** (dependency rule compiler-clean; CQRS purity — 3 read-only queries, 2 catalog commands the only writes, Details controller-orchestrates `EnsureTitleCachedCommand`; no TMDB DTO/Domain leak verified by grep; no MediatR/FluentAssertions) + **performance** (repeat reads served from `CachedTmdbClient`; Details = exactly one TMDB round-trip since the read warms the cache the command reuses; async/CT threaded; image sizes per context + CLS-safe; `public-read` limiter covers all TMDB-proxying reads) + **security** (XSS clean — all TMDB fields Razor-encoded, no `Html.Raw`; authZ fail-closed GET-only; CSP unchanged; write-on-GET idempotent/guarded/rate-limited) + **code** (2.5) + **UI** (2.5 Details — 2 blocking High **fixed in-milestone**). **OutputCache DEFERRED (accepted)** — network cost already absorbed by `CachedTmdbClient`; wiring needs delicate CSP-header/rate-limiter ordering (REVIEW_BACKLOG). **Outstanding: only the 2.7 live TMDB smoke** (manual browser pass; key is set).
- **2.5 Details + first-touch persistence — ✅ COMPLETE (2026-07-03)** — orchestrator-verified build **0/0**, **148/148** (Web.IntegrationTests 60: **+5** `DetailsTests` D1–D5). `GetTitleDetailsQuery(Media, TmdbId)→TitleDetailsVm?` (`ITmdbClient`-only, CQRS-pure; `FromDetails` maps genres→names, ranks+caps cast to 10, normalizes blank tagline/overview/character→null) + `CastMemberVm`. `DiscoveryController.Details` at **`/discover/title/{media}/{tmdbId:int}`** (`[AllowAnonymous]`, inherits `public-read`): parses the lowercase-enum media segment (`Enum.TryParse` ignoreCase + `Enum.IsDefined` + `tmdbId>0` → else **404**); reads the query → **404 on TMDB-null**; then dispatches the idempotent **`EnsureTitleCachedCommand` AFTER the read** (first-touch `Movie` persistence — ADR 0007/0008's first real display consumer; cache-hit, no extra TMDB call) inside a **graceful try/catch** (persistence failure logs Warning `LogTitlePersistFailed` EventId 2500 and still renders — a public browse page must not 500 on a DB hiccup). **Details.cshtml** (frontend-agent): backdrop hero (W1280) + a **frosted-panel title block (`bg-surface-1/95`)** for AA contrast over any backdrop, poster (W500), metadata (year · runtime · genre chips) + **labeled TMDB vote reference** (`role="img"`+aria-label, VoteCount==0 fallback), overview (Razor-encoded), top-billed cast row (W185, **keyboard-focusable** scroller), community-rating **placeholder** (Phase 3), inert add-to-watchlist **stub** (Phase 4), back-to-Discover. No CSP change. **Review gates:** architecture+perf ✅ / security ✅ / code ✅ (0 Crit/High/Med); **UI 🔧→✅** — found **2 blocking High** (hero scrim failed AA over bright backdrops; cast rail not keyboard-operable WCAG 2.1.1) → **both fixed** (frosted panel + opaque chip/pill fills + `ink-subtle`→`ink-muted`; cast `.rail-scroller` `tabindex=0`+aria-label). **Fixes also applied:** `Overview` whitespace-normalization (VM parity), `public-read` Prod permit 60→**120** (avoid false-positive 429), strengthened D1 assertions. Deferred Lows → REVIEW_BACKLOG (full-page graceful degradation, write-on-GET→Hangfire, genre-upsert DRY, cache-key log redaction, proxy ForwardedHeaders, persistence-failure test, stub/breadcrumb a11y polish).
- **2.4 Search — ✅ COMPLETE (2026-07-03)** — orchestrator-verified build **0/0**, **143/143** (Web.IntegrationTests 55: **+12** `SearchTests` S1–S12). New `SearchTitlesQuery` vertical (`ITmdbClient`-only, CQRS-pure; `HasMore = Page < min(TotalPages, 500)`) + the phase's **first FluentValidation validator** (`SearchTitlesQueryValidator`: `NotEmpty`, ≤100, `Page≥1`) + a `TitleCardVm.FromSummary` **DRY refactor** (rail + search share one projection). `DiscoveryController.Search` (full page — no-JS + deep-link baseline) + `SearchResults` (HTMX partial) with `MinQueryLength=2` **three-layer empty-query defense** (validator 400 on direct dispatch · action-guard 200 `_SearchPrompt` · Alpine min-length) and the 2.3 **200-not-500** failure grammar (`_SearchError` retry). New Web VMs `SearchPageViewModel`/`SearchPromptViewModel`/`SearchErrorViewModel`; 5 views (`Search` + `_SearchResults`/`_SearchResultsPage`/`_SearchPrompt`/`_SearchError`) with a **self-replacing `revealed` sentinel** for load-more (clean terminal state, no infinite scroll). **Frontend: `alpinejs` → `@alpinejs/csp@3.15.12`** (eval-free; CSP-safe name-only `x-*` markup) + `search` Alpine component + nav Search affordance; `SecurityHeadersMiddleware` **unchanged** (`script-src 'self'`, no eval — the only 2.4 security change was the npm swap). **Review gates: security ✅ / code ✅ (0 Crit/High); UI 🔧→✅** — found **1 High** (deep-link/no-JS→JS: `x-model` blanked the server-seeded box) + 2 a11y Med (`aria-live` flooded the whole grid; focus lost after Retry) → **all fixed** (Alpine `init()` seeds `query` from `data-query`; dedicated visually-hidden `#search-status` polite region; scoped `htmx:afterSwap` focus-restore) and re-verified. **Fixes also applied:** per-IP **`public-read` rate-limit policy** on the anonymous TMDB-proxying browse endpoints — Rail/Search/SearchResults (security Med; relaxed in Dev/Testing), `htmx.config.allowEval=false` (Low, defense-in-depth), clamped-page echo into the retry URL (Low). **2.7 live search smoke still pending** (needs the real TMDB key — key is already set). Backlog: restore-prompt-on-clear + Movie/Series toggle + out-of-order-response abort (Low).
- **2.3 Home / discovery rails — ✅ COMPLETE (2026-07-02)** — orchestrator-verified build **0/0**, **131/131** (Web.IntegrationTests 43: +6 Discovery incl. the **T6** rail-error regression). New `DiscoveryController` at **`/discover`** (`[AllowAnonymous]`): `GET /discover` shell + `GET /discover/rail?kind=&media=` lazy HTMX per-rail partial. `GetTitleRailQuery(RailKind, MediaType)→RailVm` (`ITmdbClient`-only, CQRS-pure) + `TitleCardVm`; `TmdbImageTagHelper` (`asp-tmdb-poster`, `w342`, lazy/async-decode, placeholder on null); premium dark UI (snap-scroll rails, glass 2:3 poster cards, hover lift, CSS shimmer skeletons, rating badge), Landing "Explore Cinora" CTA. CSP `img-src += https://image.tmdb.org`. **Decision:** genre chips **deferred to Details** (cards stay `ITmdbClient`-only); route `/discover` (marketing `/` + authed `/home` untouched); one query only (no ceremony `GetHomeFeedQuery` — shell is static controller config). **Review gates:** code+security ✅, performance ✅, UI 🔧→✅ — the UI gate found **1 High** (rails had no failure state → HTMX won't swap a non-2xx → infinite shimmer + stuck `aria-busy` on a TMDB outage) + 2 Med; **fixed** (controller returns a 200 `_RailError` retry partial; `hx-swap="outerHTML"` clears `aria-busy`; capped/narrowed edge-fade so it can't dim the focus ring or badge) → iteration-2 re-review **0 Crit/0 High ACCEPT**. Design spec: `phase-2-discovery-design.md` §10. **Live smoke ✅ (real TMDB → `CinoraDev`):** 3 rails × 20 real-poster items; repeat = in-memory cache hit (2580ms→2ms, +0 network calls); `SyncGenresCommand` idempotent (27 genres); `EnsureTitleCachedCommand(550)` → Fight Club persisted (1 `Movie` + 2 `MovieGenre` links) → idempotent `AlreadyCached`; independent SQL confirmed; the machine's transient TLS blip absorbed by the resilience handler. Default suite stays network-free (live harness scratchpad-only). **So §2.7 Home-rails live verification is satisfied.** Backlog (REVIEW_BACKLOG "Milestone 2.3" section): `OutputCache`→2.5 (Med), focus-restore-on-retry (Med), + 11 Low (LCP fade, skeleton CLS, motion durations, srcset, no-JS fallback, response compression, …).
- **2.2 fix round (F1–F5 + H-1) — ✅ COMPLETE (2026-07-02)** — orchestrator-verified build **0/0**, **125/125** (Application 12, incl. the H-1 SQLite regression). Applied: **F1** concurrency-safe `SyncGenres` (catch `DbUpdateException`→reload→reconcile + Warning log); **F2** `EnsureTitleCached` two-committed-step (genre upsert committed before the title write); **F3** blank-title→`NotFound`; **F4** tolerant numeric TMDB DTOs + mapper defaults. Review gates (code + architecture, parallel) surfaced **1 High (H-1)**: F2's leftover `Added` genres re-flushed into step-2's title `SaveChanges` → 500 (EF keeps `Added` after a failed save). **Fixed** with new Application port `IAppDbContext.DiscardPendingChanges()` (→ `ChangeTracker.Clear()`) called in the genre-race catch. Race tests migrated **EF InMemory → SQLite-in-memory** (real unique-index enforcement — the H-1 regression **fails pre-fix / passes post-fix**; EF InMemory could not reproduce it). Iteration-2 re-review **0 Crit / 0 High — ACCEPT**. ADR 0008 amended (two-step model + port affordance + F3 + testing decision). Backlog: narrow race catch to SQL 2601/2627 via a provider-agnostic classifier (Med); 3 Low; remove `GHSA-2m69-gcr7-jv3q` SQLite audit-suppress + bump Sqlite→10.0.8 when a patched native is cached.
- **2.2 Catalog persistence — ✅ COMPLETE (2026-07-02)** — orchestrator-verified build **0/0**, **120/120** (Web.IntegrationTests 37). `SyncGenresCommand` (union movie/tv genres, upsert by `TmdbGenreId`, idempotent) + `EnsureTitleCachedCommand` (`(TmdbId,MediaType)` existence probe → 404-no-write → `Movie.FromTmdb` + genre upsert/link + `DbUpdateException` race-reload) via `ISender`; `EnsureTitleOutcome` enum result; `FakeTmdbClient` for tests. **Decision: handlers use `IAppDbContext` directly (no `ITitleCatalog` port)** — consistent with §7 no-repository stance (design-doc port intentionally superseded). **TMDB data layer (2.0–2.2) COMPLETE. Batched review gates ✅ — architecture 0 Crit/0 High (verdict: sound to build Discovery on; direct-`IAppDbContext` = correct §7 call), code 0 Crit/0 High (Accept).** Fix round 🔧: concurrency-safe genre upsert (SyncGenres `DbUpdateException` parity + EnsureTitle genre-race isolation from the title write), blank-title→NotFound, tolerant numeric JSON, deterministic race-reload test; + ADR 0008 (commands-over-`ITitleCatalog`, port deferred to a Phase-3 trigger). Accepted no-fix: TTL coupling (perf note), SyncGenres validator exemption (parameterless), 24h genre-cache staleness on explicit sync.
- **2.1 TMDB caching decorator — ✅ COMPLETE (2026-07-02)** — orchestrator-verified build **0/0**, **116/116** (Infrastructure.Tests 30). Keys `cinora:tmdb:{rail|search|details|genres}:{media}:…`; TTLs = `CacheOptions.DefaultTtlSeconds` × per-resource factor ± jitter; never-cache-null, degrade-never-error.
- **2.0 TMDB client + adapter + resilience + options — ✅ COMPLETE (2026-07-02)** — orchestrator-verified build **0/0**, **103/103** (Domain 46, Application 7, Infrastructure.Tests 17 new, Web.IntegrationTests 33). `ITmdbClient` port (returns Application read models) + Infrastructure `TmdbClient` (typed HttpClient, v4 Bearer, `AddStandardResilienceHandler`) + `ITmdbImageUrlBuilder`; `TmdbOptions.ValidateOnStart` ON (+ dummy Testing key so the 85 tests keep booting); new `Cinora.Infrastructure.Tests` (fake-handler JSON fixtures). Mock-first.
- **Architecture design — ✅ accepted (orchestrator).** `docs/architecture/phase-2-discovery-design.md` + ADR 0006 (TMDB boundary: port returns Application read models, DTOs `internal` to Infra, standard resilience) + ADR 0007 (in-memory cache-aside decorator; genres eager via `SyncGenresCommand`, titles read-through first-touch via idempotent `EnsureTitleCachedCommand`). Build order: 2.0 client → 2.1 cache decorator → 2.2 persistence → 2.3 Home → 2.4 Search → 2.5 Details → review gates → **2.7 live (needs free TMDB v4 key)**. **DECISION: nightly Hangfire refresh DESCOPED from Phase 2** (YAGNI — no Phase-2 consumer; Hangfire introduced when a real job exists, e.g. Phase 5/6); optional 2.6 if user opts in. Phase-2 gates to honor in-milestone: `[AllowAnonymous]` public browse, CSP `img-src += image.tmdb.org`, `@alpinejs/csp` build at Search.

### Phase 1 — Foundation  ✅ COMPLETE — exit gate PASS (2026-07-02)
- **Phase 1 exit architecture review — PASS.** Whole-solution holistic gate: build **0/0**, **85/85**; dependency rule intact (Domain zero-dep; Application→Domain + EF abstractions; no Identity leak into Application public surface; §7 holds); free-only confirmed (no MediatR/FluentAssertions/paid SDKs anywhere); middleware ORDER + DI lifetimes coherent (Sender scoped, IUserRegistrationService scoped, IAssetManifest singleton-over-singletons, DatabaseHealthCheck per-scope — no captive deps). **0 Critical / 0 High / 0 code-level Medium.** Only documentation reconciliation outstanding (docs still name 'MediatR'; options names drift) → doc pass in flight. Sound to build Phase 2 on.
- **1.7 Local run scripts + `/health` — ✅ COMPLETE (2026-07-02).** Anonymous `/health` (`DatabaseHealthCheck` via `CanConnectAsync`, no new pkg) → 200 `Healthy`; `scripts/dev.ps1` (dotnet watch + npm watch) + `scripts/db-update.ps1` (idempotent); `README.md` local-setup + deployment note; env-typo guard (`Log.Warning` if not Development/Testing/Production). Orchestrator-verified: build **0/0**, **85/85** (Web.IntegrationTests 32), `/health` 200 anonymous, `db-update` reports up-to-date. **Phase-exit architecture gate running.**
- **1.6b Security hardening — ✅ COMPLETE (2026-07-02).** F2 `SecurityHeadersMiddleware` (nosniff, Referrer-Policy, strict CSP `default-src 'self'` byte-verified, restrictive Permissions-Policy) + `UseHsts` (non-Dev) + `UseHttpsRedirection`; F3 `AddRateLimiter` `"auth"` per-IP fixed-window (429; 10/min Prod, 10k/min Dev/Testing, `RateLimiting:AuthPermitLimit` override) on the 3 auth POSTs. Orchestrator-verified: build **0/0**, **84/84** (Web.IntegrationTests 31 incl. +2 headers/rate-limit tests); CSP loads same-origin hashed assets. **Bundled hardening pass (F1+F2+F3) COMPLETE.** CSP extension notes in REVIEW_BACKLOG.
- **1.6 UI/a11y review gate — done.** Text contrast measured AA-clean (5–18:1); design-brief adherence strong. **3 High (WCAG 2.2 AA — blocking): H1 input non-text border contrast (~1.3:1), H2 color-only links, H3 per-field error association**; + M1 mobile Home nav dead-end, M2/M3 + L1–L5 polish. Fix round ✅ done + orchestrator-verified: H1 input border **3.57:1** (was 1.30), H2 links underlined at rest, H3 per-field `aria-describedby`/`aria-invalid`, M1 mobile Home affordance, M2/M3 + L1–L5; build **0/0**, **84/84**, assets regenerated. **Milestone 1.6 CLOSED.** Live axe/Lighthouse/keyboard pass deferred (confirm under real `backdrop-filter`).
- **1.6a Tailwind v4 + TypeScript + base layout — ✅ COMPLETE (2026-07-02).** npm build (Tailwind v4 CLI + esbuild) → **content-hashed + minified** `wwwroot/dist` (`app-<hash>.css`, `site-<hash>.js` incl. Alpine + HTMX, `manifest.json`) resolved via `IAssetManifest`; strict `tsconfig`; publish-gated MSBuild target (`npm ci && npm run build`). Dark-luxury `@theme` tokens + `.btn`/`.card-glass`/`.input` components; glass `_Layout` (skip-link, auth-aware sticky nav, anti-forgery meta, reduced-motion); restyled Landing/Login/Register/Home/Denied (auth POST contract + `__RequestVerificationToken` preserved). HTMX `RequestVerificationToken` header wired (`AddAntiforgery` HeaderName + `site.ts` `htmx:configRequest`). All free/OSS npm (MIT/0BSD), bundled locally (no CDN). Orchestrator-verified: build **0/0**, **82/82**, hashed assets present, publish emits them.
- **1.5 Options — ✅ COMPLETE (2026-07-02).** Free-named `TmdbOptions`/`AiOptions`/`CacheOptions`/`FileStorageOptions` in Infrastructure/Options; bound in `AddInfrastructure` with lazy data-annotation validation (no `ValidateOnStart` — boots keyless, fails on first consumption per §6); non-secret defaults in `appsettings.json`; TMDB key via user-secrets. Package-free `ValidateUsingDataAnnotations()` (honors no-new-packages; swappable for the free first-party pkg later). Orchestrator-verified: build **0/0**, **82/82** (Domain 46, Application 7, Web.IntegrationTests 29).
- **1.4 Pipeline + global exception handling + Serilog — ✅ COMPLETE (2026-07-02).** Hand-rolled mediator (`ISender`/`IRequest`/`IRequestHandler`/`IPipelineBehavior` + `Sender` w/ cached typed dispatch — no MediatR); behaviors Logging→Validation→Performance; `AddApplication()` auto-scans handlers + FluentValidation validators; `ValidationException`/`NotFoundException`; `GlobalExceptionHandler : IExceptionHandler` (ValidationException→400 ValidationProblemDetails · NotFoundException→404 · **DomainException→400 [M2 RESOLVED]** · else→500 generic, no stack trace, logged); two-stage Serilog (Console + rolling File `logs/`, Microsoft/EF→Warning) + `UseSerilogRequestLogging`; env-guarded `[AllowAnonymous]` GET-only diagnostics endpoint (404 in Production) to exercise the pipeline over HTTP. FluentValidation 12.1.1 + Serilog (Apache-2.0, free). Orchestrator-verified: build **0/0**, **74/74** (Domain 46, Application 5, Web.IntegrationTests 23). Code review ✅ **ACCEPT** (0 Crit/0 High; 2 Med, 4 Low). Fix round in progress: #1 make `UseSerilogRequestLogging` outermost (mapped 4xx no longer logged as Error/500 w/ stack trace), #2 fail loud on duplicate handler registration, #3 tighten diagnostics guard to Dev/Testing, + behavior-order & error-path log-level lock-in tests. Accepted (no change): #5 `Async`-suffix omission (deliberate mediator-API parity); backlog: #4 covariant-dispatch cast (theoretical). Fix round ✅ done + orchestrator-verified (#1 Serilog-outermost, #2 fail-loud duplicate handlers, #3 diagnostics guard→Dev/Testing, + behavior-order & error-path log-level lock-in tests): build **0/0**, **77/77** (Domain 46, Application 7, Web.IntegrationTests 24). **Milestone 1.4 CLOSED.**
- **1.3 Identity + Google OAuth + Landing/Login — 🔧 step 1/3 done (security-agent): auth vertical.** MVC/Razor wired; `AddIdentity` + EF stores; cookie policy (`SameSite=Lax`, `SecurePolicy=Always`, sliding); conditional `AddGoogle` (boots without creds); `AccountController` (register/login/logout/Google) + `HomeController` (public Landing `/`, `[Authorize]` `/home`); **atomic registration via `IUserRegistrationService` + explicit `IDbContextTransaction`** (1.3 blocking requirement); anti-forgery on all POSTs; generic login errors; no email auto-linking; minimal functional views; `public partial class Program` for tests. Orchestrator-verified: build **0/0**, tests **49/49**, boot smoke w/o Google creds `/`→200, `/account/login`→200, `/account/register`→200, `/home`→302→login. Caveats for later: `EnableRetryOnFailure` must stay off (user-txn), tests need `https://localhost` base (Secure cookie). Step 2 ✅ integration tests (testing-agent): 9 new tests (11 in project, **58 total**), incl. the blocking orphan-prevention test passing vs real LocalDB `CinoraTest` (real migration DDL, not in-memory). Orchestrator-verified: build 0/0, **58/58**. **View styling folded into Milestone 1.6** (Tailwind/design system) — current views are functional/accessible placeholders, so no separate frontend step; UI review deferred to 1.6. 1.3 review gates ✅ — **security (adversarial) 0 Crit/0 High**, **code/layering ACCEPT 0 Crit/0 High**. Core controls verified sound (atomic rollback, anti-forgery, open-redirect guard, no OAuth takeover, generic login errors, secure cookies, §7 no-MediatR). Security hardening round ✅ done + orchestrator-verified (build 0/0, **58/58**): global fallback authZ + global anti-forgery filter, enumeration→generic, `email_verified` mapping+check, external-cookie cleanup, rollback `CancellationToken.None`, null-guard, retry comment. **New global contracts (see REVIEW_BACKLOG): fail-closed authZ + opt-out anti-forgery — 1.4/1.6/1.7/Phase-2 briefs must honor.** Testing follow-up round ✅ done + orchestrator-verified: +6 methods / 7 cases (CR1 duplicate-email rollback, CR2 external-registration happy/rollback/duplicate + `EmailConfirmed`↔`emailVerified`, CR5 open-redirect both branches), deleted vacuous smoke test. **Milestone 1.3 COMPLETE — build 0/0, 64/64 tests** (Domain 46, Application 1, Web.IntegrationTests 17). Deferred to bundled hardening pass (before Phase 1 exit): security headers/CSP (needs 1.6) + rate limiting. UI review deferred to 1.6, performance to Phase 2.
- **1.2 EF Core DbContext + configs + `InitialCreate` migration — ✅ COMPLETE (2026-07-02).** `CinoraDbContext : IdentityDbContext<ApplicationUser, IdentityRole<Guid>, Guid>` implementing `IAppDbContext`; `ApplicationUser` (Infrastructure); 12 `internal sealed` entity configs; `AddInfrastructure` + design-time factory; connection string in `appsettings.Development.json` (LocalDB). All handoffs implemented: composite keys (`MovieGenre`,`ReviewLike`), unique indexes (`Review`/`Watchlist`(UserId,MovieId), `Movie.TmdbId`, `Genre.TmdbGenreId`), shared-PK 1:1 + `ValueGeneratedNever`, `Rating`↔int converter + `CK_Review_Rating`, `Restrict` on all User FKs + `Friend` self-refs (cascade only on owned children), explicit column lengths, 27-genre `HasData` seed (constant Guids). EF pkgs pinned 10.0.8; Application gets EF **abstractions** only (bounded exception). Resolved `IdentityDbContext.Users` vs `IAppDbContext.Users` collision via explicit interface impl; scoped CA1861 for generated migration code. Independently re-verified by orchestrator: build **0/0**; `dotnet test` **48/48**; `dotnet ef migrations list` → `InitialCreate` **applied** to `(localdb)\MSSQLLocalDB\CinoraDev` (20 tables, 27 genres, CHECK present). **Review gates passed — code review 0 Crit/0 High/1 Med/2 Low, architecture review 0 Crit/0 High/1 Med/5 Low, both "accept".** Polish round ✅ done (applied directly by orchestrator — the delegated agent returned an anomalous empty result, 0 tool uses; I made the edits and verified): Movie unique index now composite `(TmdbId, MediaType)` (`InitialCreate` regenerated → `20260702065014_InitialCreate`, re-applied to LocalDB, `IX_Movies_TmdbId_MediaType` unique); connection-string fail-fast guard in `AddInfrastructure`; `Users`-table guard test. Re-verified: build **0/0**, `has-pending-model-changes` → none, tests **49/49**. **1.3 blocking requirement recorded: atomic registration transaction.** Deferrals in `REVIEW_BACKLOG.md`. (Security review deferred to 1.3, performance to Phase 2.)
- **1.1 fix round — ✅ verified (2026-07-02):** M1 closed (`Rating.IsValid` + `Review` guards against `default`), 2 test improvements. Re-verified: build 0/0, Domain tests **46/46**.
- **1.1 Domain entities + `Rating` value object — ✅ COMPLETE (2026-07-02).** 12 entities (private ctors + factory methods + private setters, users referenced by `Guid`), `Rating` value object (1–10, `readonly record struct`), 4 enums, internal `Guard`, `DomainException`. Domain remains zero-dependency. Independently re-verified by orchestrator: `dotnet build -c Release` → **0 warnings / 0 errors**; Domain tests **43/43** pass; full solution **45/45**. Notes for 1.2: max-length `const`s exposed for reuse; `Rating`↔int value converter needed; `[SuppressMessage]` CA1716 on `Friend` (VB keyword; C#-only project). **Review gates passed — code review 0 Crit/0 High/1 Med/4 Low, architecture review 0 Crit/0 High/2 Med/3 Low, both "accept".** Fix round in progress (M1 `Rating` default-value guard in `Review` + `IsValid` + doc; 2 test improvements); deferred findings and 1.2 handoffs recorded in `REVIEW_BACKLOG.md`.
- **1.0 Solution skeleton — ✅ COMPLETE (2026-07-02).** 7-project Clean Architecture solution (`Cinora.sln`), `Directory.Build.props` + `Directory.Packages.props` (CPM), `.editorconfig`, `global.json` (SDK 10.0.301, rollForward latestFeature), `.gitignore`. Independently re-verified by orchestrator: `dotnet build -c Release` → **0 warnings / 0 errors**; `dotnet test` → **3/3** smoke tests pass; reference edges enforce the dependency rule (Domain→none, Application→Domain, Infrastructure→Application, Web→Application+Infrastructure). Free/local only; no packages beyond the xUnit test toolchain. _Architecture verified directly (reference-edge + layering check); full multi-agent review gates begin at 1.1 where there is real code. Test assertion library resolved: **xUnit built-in `Assert`** (FluentAssertions banned)._
- 2026-07-02 — Orchestration started. Environment verified. Architecture design accepted (`solution-structure.md` + ADR 0001–0003).
